// One-shot, exact-base branch repair. Removed before the PR is merged.
import fs from 'node:fs'
import { createHash } from 'node:crypto'
import { spawnSync } from 'node:child_process'
import assert from 'node:assert/strict'

const root = process.cwd()
const paths = {
  refresh: 'backend/services/matching/staleMatchExplainRefresh.js',
  invariants: 'backend/startup/enforceInvariants.js',
  budgetTests: 'tests/unit/stale-match-refresh-budget.test.mjs',
  receiptTests: 'backend/tests/staleRefreshInvariantReceipt.test.js',
}
const read = path => fs.readFileSync(path, 'utf8').replace(/\r\n/g, '\n')
const hash = text => { const bytes = Buffer.from(text); return createHash('sha1').update(`blob ${bytes.length}\0`).update(bytes).digest('hex') }
const exact = (text, before, after) => { assert.equal(text.split(before).length, 2, 'Patch anchor must occur exactly once'); return text.replace(before, after) }
const run = (args, expected) => {
  const result = spawnSync(process.execPath, args, { cwd: root, encoding: 'utf8', timeout: 180000, maxBuffer: 10 * 1024 * 1024 })
  const output = String(result.stdout ?? '') + String(result.stderr ?? '')
  console.log(output.slice(-12000))
  assert.equal(result.status, expected, `Unexpected exit for ${args.join(' ')}`)
  if (expected === 1) assert.match(output, /AssertionError/, 'The red test must fail on assertions, not infrastructure')
}

let refresh = read(paths.refresh)
let invariants = read(paths.invariants)
const budgetTests = read(paths.budgetTests)
assert.equal(hash(refresh), '289a06a52c8e2004d5ae096ded7ef61fded311f1')
assert.equal(hash(invariants), 'b65d14d89af8337beb39b8d2c13e4710faaf8a9a')
assert.equal(hash(budgetTests), '00382d21cceffd6fccae03f22ae1c47ae4246bfd')
assert.equal(fs.existsSync(paths.receiptTests), false)

const budgetTestsAppend = "\ntest('completion readback uses current calendar freshness, not the initial page timestamp', async () => {\n  const OriginalDate = globalThis.Date\n  let time = OriginalDate.parse('2027-06-30T23:59:59Z')\n  globalThis.Date = class extends OriginalDate {\n    constructor(...args) { super(...(args.length ? args : [time])) }\n    static now() { return time }\n  }\n  const { raw, adapter, deps } = fixture(1)\n  try {\n    raw.prepare('UPDATE profile_opportunity_matches SET match_explain_json=?').run(JSON.stringify({\n      scoring_policy_version: 'current', signal_version: PROFILE_SIGNAL_VERSION,\n      matchedNeeds: [], school_origin_period: '2027-H1',\n    }))\n    const rollover = faultAdapter(adapter, (sql, prepare) => {\n      const statement = prepare()\n      if (!sql.includes('m.id AS match_id')) return statement\n      return { ...statement, all: (...args) => {\n        const rows = statement.all(...args)\n        time = OriginalDate.parse('2027-07-01T00:00:01Z')\n        return rows\n      } }\n    })\n    const result = await runStaleMatchExplainRefresh(rollover, { deps })\n    assert.equal(result.refreshed, 0)\n    assert.equal(result.remaining_candidates, 1)\n    assert.equal(result.complete, false)\n    assert.equal(result.status, 'pending')\n  } finally { raw.close(); globalThis.Date = OriginalDate }\n})\n\ntest('malformed JSON with current marker text cannot be declared fully refreshed', async () => {\n  const { raw, adapter, deps } = fixture(1)\n  try {\n    raw.prepare('UPDATE profile_opportunity_matches SET match_explain_json=?').run(\n      `{\"scoring_policy_version\":\"current\",\"signal_version\":\"${PROFILE_SIGNAL_VERSION}\",\"matchedSignals\":[]`,\n    )\n    const result = await runStaleMatchExplainRefresh(adapter, { deps })\n    assert.equal(result.remaining_candidates, 0)\n    assert.equal(result.remaining_stale, 1)\n    assert.equal(result.verification_scanned, 1)\n    assert.equal(result.complete, false)\n    assert.equal(result.status, 'pending')\n  } finally { raw.close() }\n})\n\ntest('an exact-audit row limit cannot turn a partial inspection into completion', async () => {\n  const { raw, adapter, deps } = fixture(3)\n  try {\n    raw.prepare('UPDATE profile_opportunity_matches SET match_explain_json=?').run(JSON.stringify({\n      scoring_policy_version: 'current', signal_version: PROFILE_SIGNAL_VERSION, matchedNeeds: [],\n    }))\n    const limited = await runStaleMatchExplainRefresh(adapter, { verificationRowBudget: 2, deps })\n    assert.equal(limited.remaining_candidates, 0)\n    assert.equal(limited.remaining_stale, null)\n    assert.equal(limited.verification_scanned, 2)\n    assert.equal(limited.verification_truncated, true)\n    assert.equal(limited.complete, false)\n    const verified = await runStaleMatchExplainRefresh(adapter, { verificationRowBudget: 4, deps })\n    assert.equal(verified.remaining_stale, 0)\n    assert.equal(verified.complete, true)\n  } finally { raw.close() }\n})\n\ntest('failed exact verification is not hidden by a successful zero-candidate count', async () => {\n  const { raw, adapter, deps } = fixture(0)\n  try {\n    const fault = faultAdapter(adapter, (sql, prepare) => {\n      if (sql.includes('verification_explain')) throw Error('exact audit unavailable')\n      return prepare()\n    })\n    const result = await runStaleMatchExplainRefresh(fault, { deps })\n    assert.equal(result.remaining_candidates, 0)\n    assert.equal(result.remaining_stale, null)\n    assert.equal(result.verification_failed, true)\n    assert.equal(result.complete, false)\n    assert.equal(result.status, 'failed')\n  } finally { raw.close() }\n})\n\ntest('an expired time budget leaves exact verification incomplete, not a false all-clear', async () => {\n  const { raw, adapter, deps } = fixture(1)\n  try {\n    raw.prepare('UPDATE profile_opportunity_matches SET match_explain_json=?').run(JSON.stringify({\n      scoring_policy_version: 'current', signal_version: PROFILE_SIGNAL_VERSION, matchedNeeds: [],\n    }))\n    const result = await runStaleMatchExplainRefresh(adapter, { timeBudgetMs: 0, deps })\n    assert.equal(result.remaining_candidates, 0)\n    assert.equal(result.remaining_stale, null)\n    assert.equal(result.verification_truncated, true)\n    assert.equal(result.complete, false)\n  } finally { raw.close() }\n})\n"
const receiptTests = "import { afterAll, beforeEach, expect, it, vi } from 'vitest'\nimport Database from 'better-sqlite3'\n\nconst refresh = vi.hoisted(() => vi.fn())\nvi.mock('../services/matching/staleMatchExplainRefresh.js', () => ({ runStaleMatchExplainRefresh: refresh }))\nconst previousKey = process.env.RUNTIME_SECRETS_KEY\nprocess.env.RUNTIME_SECRETS_KEY = previousKey || 'b'.repeat(64)\nconst { enforceStaleMatchExplainRefresh, projectPersistedStep } = await import('../startup/enforceInvariants.js')\nafterAll(() => {\n  if (previousKey === undefined) delete process.env.RUNTIME_SECRETS_KEY\n  else process.env.RUNTIME_SECRETS_KEY = previousKey\n})\nbeforeEach(() => refresh.mockReset())\n\nfunction result(overrides = {}) {\n  return {\n    ok: true, scanned: 2, refreshed: 2, would_refresh: 0, write_enabled: true,\n    truncated: false, remaining_candidates: 0, remaining_stale: 0,\n    verification_scanned: 2, verification_truncated: false, verification_failed: false,\n    verified_at: '2026-09-18T00:00:00.000Z', complete: true, status: 'complete',\n    unscorable: 0, skipped_no_profile: 0, convergence_errors: 0,\n    concurrent_changes_skipped: 0, ...overrides,\n  }\n}\nasync function project(value) {\n  refresh.mockResolvedValue(value)\n  const step = await enforceStaleMatchExplainRefresh({})\n  return { step, persisted: projectPersistedStep(step) }\n}\n\nit('keeps a completion verification failure failed through the real wrapper and projection', async () => {\n  const { step, persisted } = await project(result({ ok: false, remaining_candidates: null, remaining_stale: null,\n    verification_failed: true, verified_at: null, complete: false, status: 'failed' }))\n  expect(step.ok).toBe(false)\n  expect(persisted).toMatchObject({ ok: false, repaired: 2, complete: false, status: 'failed',\n    remaining_candidates: null, remaining_stale: null, verification_failed: true, verified_at: null })\n})\nit('persists verified zero counts and explicit false flags instead of omitting them', async () => {\n  const { persisted } = await project(result())\n  expect(persisted).toMatchObject({ ok: true, complete: true, status: 'complete', remaining_candidates: 0,\n    remaining_stale: 0, verification_failed: false, verification_truncated: false, verification_scanned: 2 })\n})\nit('distinguishes a pending batch from the whole drain being complete', async () => {\n  const { persisted } = await project(result({ remaining_candidates: 4, remaining_stale: null,\n    complete: false, status: 'pending', truncated: true }))\n  expect(persisted).toMatchObject({ ok: true, complete: false, status: 'pending', remaining_candidates: 4,\n    remaining_stale: null, truncated: true })\n})\nit('carries exact stale evidence even when the substring candidate count is zero', async () => {\n  const { persisted } = await project(result({ remaining_stale: 1, complete: false, status: 'pending' }))\n  expect(persisted).toMatchObject({ remaining_candidates: 0, remaining_stale: 1, complete: false, status: 'pending' })\n})\nit('retains incomplete exact-audit evidence without converting unknown into zero', async () => {\n  const { persisted } = await project(result({ remaining_stale: null, verification_truncated: true,\n    complete: false, status: 'pending' }))\n  expect(persisted).toMatchObject({ remaining_stale: null, verification_truncated: true, complete: false })\n})\nit('keeps disabled writes separate from an enabled completed drain', async () => {\n  const { step, persisted } = await project(result({ write_enabled: false, refreshed: 0, would_refresh: 2,\n    remaining_candidates: 2, remaining_stale: null, complete: false, status: 'disabled' }))\n  expect(step.wouldRepair).toBe(2)\n  expect(persisted).toMatchObject({ enforced: false, repaired: 0, complete: false, status: 'disabled' })\n})\nit('keeps unresolved profile, scoring and persistence counters in the durable receipt', async () => {\n  const { persisted } = await project(result({ ok: false, unscorable: 1, skipped_no_profile: 2,\n    convergence_errors: 3, concurrent_changes_skipped: 4, complete: false, status: 'failed' }))\n  const db = new Database(':memory:')\n  try {\n    db.exec('CREATE TABLE system_kv (key TEXT PRIMARY KEY, value TEXT)')\n    db.prepare('INSERT INTO system_kv VALUES (?, ?)').run('receipt', JSON.stringify(persisted))\n    const readback = JSON.parse(db.prepare('SELECT value FROM system_kv WHERE key=?').get('receipt').value)\n    expect(readback).toMatchObject({ ok: false, unscorable: 1, skipped_no_profile: 2,\n      convergence_errors: 3, concurrent_changes_skipped: 4, complete: false, status: 'failed' })\n  } finally { db.close() }\n})\nit('does not widen the diagnostic projection for unrelated invariant steps', () => {\n  expect(projectPersistedStep({ name: 'unrelated', ok: true, complete: true, status: 'complete',\n    remaining_candidates: 0, verification_scanned: 10 })).toEqual({ name: 'unrelated', ok: true, repaired: 0, scanned: 0 })\n})\nit('does not fabricate a verified backlog after the worker throws', async () => {\n  refresh.mockRejectedValue(Error('fixture worker failure'))\n  const persisted = projectPersistedStep(await enforceStaleMatchExplainRefresh({}))\n  expect(persisted.ok).toBe(false)\n  expect(persisted.error).toContain('fixture worker failure')\n  expect(persisted.remaining_candidates).toBeUndefined()\n})\n"

fs.writeFileSync(paths.budgetTests, budgetTests + budgetTestsAppend)
fs.writeFileSync(paths.receiptTests, receiptTests)
run(['--test', paths.budgetTests], 1)
run(['scripts/run-vitest-isolated.mjs', 'run', paths.receiptTests], 1)
console.log('REVIEW_REGRESSIONS_RED_CONFIRMED')
refresh = exact(refresh, " * @param {number} [opts.timeBudgetMs]\n", " * @param {number} [opts.timeBudgetMs]\n * @param {number} [opts.verificationRowBudget] Read-only exact-audit cap; default 10000, maximum 10000.\n")
refresh = exact(refresh, "  const writeEnabled = opts.writeEnabled !== false &&", "  const verificationRowBudget = Number.isFinite(opts.verificationRowBudget)\n    ? Math.max(0, Math.min(10000, Math.floor(opts.verificationRowBudget))) : 10000\n  const writeEnabled = opts.writeEnabled !== false &&")
refresh = exact(refresh, "    remaining_candidates: null,\n", "    remaining_candidates: null,\n    remaining_stale: null,\n    verification_scanned: 0,\n    verification_truncated: false,\n")
refresh = exact(refresh, "  // A page finishing is not evidence that the backlog is empty. Count the\n  // same active-catalog SQL candidate scope after writes, not rows attempted.\n  // The predicate is deliberately conservative: this is a candidate count,\n  // not a claim that every remaining row fails the JS freshness predicate.\n  const finish = async () => {\n    try {\n      const row = await db.prepare(\n        `SELECT COUNT(*) AS remaining_candidates\n           FROM profile_opportunity_matches m\n           JOIN funding_opportunities fo ON fo.id = m.opportunity_id\n          WHERE ${stalePred}\n            AND (fo.is_active IS NULL OR fo.is_active = ${isPg ? 'TRUE' : '1'})`,\n      ).get()\n      const count = row?.remaining_candidates\n      const numeric = typeof count === 'number' || (typeof count === 'string' && /^[0-9]+$/.test(count))\n      const remaining = Number(count)\n      if (!numeric || !Number.isSafeInteger(remaining) || remaining < 0) throw new Error('Invalid remaining-candidate count')\n      summary.remaining_candidates = remaining\n      summary.verified_at = new Date().toISOString()\n    } catch {\n      summary.ok = false\n      summary.verification_failed = true\n    }\n    summary.complete = summary.ok && writeEnabled && summary.remaining_candidates === 0 &&\n      summary.unscorable === 0 && summary.skipped_no_profile === 0\n    summary.status = !summary.ok ? 'failed' : !writeEnabled ? 'disabled' : summary.complete ? 'complete' : 'pending'\n    summary.elapsed_ms = Date.now() - startedAt\n    // Aggregate-only receipt: no applicant identifiers, URLs, titles or evidence.\n    log.info('batch receipt', summary)\n    return summary\n  }\n", "  // A page finishing is not evidence that the backlog is empty. Count the\n  // same active-catalog SQL candidate scope after writes, not rows attempted.\n  // SQL uses marker text, so it can both over-select current rows and miss\n  // malformed JSON carrying those markers. A zero needs an exact JS audit.\n  const finish = async () => {\n    // Calendar-sensitive evidence may expire while this batch is running.\n    const remainingPred = staleMatchExplainSql('m')\n    try {\n      const row = await db.prepare(\n        `SELECT COUNT(*) AS remaining_candidates\n           FROM profile_opportunity_matches m\n           JOIN funding_opportunities fo ON fo.id = m.opportunity_id\n          WHERE ${remainingPred}\n            AND (fo.is_active IS NULL OR fo.is_active = ${isPg ? 'TRUE' : '1'})`,\n      ).get()\n      const count = row?.remaining_candidates\n      const numeric = typeof count === 'number' || (typeof count === 'string' && /^[0-9]+$/.test(count))\n      const remaining = Number(count)\n      if (!numeric || !Number.isSafeInteger(remaining) || remaining < 0) throw new Error('Invalid remaining-candidate count')\n      summary.remaining_candidates = remaining\n      if (remaining === 0 && summary.ok && writeEnabled) {\n        if (Date.now() - startedAt >= timeBudgetMs) summary.verification_truncated = true\n        else {\n          const audit = await db.prepare(\n            `SELECT m.match_explain_json AS verification_explain\n               FROM profile_opportunity_matches m\n               JOIN funding_opportunities fo ON fo.id = m.opportunity_id\n              WHERE (fo.is_active IS NULL OR fo.is_active = ${isPg ? 'TRUE' : '1'})\n              ORDER BY m.id LIMIT ?`,\n          ).all(verificationRowBudget + 1)\n          let stale = 0\n          summary.verification_truncated = audit.length > verificationRowBudget\n          for (const candidate of audit.slice(0, verificationRowBudget)) {\n            if (Date.now() - startedAt >= timeBudgetMs) {\n              summary.verification_truncated = true\n              break\n            }\n            summary.verification_scanned += 1\n            if (isStaleMatchExplain(candidate.verification_explain)) stale += 1\n          }\n          // Do not combine observations made under different calendar rules.\n          if (remainingPred !== staleMatchExplainSql('m')) summary.verification_truncated = true\n          if (!summary.verification_truncated) summary.remaining_stale = stale\n        }\n      }\n      summary.verified_at = new Date().toISOString()\n    } catch {\n      summary.ok = false\n      summary.verification_failed = true\n    }\n    summary.complete = summary.ok && writeEnabled && summary.remaining_candidates === 0 &&\n      summary.remaining_stale === 0 && !summary.verification_truncated && summary.unscorable === 0 && summary.skipped_no_profile === 0\n    summary.status = !summary.ok ? 'failed' : !writeEnabled ? 'disabled' : summary.complete ? 'complete' : 'pending'\n    summary.elapsed_ms = Date.now() - startedAt\n    // Aggregate-only receipt: no applicant identifiers, URLs, titles or evidence.\n    log.info('batch receipt', summary)\n    return summary\n  }\n")
assert.equal(hash(refresh), "75392396e357a8be02cbb88e2d410a7316f0aa5c", "Refresh bytes differ from the locally verified correction")
invariants = exact(invariants, "  for (const key of PERSISTED_STEP_DIAGNOSTICS) {\n    if (s[key] !== undefined) out[key] = s[key]\n  }\n", "  for (const key of PERSISTED_STEP_DIAGNOSTICS) {\n    if (s[key] !== undefined) out[key] = s[key]\n  }\n  // The stale-refresh receipt distinguishes successful work from verified\n  // completion. Preserve explicit zero, false and null without affecting\n  // unrelated invariant records.\n  if (s.name === 'stale_match_explain_refresh') {\n    for (const key of [\n      'remaining_candidates', 'remaining_stale', 'verification_scanned',\n      'verification_truncated', 'verification_failed', 'verified_at',\n      'complete', 'status', 'unscorable', 'skipped_no_profile',\n      'convergence_errors', 'concurrent_changes_skipped', 'skipped',\n    ]) {\n      if (s[key] !== undefined) out[key] = s[key]\n    }\n  }\n")

const wrapperStart = invariants.indexOf('export async function enforceStaleMatchExplainRefresh(db) {')
assert.ok(wrapperStart >= 0)
const wrapperEnd = invariants.indexOf('\n}\n', wrapperStart) + 3
assert.ok(wrapperEnd > wrapperStart)
let wrapper = invariants.slice(wrapperStart, wrapperEnd)
wrapper = exact(wrapper, '    return {\n      scanned: res.scanned ?? 0,', '    return {\n      ...res,\n      scanned: res.scanned ?? 0,')
wrapper = exact(wrapper, "      return { scanned: 0, repaired: 0, enforced: true, skipped: 'deps' }", "      return { ok: false, scanned: 0, repaired: 0, enforced: false, skipped: 'deps',\n        remaining_candidates: null, remaining_stale: null, verification_failed: true,\n        complete: false, status: 'failed' }")
invariants = invariants.slice(0, wrapperStart) + wrapper + invariants.slice(wrapperEnd)
fs.writeFileSync(paths.refresh, refresh)
fs.writeFileSync(paths.invariants, invariants)
run(['--check', paths.refresh], 0)
run(['--check', paths.invariants], 0)
run(['--test', paths.budgetTests], 0)
run(['scripts/run-vitest-isolated.mjs', 'run', paths.receiptTests,
  'backend/tests/staleMatchExplainRefresh.test.js', 'backend/tests/fourTruthProofRefresh.test.js',
  'backend/tests/enforceInvariants.test.js'], 0)
console.log('REVIEW_REGRESSIONS_GREEN_CONFIRMED')
for (const path of Object.values(paths)) console.log('GENERATED_BLOB', path, hash(read(path)))
