# GrantFlow – Nginx reverse proxy configuration for Digital Ocean
# Copy to: /etc/nginx/sites-available/grantflow.conf
# Enable: ln -s /etc/nginx/sites-available/grantflow.conf /etc/nginx/sites-enabled/grantflow.conf

# Rate limiting zones
limit_req_zone $binary_remote_addr zone=grantflow_api:10m rate=30r/m;

server {
    listen 80;
    server_name app.axiombiolabs.org www.axiombiolabs.org;

    # Redirect HTTP → HTTPS (Cloudflare handles TLS termination)
    # If Cloudflare Flexible SSL is used, remove this block and keep the location blocks below.
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name app.axiombiolabs.org www.axiombiolabs.org;

    # SSL certificates (use Cloudflare Origin Certificate or Let's Encrypt)
    ssl_certificate     /etc/ssl/certs/grantflow.crt;
    ssl_certificate_key /etc/ssl/private/grantflow.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # Cloudflare real IP restoration
    set_real_ip_from 103.21.244.0/22;
    set_real_ip_from 103.22.200.0/22;
    set_real_ip_from 103.31.4.0/22;
    set_real_ip_from 104.16.0.0/13;
    set_real_ip_from 104.24.0.0/14;
    set_real_ip_from 108.162.192.0/18;
    set_real_ip_from 131.0.72.0/22;
    set_real_ip_from 141.101.64.0/18;
    set_real_ip_from 162.158.0.0/15;
    set_real_ip_from 172.64.0.0/13;
    set_real_ip_from 173.245.48.0/20;
    set_real_ip_from 188.114.96.0/20;
    set_real_ip_from 190.93.240.0/20;
    set_real_ip_from 197.234.240.0/22;
    set_real_ip_from 198.41.128.0/17;
    set_real_ip_from 2400:cb00::/32;
    set_real_ip_from 2606:4700::/32;
    set_real_ip_from 2803:f800::/32;
    set_real_ip_from 2405:b500::/32;
    set_real_ip_from 2405:8100::/32;
    set_real_ip_from 2a06:98c0::/29;
    set_real_ip_from 2c0f:f248::/32;
    real_ip_header CF-Connecting-IP;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # API proxy → backend on localhost:4000
    location /grantflow/api/ {
        limit_req zone=grantflow_api burst=20 nodelay;

        proxy_pass         http://127.0.0.1:4000/api/;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
        proxy_read_timeout 120s;

        # Increase body limit for document uploads (matches backend 10 MB limit)
        client_max_body_size 12m;
    }

    # Health check (no rate limit, needed for monitoring)
    location = /grantflow/api/health {
        proxy_pass       http://127.0.0.1:4000/api/health;
        proxy_set_header Host $host;
        access_log off;
    }

    # Static frontend assets (built by `npm run build`)
    location /grantflow/ {
        alias /var/www/html/grantflow/;

        # SPA fallback: unknown sub-paths → index.html
        try_files $uri $uri/ /grantflow/index.html;

        # Cache immutable hashed assets aggressively
        location ~* \.(js|css|woff2?|ttf|svg|png|ico|webp)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            access_log off;
        }
    }

    # Redirect bare /grantflow to /grantflow/
    location = /grantflow {
        return 301 /grantflow/;
    }

    # Deny hidden files
    location ~ /\. {
        deny all;
    }
}
