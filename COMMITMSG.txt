Fix async withTransaction for SQLite: add serialized async path

SqliteDb.withTransaction now detects async callbacks and routes them
through manual BEGIN IMMEDIATE/COMMIT with a serialization lock instead
of throwing. Synchronous callbacks still use the native better-sqlite3
transaction wrapper. This fixes 500 errors in integration tests where
request-path code passes async callbacks to withTransaction.
