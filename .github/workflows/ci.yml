name: CI

on:
<<<<<<< HEAD
  pull_request:
  push:
    branches:
      - main
      - stabilization/doctor-gate

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
=======
  push:
    branches: [main]
  pull_request:
    branches: [main]
>>>>>>> origin/main

jobs:
  test:
    runs-on: ubuntu-latest
<<<<<<< HEAD
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci --no-audit --no-fund

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Unit
        run: npm run unit

      - name: Build
        run: npm run build

=======
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test
>>>>>>> origin/main
