# Grounded resource ranking

Correct coverage percentages exposed a second numeric cutoff: display and the integrity sweep removed local resource directories below 7 even when canonical scoring recorded a full-credit declared need and positive geography. The shared pointer authority now recognizes grounded REVIEW evidence at an unchanged nonnegative score. All four pointer gates, canonical rejection, eligibility, and research-lead classification remain in force. The funder-recall writer uses the same evidence predicate.

The integrity sweep deletes unsupported low-score rows only when their selected score, decision, and explanation still match, preserving concurrent rescoring.

Validation: canonical scoring through serialized evidence and display; exact-need, missing policy, geography, need mismatch, partial credit, rejection, negative eligibility, and expiry regressions; SQLite sweep and concurrent-refresh regression. Focused resource/surfacing family: 194 passed, 4 existing skips. Production golden-profile restoration still requires deployment and fresh evidence; this change alone is not a healthy production receipt.
