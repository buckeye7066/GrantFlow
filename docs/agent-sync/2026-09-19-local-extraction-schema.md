# Local extraction response contract, September 19

The free-only exact-50 command on commit 66491bb failed dependency preflight:
SearxNG returned three live results, but the local extractor returned zero
grounded candidates. It created no profiles and proved clean temporary cleanup.
That failed receipt is preserved; it is not a completed fifty-profile run.

A real loopback comparison exposed type placeholders and normalized values
being emitted where source quotations were required. Schema-capable routes can
now receive a bounded page-fact schema and compact instructions at the normal
web-extractor -> provider-router -> free-provider boundary. Legacy providers
and text requests keep their old prompt/request shape. Dates are ISO-shaped,
states use canonical two-letter codes, and links are inventory IDs or null.
The final evidence validator still rejects unsupported facts and the matcher
remains the only eligibility authority. No acceptance bar was changed.

Regressions reproduced missing schema forwarding and incorrect prompt
selection before their respective fixes. Live compact-prompt diagnostics
finished in 24.3 and 22.1 seconds with correct eligibility, Tennessee geography
and December 31 deadline; the second missed the lower award bound, so this is
not an assertion of complete model accuracy. A proposed evidence-sentence enum
was rejected after producing an incorrect nationwide claim and is NOT in code.

No pull request was merged and no production subscription worker was enrolled.
An attempted change to the existing owner worker was tool-blocked and never
executed. Independent-provider credential probes were also blocked. Those
operations were not retried through an alternate path. Production performance
and the requested account activations remain separate uncompleted acceptance
items. Exact tests, final-source live proof and pushed commit are recorded in
the continuation ledger outside the repository.
