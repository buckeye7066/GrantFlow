# DEPRECATED — do not use or copy this compatibility file.
#
# GrantFlow loads `.env` from the repository root. Start from the generated root
# template instead:
#
#   cp ../.env.example ../.env
#
# See ../docs/ENVIRONMENT.md before configuring local or deployed environments.
# The managed backend-only reference is ./.env.example; do not hand-edit it.
