# FlexFactor audit — GrantFlow

- **Project:** `C:\Users\firer\GrantFlow`
- **Branch:** `flexfactor/audit-grantflow`
- **Files reviewed:** 3
- **Defects found:** 54
- **Files fixed:** 0
- **Baseline build:** passed
- **Unit tests added:** 0 (suite not run)
- **Button/UI (Playwright):** skipped
- **Cycles run:** 1
- **Providers:** anthropic:claude-opus-4-8, openai:gpt-4o-mini
- **Git:** nothing-to-commit

## Remaining defects NOT auto-fixed (fix floor = high)

_These were found but left as-is - review and decide. Critical/high here means a file that could not be safely auto-fixed (see manual-review list)._

### high (14)
- `src\components\organizations\OrganizationForm.jsx` line 563 (security) - **Case-sensitive MIME type check bypassed**: The code checks `!allowedTypes.includes(file.type.toLowerCase())` but `allowedTypes` uses lowercase literal strings. However, if file.type contains uppercase characters, the check passes `toLowerCase()` and may not match expected types; more critically, `file.type` is user-controlled and could be spoofed by attackers uploading malicious files with fake MIME types. _Suggested fix:_ Validate by actual file content (magic bytes/file signature) rather than relying solely on user-supplied MIME types. At minimum, enforce Content-Type validation server-side.
- `src\components\organizations\OrganizationForm.jsx` line 807 (security) - **AI prompt injection vulnerability**: User input from `aiInputText` is directly interpolated into the LLM prompt (line 621) without sanitization, allowing attackers to inject prompt instructions that override the intended extraction behavior, potentially exfiltrating or manipulating returned data. _Suggested fix:_ Escape or neutralize user input before interpolating into the prompt, or use a templating system that separates instructions from user data; alternatively, pass user input as a separate schema parameter to the LLM client rather than string interpolation.
- `src\components\organizations\OrganizationForm.jsx` line 1121 (security) - **AI keyword suggestion lacks prompt injection defense**: Similar to handleAIHarvest, the contextInfo constructed at lines 1097–1106 interpolates formData fields directly into the LLM prompt without sanitization, allowing malicious form values to inject instructions. _Suggested fix:_ Sanitize formData fields before interpolation, or use a structured request format that separates user data from instructions.
- `src\components\organizations\OrganizationForm.jsx` line 1161 (security) - **AI focus areas suggestion vulnerable to prompt injection**: Line 1161 in handleSuggestFocusAreas (truncated but follows same pattern) will interpolate form data into the prompt without sanitization, identical to keyword suggestion vulnerability. _Suggested fix:_ Apply same sanitization as keyword handler; extract repeated pattern into a helper function to avoid duplication.
- `src\components\organizations\OrganizationForm.jsx` line 103 (security) - **Inadequate URL validation**: The URL validation only checks for 'http' and 'https' schemes; however, it lacks control for ensured safety (e.g., redirects to potentially dangerous sites). This could lead to Open Redirect vulnerabilities. _Suggested fix:_ Implement a more robust URL validation using a safe list of domains or libraries that allow for securing potential redirects.
- `src\components\organizations\OrganizationForm.jsx` line 949 (security) - **Potential insecure handling of AI responses**: The AI-generated responses are not subject to rigorous validation before being merged into `formData`, which could allow injection attacks if malicious data is returned. _Suggested fix:_ Implement strict validation for AI-generated fields to ensure unwanted data formats cannot be injected into the state.
- `src\pages\Automation.jsx` line 1043 (bug) - **Array method called on potentially non-array value**: Line 1043 calls `.map()` on `recentFailures`, but `recentFailures` is not validated to be an array at that point. The guard at line 1042 checks `hasFailures`, which depends on the array check at line 842, but TypeScript/runtime won't guarantee `recentFailures` is an array when `.map()` is called if the state changes between checks or if there's a race condition in props passing. _Suggested fix:_ Add explicit array validation: `{hasFailures && Array.isArray(recentFailures) ? recentFailures.slice(0, 4).map(renderFailureRow) : null}`
- `src\pages\Automation.jsx` line 1031 (bug) - **Array method called on potentially non-array value**: Line 1031 calls `.slice(0, 6).map()` on `runningJobs`, but despite the `hasRunning` check at line 1029, there is no inline type guard to ensure `runningJobs` is actually an array at call time. Props could change or TypeScript inference may be insufficient. _Suggested fix:_ Add explicit array validation: `{hasRunning && Array.isArray(runningJobs) ? runningJobs.slice(0, 6).map(renderJobRow) : null}`
- `src\pages\Automation.jsx` line 230 (edge-case) - **deriveStateFromProfile can return null without indication**: The function deriveStateFromProfile returns null if no valid state is found, which could lead to silent failures or unhandled cases where the consuming code does not properly handle a null return. Calling functions may assume a valid state is always returned. _Suggested fix:_ Implement a logging mechanism or throw an error/custom message if no valid state is derived, ensuring proper handling.
- `src\components\anya\AnyaChat.jsx` line 625 (error-handling) - **Unhandled promise rejection in error catch block**: In the onboarding handler (line 619-630), the catch block calls `toast()` to show an error message, but `toast()` return value is not awaited and the function does not re-throw or properly handle the error state. More critically, if the `toast()` itself fails, that error is silently swallowed. The user gets no clear signal that their profile save failed beyond a toast that may not persist. _Suggested fix:_ Ensure the error is properly logged before the toast, and either await the toast or guarantee it completes. Consider setting an error state variable that persists in the UI if the toast is dismissed. Test that profile save failures are consistently surfaced to users and don't leave the UI in an ambiguous state.
- `src\components\anya\AnyaChat.jsx` line 1313 (edge-case) - **Fragile error detection for missing profile**: Line 1313 detects a missing profile by checking `status === 404 || /profile not found/i.test(message)`. This regex check is case-insensitive but fragile: if the error message format changes on the backend, or if the API returns a different error code, the fallback to a profile-less session won't trigger and the error will be re-thrown, disabling admin tools (violating Anya goals 4 & 8). _Suggested fix:_ Replace the regex with a structured error check. Ensure the backend consistently returns a well-defined error code or message for missing profiles. Consider checking `error?.code === 'PROFILE_NOT_FOUND'` instead of parsing strings.
- `src\components\anya\AnyaChat.jsx` line 880 (concurrency) - **Race condition: loadTools effect has no dependency on feature flags or context changes**: Lines 881-903: the `loadTools` effect only depends on `[isAdmin]`, so it only re-runs when admin status changes. However, if `listAnyaTools({ includeAdmin: isAdmin })` behavior changes or the admin flag is updated, the tools list may become stale. More critically, if the tools API is slow, multiple mounts could trigger multiple concurrent calls and the last one to complete overwrites earlier results without de-duplication. _Suggested fix:_ Add a per-call ID or use a request cancellation token. Consider using `AbortController` to cancel in-flight requests when the component unmounts or when a new request starts: `const controller = new AbortController(); ... loadTools(controller.signal)`.
- `src\components\anya\AnyaChat.jsx` line 108 (concurrency) - **Potential race condition with state updates**: The state variables are updated based on asynchronous operations (like API calls) without ensuring that the component is still mounted (lines 831, 849). This can lead to scenarios where state updates are attempted on an unmounted component, potentially causing memory leaks or console errors. _Suggested fix:_ Wrap state changes in a check to ensure the component is still mounted, similar to what is already done in other asynchronous methods.
- `src\components\anya\AnyaChat.jsx` line 365 (performance) - **Inefficient state updates leading to re-renders**: Every time a message is sent, a new message object is created and set to the state, potentially leading to unnecessary re-renders. The handling in the catch block is particularly problematic where the optimistic state is reset again after an error. _Suggested fix:_ Reassess the state updating pattern to minimize excessive state updates and utilize proper keys for controlled components.

### medium (32)
- `src\components\organizations\OrganizationForm.jsx` line 78 (correctness) - **Incomplete regex for numeric string validation**: The regex `/^[-+]?(\ d+\.?\d*|\.\d+)$/` permits strings like '.' (single dot) which will parse to NaN, violating the contract that toNumberOrNull always returns a valid number or null. _Suggested fix:_ Tighten the regex to require at least one digit: `/^[-+]?(\d+(\.\d+)?|\.\d+)$/` so '.' alone fails validation.
- `src\components\organizations\OrganizationForm.jsx` line 582 (error-handling) - **Unsafe optional chaining in error message**: Line 592 constructs an error message with `(error && error.message) ? error.message : ...` but error could be any type (string, object, null, etc.), and accessing `.message` on a non-Error object may be undefined, resulting in a confusing message. _Suggested fix:_ Use `error?.message || 'Unknown error'` and validate error is an Error instance: `error instanceof Error ? error.message : String(error)`.
- `src\components\organizations\OrganizationForm.jsx` line 1031 (logic) - **Incorrect logic for detecting empty strings in numeric coercion**: The condition `!(typeof original === 'string' && original.trim() === '')` excludes empty strings from being flagged as dropped, but empty strings should silently coerce to null without warning, not trigger a dropped-field warning. _Suggested fix:_ Change logic to: only push to droppedNumericFields if `parsed === null && original !== '' && original !== null && original !== undefined`.
- `src\components\organizations\OrganizationForm.jsx` line 449 (edge-case) - **Unhandled lazy initialization race on mount**: useState with lazy initializer calls `initializeFormData(organization, contactMethods)` at mount time, but `contactMethods` may still be loading (isLoadingContacts true) or empty, causing form to be initialized with incomplete contact data before the query completes. _Suggested fix:_ Initialize form with organization only; wait for the useEffect at line 460 to populate contacts after the query settles, or set `initialData: []` on the query to guarantee an empty array rather than undefined during load.
- `src\components\organizations\OrganizationForm.jsx` line 488 (correctness) - **Dependency array missing isContactsFetched**: The useEffect at line 460 includes `isContactsFetched` in its logic (line 475) but does not include it in the dependency array (line 488), causing stale closures when isContactsFetched changes without a re-run of the effect. _Suggested fix:_ Add `isContactsFetched` to the dependency array: `[organization, contactMethods, isLoadingContacts, isContactsFetched]` (already present, but verify on re-audit).
- `src\components\organizations\OrganizationForm.jsx` line 625 (correctness) - **AI prompt contains outdated applicant_type examples**: The prompt at line 625 instructs AI to return 'high_school_student', 'college_student', 'graduate_student', but normalizeApplicantType at line 56 maps these to 'student', and the canonical PROFILE_TYPE_IDS (line 48) does not include these legacy values, causing a disconnect between prompt and validation. _Suggested fix:_ Update the prompt to use canonical applicant_type values only: 'organization', 'student', 'individual_need', etc., not legacy aliases.
- `src\components\organizations\OrganizationForm.jsx` line 430 (error-handling) - **Contact method fetch called without error boundary**: The useQuery at line 426 will silently fail to fetch if organization.id is undefined or the API call throws; the useEffect at line 438 handles isContactsError but does not prevent the form from being initialized with empty contactMethods, leaving the user unaware of a loading/error state until they interact. _Suggested fix:_ Display a loading spinner or skeleton while isLoadingContacts is true; ensure error toast is surfaced immediately and persistently.
- `src\components\organizations\OrganizationForm.jsx` line 996 (correctness) - **LLM response validation only checks top-level type**: The check at line 996 validates that response is an object, but does not validate the presence or types of extracted fields; a response with all null/undefined fields will pass validation and produce an empty mergeData, silently ignoring failed extraction. _Suggested fix:_ Add a check to ensure response contains at least one non-null key, or log a warning when no fields were extracted.
- `src\components\organizations\OrganizationForm.jsx` line 78 (correctness) - **Regex allows leading zeros in integers**: The regex at line 78 permits strings like '007' which parse to 7, but in some contexts (e.g., ZIP codes, tax IDs) leading zeros are significant and should be preserved or rejected. _Suggested fix:_ Clarify the intent: if leading zeros must be rejected for integers, update regex to `/^[-+]?(0|[1-9]\d*(\.\d+)?|\.\d+)$/`; if they should be preserved, store as strings instead.
- `src\components\organizations\OrganizationForm.jsx` line 373 (correctness) - **Incorrect reference to `assistance_category`**: The variable 'assistance_category' is used instead of 'assistance_categories'. This leads to the old field not being processed correctly. _Suggested fix:_ Change 'initialData.assistance_category' to 'initialData.assistance_categories' in line 373.
- `src\components\organizations\OrganizationForm.jsx` line 95 (edge-case) - **Unsanitized potential URL input**: The website normalization logic does not ensure that transformed URLs are always safe or valid before returning them. This could potentially allow malicious URLs through. _Suggested fix:_ Add additional validation or sanitization steps to ensure transformed URLs do not contain harmful payloads.
- `src\pages\Automation.jsx` line 352 (bug) - **Calling .toLocaleString() on non-numeric value**: At line 348, `sections` is assigned either a number (`.length`) or `null`. At line 352, `.toLocaleString('en-US')` is called on `sections`, but `formatNumber()` was not called first, so if sections is null, this will throw a TypeError. The logic branches at 349-350 should prevent null, but the code path is fragile. _Suggested fix:_ Ensure `sections` is validated: `const display = typeof sections === 'number' ? sections.toLocaleString('en-US') : ''` before using it.
- `src\pages\Automation.jsx` line 815 (correctness) - **Missing null check before function call**: At line 817, `onInspect(lastJob.id)` is called without verifying `onInspect` is a function, even though the condition checks `typeof onInspect === 'function'` at line 699. However, the click handler is defined regardless and could be invoked if the state changes after render. The guard should be inside the handler. _Suggested fix:_ Move the function type check inside the onClick handler: `onClick={(event) => { event.stopPropagation(); if (lastJob && typeof onInspect === 'function') onInspect(lastJob.id); }}` 
- `src\pages\Automation.jsx` line 916 (correctness) - **Missing null check on function before invocation**: At line 916, `onInspect(job.id)` is called unconditionally in the onClick handler, but there is no runtime guard that `onInspect` is a function. If the component receives `onInspect={undefined}`, this will throw a TypeError. _Suggested fix:_ Add a function guard: `onClick={(event) => { event.stopPropagation(); if (typeof onInspect === 'function') onInspect(job.id); }}` 
- `src\pages\Automation.jsx` line 969 (correctness) - **Missing null check on function before invocation**: At line 969, `onInspect(job.id)` is called unconditionally without verifying `onInspect` is a function. If `onInspect` is undefined or not a function, this throws a TypeError. _Suggested fix:_ Add a guard: `if (typeof onInspect === 'function') onInspect(job.id);`
- `src\pages\Automation.jsx` line 1101 (correctness) - **Missing null check on function before invocation**: At line 1101, `onCancel(job.id)` is called without a runtime guard that `onCancel` is a function, despite the check at line 1096. If props change or the check evaluates differently at call time, this throws a TypeError. _Suggested fix:_ Add inline guard: `onClick={() => { if (typeof onCancel === 'function') onCancel(job.id); }}`
- `src\pages\Automation.jsx` line 1113 (correctness) - **Missing null check on function before invocation**: At line 1113, `onRetry(job.id)` is called without a runtime guard, despite the conditional at line 1108. If the function is undefined at invocation time, this throws. _Suggested fix:_ Add guard: `onClick={() => { if (typeof onRetry === 'function') onRetry(job.id); }}`
- `src\pages\Automation.jsx` line 228 (edge-case) - **Potential false negatives in deriveZipFromProfile**: The function deriveZipFromProfile could fail to return a valid ZIP code if all candidates are invalid, leading to incorrect profiles being processed or saved. This issue manifests if a profile does not have a valid zip code that conforms to the expected formats. _Suggested fix:_ Add a logging mechanism that captures instances where no valid ZIP code is found, and consider throwing error or returning a specific marker value.
- `src\pages\Automation.jsx` line 295 (dead-code) - **Unreachable code in summarizeJob**: There are multiple instances in summarizeJob where the 'summary' variable is set to null. The later part of the function that formats the duration may not execute as intended if the summary is null, causing potential confusion. _Suggested fix:_ Ensure valid checks before using the summary variable, or remove checks that set it to null if not necessary.
- `src\pages\Automation.jsx` line 234 (edge-case) - **Potential failure in deriveStateFromProfile**: If none of the candidates return a valid state, deriveStateFromProfile will return null, potentially leading to unhandled situations in UI or business logic. _Suggested fix:_ Implement error handling to return a clear message or throw an error instead of returning null.
- `src\components\anya\AnyaChat.jsx` line 470 (dead-code) - **Dead code: isFirstRun is hardcoded to false**: The variable `isFirstRun` is set to `false` on line 470 and never changes. The comments indicate first-run onboarding was retired, but the entire onboarding flow (lines 514-520, 536-631, 636-657, and the OnboardingFlow component lines 192-384) remains as dead code that will never execute since all its entry points check `isFirstRun` first. _Suggested fix:_ Remove the `isFirstRun` state variable, the entire OnboardingFlow component, all onboarding-related state (obProfileType, obSituations, obState), and all effects/handlers that reference `isFirstRun` (lines 515-521, 537-631, 636-657). Remove the onboarding UI rendering from the main component render. This reduces bundle size and eliminates maintenance burden of unused code paths.
- `src\components\anya\AnyaChat.jsx` line 1039 (bug) - **Potential XSS vulnerability via JSON.stringify in user-facing message**: Line 1039 constructs a message with `JSON.stringify(ctx)` and sends it as a message. If the serialized context is later rendered without proper escaping in message bubbles, it could be vulnerable to XSS. The serialized JSON is user-facing data that gets displayed in the chat. _Suggested fix:_ Either sanitize the JSON before display, or ensure the message bubble component (MessageBubble) always escapes content. Ideally, send a structured payload to the backend instead of JSON.stringify'd text, so the context is kept out of user-facing message threads.
- `src\components\anya\AnyaChat.jsx` line 1149 (edge-case) - **No validation that postAnyaMessage response has expected shape**: Line 1149 calls `postAnyaMessage()` and expects `response?.messages` to be an array. If the response shape changes or is malformed, the code falls back to `refreshMessages(sessionId)` (line 1159). However, if the response is `null` or `undefined`, the fallback may not catch all error states. The final `response?.degraded` check (line 1164) doesn't validate the response exists before checking a property. _Suggested fix:_ Add explicit validation: check `response && typeof response === 'object'` before accessing properties. Log the actual response shape if it's unexpected so debugging is easier.
- `src\components\anya\AnyaChat.jsx` line 1213 (edge-case) - **Unsafe optional chaining for nested response object**: Line 1212: `response?.result?.output?.matches?.length ?? 0` assumes a deeply nested structure. If any intermediate level exists but is not an object (e.g., `result` is a string), accessing `.output` will throw a TypeError despite the `?.` chaining working correctly for nullish values. _Suggested fix:_ Use defensive checks: `const matches = response?.result?.output?.matches; const matchCount = Array.isArray(matches) ? matches.length : 0;`
- `src\components\anya\AnyaChat.jsx` line 1274 (edge-case) - **Incomplete task update response handling**: Line 1275-1280: if `response?.task` is undefined, the code constructs a fallback task object by spreading the original task and overriding status. However, this optimistically assumes the backend accepted the status change. If the backend validation fails silently and returns an empty response, the UI will show a success toast and update the local state, but the server state may differ. _Suggested fix:_ Require the backend to return the updated task object. If it doesn't, throw an error: `if (!response?.task) throw new Error('No task in response')`. This ensures the UI and server stay in sync.
- `src\components\anya\AnyaChat.jsx` line 1094 (edge-case) - **No validation of createAnyaSession response after starting new conversation**: Line 1094 calls `createAnyaSession()` but doesn't validate the returned session object. If the backend fails to create a session (returns null/undefined/malformed), `setSessionId(newSession?.id ?? null)` will silently set sessionId to null, leaving the UI in a broken state where users can't interact with Anya. _Suggested fix:_ Validate the response: `if (!newSession?.id) throw new Error('Failed to create session')`  so the catch block (lines 1097-1102) properly reports it to the user instead of silently failing.
- `src\components\anya\AnyaChat.jsx` line 946 (bug) - **Ref stored in state without synchronization**: Lines 946-949: `prefillSendDepsRef.current` is manually updated in a useEffect, but if `currentPage` or other deps change between renders and the prefill effect reads the ref (line 961), there's a race where the ref might not yet be updated if the prefill effect fires before the sync effect runs. React effects run in order, but this is fragile. _Suggested fix:_ Pass `currentPage` and `pageContextPayload` directly into the prefill effect's dependency array instead of using a ref. Remove the manual ref sync. This ensures closure captures current values safely.
- `src\components\anya\AnyaChat.jsx` line 956 (edge-case) - **Prefill message check doesn't guard against whitespace-only messages**: Line 957-958 trims the message and checks if it's falsy, but a message that is only whitespace will pass the `prefillMessage` truthiness check on line 956 and then be trimmed to empty on line 957. This wastes a render and API call. _Suggested fix:_ Trim earlier: check `if (!prefillMessage?.trim())` on line 956 to avoid processing whitespace-only messages.
- `src\components\anya\AnyaChat.jsx` line 853 (edge-case) - **Silent failure if both effectiveProfileId and isAdmin are falsy**: Lines 853-867: if neither `effectiveProfileId` nor `isAdmin` is true, the code sets a synthetic guidance message (lines 858-866) but does NOT call bootstrap(). This means `sessionId` remains null, so the user cannot actually send messages to Anya. The synthetic message tells them to create a profile, but the chat is completely non-functional. _Suggested fix:_ Either bootstrap a profile-less session even when no profile is selected (so users can still chat with Anya), or explicitly disable the input and show a clearer message like 'Sign in or create a profile to use Anya.' Currently, the input appears enabled but fails silently on send.
- `src\components\anya\AnyaChat.jsx` line 153 (security) - **Potential XSS risk with inline rendering**: Rendering user-provided content directly (line 1410) without escaping it may lead to XSS attacks if malicious content is sent to render. `message.content` should be sanitized before rendering to avoid any vulnerabilities. _Suggested fix:_ Use a library like DOMPurify to sanitize user input before rendering it.
- `src\components\anya\AnyaChat.jsx` line 187 (error-handling) - **Silent failure in API response handling**: The code does not adequately handle cases where API responses are required for the successful creation of profiles (lines 556-576). The failure to check for valid profiles can lead to misleading assumptions about the user's state. _Suggested fix:_ Implement more robust error handling particularly around API calls with better messaging and checks to prevent proceeding with invalid data.
- `src\components\anya\AnyaChat.jsx` line 562 (error-handling) - **Potential unhandled errors**: The catch block in asynchronous function does not specifically handle different types of errors that can occur during API interactions. This might lead to uncaught errors being misrepresented. _Suggested fix:_ Introduce error categorization and handling strategies for various expected errors, providing clearer feedback to the user regarding what went wrong.

### low (8)
- `src\components\organizations\OrganizationForm.jsx` line 1039 (style) - **Early return without mutation check in array field coercion**: Line 1039 has `if (Array.isArray(coercedResponse[field])) return;` which exits early if the field is already an array, but the intent is to process non-arrays; this is correct but is a needlessly defensive check that could mask bugs if an array field somehow arrives as non-array. _Suggested fix:_ Consider adding a comment to clarify intent, or restructure for clarity: `if (!Array.isArray(coercedResponse[field])) { /* coerce */ }`.
- `src\components\organizations\OrganizationForm.jsx` line 546 (correctness) - **handleSelectChange does not validate name parameter**: The handleSelectChange function accepts a `name` parameter without validating it against known fields, allowing arbitrary object keys to be set on formData if a malicious/buggy caller passes an unexpected name. _Suggested fix:_ Consider whitelisting expected field names or adding a comment documenting that callers are responsible for validation.
- `src\components\organizations\OrganizationForm.jsx` line 112 (edge-case) - **initializeFormData does not handle nested contact method conflicts**: If contactMethods contains malformed entries or both 'email' and 'phone' are extremely large arrays, there is no size limit or validation, potentially creating a huge formData object. _Suggested fix:_ Add validation to limit contactMethods array size or validate structure: `const emails = (contactMethods || []).filter(c => c?.type === 'email').slice(0, MAX_EMAILS).map(c => c?.value);`
- `src\components\organizations\OrganizationForm.jsx` line 369 (correctness) - **Redundant delete after migration may hide bugs**: Line 369 deletes `student_grade_level` after pushing to the array, but if the old field had a value and the new array was not empty, the migration silently skips the old value. This is by design (line 366 checks `length === 0`) but could mask data loss if a caller expects both to coexist. _Suggested fix:_ Add a comment explaining the migration logic, or consider a more explicit merge strategy.
- `src\components\organizations\OrganizationForm.jsx` line 603 (edge-case) - **File input reset on network error may not succeed**: At line 600, if e is null or e.target is not a valid input element, `e.target.value = ''` silently fails, leaving the file input populated and preventing re-upload of the same file. _Suggested fix:_ Guard more defensively: `if (e?.target?.value !== undefined) { e.target.value = ''; }`
- `src\components\organizations\OrganizationForm.jsx` line 1070 (style) - **Toast message construction is fragile**: The ternary at line 1070 will produce a verbose toast with a long error message if many numeric fields fail, potentially overflowing UI or being unreadable. _Suggested fix:_ Truncate the field list or show a summary: `droppedNumericFields.length > 0 ? `Could not parse ${droppedNumericFields.length} numeric fields` : ...`
- `src\components\anya\AnyaChat.jsx` line 709 (performance) - **Potential inefficiency in tool categorization logic**: The `adminTools` useMemo (lines 671-722) iterates through `adminToolsList` multiple times (once per filter() call on lines 678, 683, 688, 693, 698, 703, 708, 713, and 718). For a large tool list, this is O(n*categories) instead of O(n). The categorized Set is populated and checked repeatedly. _Suggested fix:_ Refactor to a single pass: iterate through `adminToolsList` once, determine each tool's category via a switch/if-else chain, and push to the appropriate group in an object. This reduces time complexity from O(n*k) to O(n).
- `src\components\anya\AnyaChat.jsx` line 1176 (error-handling) - **Restored input value may differ from trimmed sent value**: Line 1176 restores the full input (before trim) when send fails: `setInput(trimmed)`. But `trimmed` is already trimmed, so this line sets input to the trimmed version, not the original. The variable name is misleading. _Suggested fix:_ Store the original input before trimming: `const original = input; const trimmed = original.trim(); ... setInput(original)` so the user sees exactly what they typed if the send fails.

## Defects by file

### `src\components\organizations\OrganizationForm.jsx` ⚠️ reported
- **[high]** line 563 (security) — **Case-sensitive MIME type check bypassed**: The code checks `!allowedTypes.includes(file.type.toLowerCase())` but `allowedTypes` uses lowercase literal strings. However, if file.type contains uppercase characters, the check passes `toLowerCase()` and may not match expected types; more critically, `file.type` is user-controlled and could be spoofed by attackers uploading malicious files with fake MIME types. _Fix:_ Validate by actual file content (magic bytes/file signature) rather than relying solely on user-supplied MIME types. At minimum, enforce Content-Type validation server-side.
- **[high]** line 807 (security) — **AI prompt injection vulnerability**: User input from `aiInputText` is directly interpolated into the LLM prompt (line 621) without sanitization, allowing attackers to inject prompt instructions that override the intended extraction behavior, potentially exfiltrating or manipulating returned data. _Fix:_ Escape or neutralize user input before interpolating into the prompt, or use a templating system that separates instructions from user data; alternatively, pass user input as a separate schema parameter to the LLM client rather than string interpolation.
- **[high]** line 1121 (security) — **AI keyword suggestion lacks prompt injection defense**: Similar to handleAIHarvest, the contextInfo constructed at lines 1097–1106 interpolates formData fields directly into the LLM prompt without sanitization, allowing malicious form values to inject instructions. _Fix:_ Sanitize formData fields before interpolation, or use a structured request format that separates user data from instructions.
- **[high]** line 1161 (security) — **AI focus areas suggestion vulnerable to prompt injection**: Line 1161 in handleSuggestFocusAreas (truncated but follows same pattern) will interpolate form data into the prompt without sanitization, identical to keyword suggestion vulnerability. _Fix:_ Apply same sanitization as keyword handler; extract repeated pattern into a helper function to avoid duplication.
- **[high]** line 103 (security) — **Inadequate URL validation**: The URL validation only checks for 'http' and 'https' schemes; however, it lacks control for ensured safety (e.g., redirects to potentially dangerous sites). This could lead to Open Redirect vulnerabilities. _Fix:_ Implement a more robust URL validation using a safe list of domains or libraries that allow for securing potential redirects.
- **[high]** line 949 (security) — **Potential insecure handling of AI responses**: The AI-generated responses are not subject to rigorous validation before being merged into `formData`, which could allow injection attacks if malicious data is returned. _Fix:_ Implement strict validation for AI-generated fields to ensure unwanted data formats cannot be injected into the state.
- **[medium]** line 78 (correctness) — **Incomplete regex for numeric string validation**: The regex `/^[-+]?(\ d+\.?\d*|\.\d+)$/` permits strings like '.' (single dot) which will parse to NaN, violating the contract that toNumberOrNull always returns a valid number or null. _Fix:_ Tighten the regex to require at least one digit: `/^[-+]?(\d+(\.\d+)?|\.\d+)$/` so '.' alone fails validation.
- **[medium]** line 582 (error-handling) — **Unsafe optional chaining in error message**: Line 592 constructs an error message with `(error && error.message) ? error.message : ...` but error could be any type (string, object, null, etc.), and accessing `.message` on a non-Error object may be undefined, resulting in a confusing message. _Fix:_ Use `error?.message || 'Unknown error'` and validate error is an Error instance: `error instanceof Error ? error.message : String(error)`.
- **[medium]** line 1031 (logic) — **Incorrect logic for detecting empty strings in numeric coercion**: The condition `!(typeof original === 'string' && original.trim() === '')` excludes empty strings from being flagged as dropped, but empty strings should silently coerce to null without warning, not trigger a dropped-field warning. _Fix:_ Change logic to: only push to droppedNumericFields if `parsed === null && original !== '' && original !== null && original !== undefined`.
- **[medium]** line 449 (edge-case) — **Unhandled lazy initialization race on mount**: useState with lazy initializer calls `initializeFormData(organization, contactMethods)` at mount time, but `contactMethods` may still be loading (isLoadingContacts true) or empty, causing form to be initialized with incomplete contact data before the query completes. _Fix:_ Initialize form with organization only; wait for the useEffect at line 460 to populate contacts after the query settles, or set `initialData: []` on the query to guarantee an empty array rather than undefined during load.
- **[medium]** line 488 (correctness) — **Dependency array missing isContactsFetched**: The useEffect at line 460 includes `isContactsFetched` in its logic (line 475) but does not include it in the dependency array (line 488), causing stale closures when isContactsFetched changes without a re-run of the effect. _Fix:_ Add `isContactsFetched` to the dependency array: `[organization, contactMethods, isLoadingContacts, isContactsFetched]` (already present, but verify on re-audit).
- **[medium]** line 625 (correctness) — **AI prompt contains outdated applicant_type examples**: The prompt at line 625 instructs AI to return 'high_school_student', 'college_student', 'graduate_student', but normalizeApplicantType at line 56 maps these to 'student', and the canonical PROFILE_TYPE_IDS (line 48) does not include these legacy values, causing a disconnect between prompt and validation. _Fix:_ Update the prompt to use canonical applicant_type values only: 'organization', 'student', 'individual_need', etc., not legacy aliases.
- **[medium]** line 430 (error-handling) — **Contact method fetch called without error boundary**: The useQuery at line 426 will silently fail to fetch if organization.id is undefined or the API call throws; the useEffect at line 438 handles isContactsError but does not prevent the form from being initialized with empty contactMethods, leaving the user unaware of a loading/error state until they interact. _Fix:_ Display a loading spinner or skeleton while isLoadingContacts is true; ensure error toast is surfaced immediately and persistently.
- **[medium]** line 996 (correctness) — **LLM response validation only checks top-level type**: The check at line 996 validates that response is an object, but does not validate the presence or types of extracted fields; a response with all null/undefined fields will pass validation and produce an empty mergeData, silently ignoring failed extraction. _Fix:_ Add a check to ensure response contains at least one non-null key, or log a warning when no fields were extracted.
- **[medium]** line 78 (correctness) — **Regex allows leading zeros in integers**: The regex at line 78 permits strings like '007' which parse to 7, but in some contexts (e.g., ZIP codes, tax IDs) leading zeros are significant and should be preserved or rejected. _Fix:_ Clarify the intent: if leading zeros must be rejected for integers, update regex to `/^[-+]?(0|[1-9]\d*(\.\d+)?|\.\d+)$/`; if they should be preserved, store as strings instead.
- **[medium]** line 373 (correctness) — **Incorrect reference to `assistance_category`**: The variable 'assistance_category' is used instead of 'assistance_categories'. This leads to the old field not being processed correctly. _Fix:_ Change 'initialData.assistance_category' to 'initialData.assistance_categories' in line 373.
- **[medium]** line 95 (edge-case) — **Unsanitized potential URL input**: The website normalization logic does not ensure that transformed URLs are always safe or valid before returning them. This could potentially allow malicious URLs through. _Fix:_ Add additional validation or sanitization steps to ensure transformed URLs do not contain harmful payloads.
- **[low]** line 1039 (style) — **Early return without mutation check in array field coercion**: Line 1039 has `if (Array.isArray(coercedResponse[field])) return;` which exits early if the field is already an array, but the intent is to process non-arrays; this is correct but is a needlessly defensive check that could mask bugs if an array field somehow arrives as non-array. _Fix:_ Consider adding a comment to clarify intent, or restructure for clarity: `if (!Array.isArray(coercedResponse[field])) { /* coerce */ }`.
- **[low]** line 546 (correctness) — **handleSelectChange does not validate name parameter**: The handleSelectChange function accepts a `name` parameter without validating it against known fields, allowing arbitrary object keys to be set on formData if a malicious/buggy caller passes an unexpected name. _Fix:_ Consider whitelisting expected field names or adding a comment documenting that callers are responsible for validation.
- **[low]** line 112 (edge-case) — **initializeFormData does not handle nested contact method conflicts**: If contactMethods contains malformed entries or both 'email' and 'phone' are extremely large arrays, there is no size limit or validation, potentially creating a huge formData object. _Fix:_ Add validation to limit contactMethods array size or validate structure: `const emails = (contactMethods || []).filter(c => c?.type === 'email').slice(0, MAX_EMAILS).map(c => c?.value);`
- **[low]** line 369 (correctness) — **Redundant delete after migration may hide bugs**: Line 369 deletes `student_grade_level` after pushing to the array, but if the old field had a value and the new array was not empty, the migration silently skips the old value. This is by design (line 366 checks `length === 0`) but could mask data loss if a caller expects both to coexist. _Fix:_ Add a comment explaining the migration logic, or consider a more explicit merge strategy.
- **[low]** line 603 (edge-case) — **File input reset on network error may not succeed**: At line 600, if e is null or e.target is not a valid input element, `e.target.value = ''` silently fails, leaving the file input populated and preventing re-upload of the same file. _Fix:_ Guard more defensively: `if (e?.target?.value !== undefined) { e.target.value = ''; }`
- **[low]** line 1070 (style) — **Toast message construction is fragile**: The ternary at line 1070 will produce a verbose toast with a long error message if many numeric fields fail, potentially overflowing UI or being unreadable. _Fix:_ Truncate the field list or show a summary: `droppedNumericFields.length > 0 ? `Could not parse ${droppedNumericFields.length} numeric fields` : ...`

### `src\pages\Automation.jsx` ⚠️ reported
- **[high]** line 1043 (bug) — **Array method called on potentially non-array value**: Line 1043 calls `.map()` on `recentFailures`, but `recentFailures` is not validated to be an array at that point. The guard at line 1042 checks `hasFailures`, which depends on the array check at line 842, but TypeScript/runtime won't guarantee `recentFailures` is an array when `.map()` is called if the state changes between checks or if there's a race condition in props passing. _Fix:_ Add explicit array validation: `{hasFailures && Array.isArray(recentFailures) ? recentFailures.slice(0, 4).map(renderFailureRow) : null}`
- **[high]** line 1031 (bug) — **Array method called on potentially non-array value**: Line 1031 calls `.slice(0, 6).map()` on `runningJobs`, but despite the `hasRunning` check at line 1029, there is no inline type guard to ensure `runningJobs` is actually an array at call time. Props could change or TypeScript inference may be insufficient. _Fix:_ Add explicit array validation: `{hasRunning && Array.isArray(runningJobs) ? runningJobs.slice(0, 6).map(renderJobRow) : null}`
- **[high]** line 230 (edge-case) — **deriveStateFromProfile can return null without indication**: The function deriveStateFromProfile returns null if no valid state is found, which could lead to silent failures or unhandled cases where the consuming code does not properly handle a null return. Calling functions may assume a valid state is always returned. _Fix:_ Implement a logging mechanism or throw an error/custom message if no valid state is derived, ensuring proper handling.
- **[medium]** line 352 (bug) — **Calling .toLocaleString() on non-numeric value**: At line 348, `sections` is assigned either a number (`.length`) or `null`. At line 352, `.toLocaleString('en-US')` is called on `sections`, but `formatNumber()` was not called first, so if sections is null, this will throw a TypeError. The logic branches at 349-350 should prevent null, but the code path is fragile. _Fix:_ Ensure `sections` is validated: `const display = typeof sections === 'number' ? sections.toLocaleString('en-US') : ''` before using it.
- **[medium]** line 815 (correctness) — **Missing null check before function call**: At line 817, `onInspect(lastJob.id)` is called without verifying `onInspect` is a function, even though the condition checks `typeof onInspect === 'function'` at line 699. However, the click handler is defined regardless and could be invoked if the state changes after render. The guard should be inside the handler. _Fix:_ Move the function type check inside the onClick handler: `onClick={(event) => { event.stopPropagation(); if (lastJob && typeof onInspect === 'function') onInspect(lastJob.id); }}` 
- **[medium]** line 916 (correctness) — **Missing null check on function before invocation**: At line 916, `onInspect(job.id)` is called unconditionally in the onClick handler, but there is no runtime guard that `onInspect` is a function. If the component receives `onInspect={undefined}`, this will throw a TypeError. _Fix:_ Add a function guard: `onClick={(event) => { event.stopPropagation(); if (typeof onInspect === 'function') onInspect(job.id); }}` 
- **[medium]** line 969 (correctness) — **Missing null check on function before invocation**: At line 969, `onInspect(job.id)` is called unconditionally without verifying `onInspect` is a function. If `onInspect` is undefined or not a function, this throws a TypeError. _Fix:_ Add a guard: `if (typeof onInspect === 'function') onInspect(job.id);`
- **[medium]** line 1101 (correctness) — **Missing null check on function before invocation**: At line 1101, `onCancel(job.id)` is called without a runtime guard that `onCancel` is a function, despite the check at line 1096. If props change or the check evaluates differently at call time, this throws a TypeError. _Fix:_ Add inline guard: `onClick={() => { if (typeof onCancel === 'function') onCancel(job.id); }}`
- **[medium]** line 1113 (correctness) — **Missing null check on function before invocation**: At line 1113, `onRetry(job.id)` is called without a runtime guard, despite the conditional at line 1108. If the function is undefined at invocation time, this throws. _Fix:_ Add guard: `onClick={() => { if (typeof onRetry === 'function') onRetry(job.id); }}`
- **[medium]** line 228 (edge-case) — **Potential false negatives in deriveZipFromProfile**: The function deriveZipFromProfile could fail to return a valid ZIP code if all candidates are invalid, leading to incorrect profiles being processed or saved. This issue manifests if a profile does not have a valid zip code that conforms to the expected formats. _Fix:_ Add a logging mechanism that captures instances where no valid ZIP code is found, and consider throwing error or returning a specific marker value.
- **[medium]** line 295 (dead-code) — **Unreachable code in summarizeJob**: There are multiple instances in summarizeJob where the 'summary' variable is set to null. The later part of the function that formats the duration may not execute as intended if the summary is null, causing potential confusion. _Fix:_ Ensure valid checks before using the summary variable, or remove checks that set it to null if not necessary.
- **[medium]** line 234 (edge-case) — **Potential failure in deriveStateFromProfile**: If none of the candidates return a valid state, deriveStateFromProfile will return null, potentially leading to unhandled situations in UI or business logic. _Fix:_ Implement error handling to return a clear message or throw an error instead of returning null.

### `src\components\anya\AnyaChat.jsx` ⚠️ reported
- **[high]** line 625 (error-handling) — **Unhandled promise rejection in error catch block**: In the onboarding handler (line 619-630), the catch block calls `toast()` to show an error message, but `toast()` return value is not awaited and the function does not re-throw or properly handle the error state. More critically, if the `toast()` itself fails, that error is silently swallowed. The user gets no clear signal that their profile save failed beyond a toast that may not persist. _Fix:_ Ensure the error is properly logged before the toast, and either await the toast or guarantee it completes. Consider setting an error state variable that persists in the UI if the toast is dismissed. Test that profile save failures are consistently surfaced to users and don't leave the UI in an ambiguous state.
- **[high]** line 1313 (edge-case) — **Fragile error detection for missing profile**: Line 1313 detects a missing profile by checking `status === 404 || /profile not found/i.test(message)`. This regex check is case-insensitive but fragile: if the error message format changes on the backend, or if the API returns a different error code, the fallback to a profile-less session won't trigger and the error will be re-thrown, disabling admin tools (violating Anya goals 4 & 8). _Fix:_ Replace the regex with a structured error check. Ensure the backend consistently returns a well-defined error code or message for missing profiles. Consider checking `error?.code === 'PROFILE_NOT_FOUND'` instead of parsing strings.
- **[high]** line 880 (concurrency) — **Race condition: loadTools effect has no dependency on feature flags or context changes**: Lines 881-903: the `loadTools` effect only depends on `[isAdmin]`, so it only re-runs when admin status changes. However, if `listAnyaTools({ includeAdmin: isAdmin })` behavior changes or the admin flag is updated, the tools list may become stale. More critically, if the tools API is slow, multiple mounts could trigger multiple concurrent calls and the last one to complete overwrites earlier results without de-duplication. _Fix:_ Add a per-call ID or use a request cancellation token. Consider using `AbortController` to cancel in-flight requests when the component unmounts or when a new request starts: `const controller = new AbortController(); ... loadTools(controller.signal)`.
- **[high]** line 108 (concurrency) — **Potential race condition with state updates**: The state variables are updated based on asynchronous operations (like API calls) without ensuring that the component is still mounted (lines 831, 849). This can lead to scenarios where state updates are attempted on an unmounted component, potentially causing memory leaks or console errors. _Fix:_ Wrap state changes in a check to ensure the component is still mounted, similar to what is already done in other asynchronous methods.
- **[high]** line 365 (performance) — **Inefficient state updates leading to re-renders**: Every time a message is sent, a new message object is created and set to the state, potentially leading to unnecessary re-renders. The handling in the catch block is particularly problematic where the optimistic state is reset again after an error. _Fix:_ Reassess the state updating pattern to minimize excessive state updates and utilize proper keys for controlled components.
- **[medium]** line 470 (dead-code) — **Dead code: isFirstRun is hardcoded to false**: The variable `isFirstRun` is set to `false` on line 470 and never changes. The comments indicate first-run onboarding was retired, but the entire onboarding flow (lines 514-520, 536-631, 636-657, and the OnboardingFlow component lines 192-384) remains as dead code that will never execute since all its entry points check `isFirstRun` first. _Fix:_ Remove the `isFirstRun` state variable, the entire OnboardingFlow component, all onboarding-related state (obProfileType, obSituations, obState), and all effects/handlers that reference `isFirstRun` (lines 515-521, 537-631, 636-657). Remove the onboarding UI rendering from the main component render. This reduces bundle size and eliminates maintenance burden of unused code paths.
- **[medium]** line 1039 (bug) — **Potential XSS vulnerability via JSON.stringify in user-facing message**: Line 1039 constructs a message with `JSON.stringify(ctx)` and sends it as a message. If the serialized context is later rendered without proper escaping in message bubbles, it could be vulnerable to XSS. The serialized JSON is user-facing data that gets displayed in the chat. _Fix:_ Either sanitize the JSON before display, or ensure the message bubble component (MessageBubble) always escapes content. Ideally, send a structured payload to the backend instead of JSON.stringify'd text, so the context is kept out of user-facing message threads.
- **[medium]** line 1149 (edge-case) — **No validation that postAnyaMessage response has expected shape**: Line 1149 calls `postAnyaMessage()` and expects `response?.messages` to be an array. If the response shape changes or is malformed, the code falls back to `refreshMessages(sessionId)` (line 1159). However, if the response is `null` or `undefined`, the fallback may not catch all error states. The final `response?.degraded` check (line 1164) doesn't validate the response exists before checking a property. _Fix:_ Add explicit validation: check `response && typeof response === 'object'` before accessing properties. Log the actual response shape if it's unexpected so debugging is easier.
- **[medium]** line 1213 (edge-case) — **Unsafe optional chaining for nested response object**: Line 1212: `response?.result?.output?.matches?.length ?? 0` assumes a deeply nested structure. If any intermediate level exists but is not an object (e.g., `result` is a string), accessing `.output` will throw a TypeError despite the `?.` chaining working correctly for nullish values. _Fix:_ Use defensive checks: `const matches = response?.result?.output?.matches; const matchCount = Array.isArray(matches) ? matches.length : 0;`
- **[medium]** line 1274 (edge-case) — **Incomplete task update response handling**: Line 1275-1280: if `response?.task` is undefined, the code constructs a fallback task object by spreading the original task and overriding status. However, this optimistically assumes the backend accepted the status change. If the backend validation fails silently and returns an empty response, the UI will show a success toast and update the local state, but the server state may differ. _Fix:_ Require the backend to return the updated task object. If it doesn't, throw an error: `if (!response?.task) throw new Error('No task in response')`. This ensures the UI and server stay in sync.
- **[medium]** line 1094 (edge-case) — **No validation of createAnyaSession response after starting new conversation**: Line 1094 calls `createAnyaSession()` but doesn't validate the returned session object. If the backend fails to create a session (returns null/undefined/malformed), `setSessionId(newSession?.id ?? null)` will silently set sessionId to null, leaving the UI in a broken state where users can't interact with Anya. _Fix:_ Validate the response: `if (!newSession?.id) throw new Error('Failed to create session')`  so the catch block (lines 1097-1102) properly reports it to the user instead of silently failing.
- **[medium]** line 946 (bug) — **Ref stored in state without synchronization**: Lines 946-949: `prefillSendDepsRef.current` is manually updated in a useEffect, but if `currentPage` or other deps change between renders and the prefill effect reads the ref (line 961), there's a race where the ref might not yet be updated if the prefill effect fires before the sync effect runs. React effects run in order, but this is fragile. _Fix:_ Pass `currentPage` and `pageContextPayload` directly into the prefill effect's dependency array instead of using a ref. Remove the manual ref sync. This ensures closure captures current values safely.
- **[medium]** line 956 (edge-case) — **Prefill message check doesn't guard against whitespace-only messages**: Line 957-958 trims the message and checks if it's falsy, but a message that is only whitespace will pass the `prefillMessage` truthiness check on line 956 and then be trimmed to empty on line 957. This wastes a render and API call. _Fix:_ Trim earlier: check `if (!prefillMessage?.trim())` on line 956 to avoid processing whitespace-only messages.
- **[medium]** line 853 (edge-case) — **Silent failure if both effectiveProfileId and isAdmin are falsy**: Lines 853-867: if neither `effectiveProfileId` nor `isAdmin` is true, the code sets a synthetic guidance message (lines 858-866) but does NOT call bootstrap(). This means `sessionId` remains null, so the user cannot actually send messages to Anya. The synthetic message tells them to create a profile, but the chat is completely non-functional. _Fix:_ Either bootstrap a profile-less session even when no profile is selected (so users can still chat with Anya), or explicitly disable the input and show a clearer message like 'Sign in or create a profile to use Anya.' Currently, the input appears enabled but fails silently on send.
- **[medium]** line 153 (security) — **Potential XSS risk with inline rendering**: Rendering user-provided content directly (line 1410) without escaping it may lead to XSS attacks if malicious content is sent to render. `message.content` should be sanitized before rendering to avoid any vulnerabilities. _Fix:_ Use a library like DOMPurify to sanitize user input before rendering it.
- **[medium]** line 187 (error-handling) — **Silent failure in API response handling**: The code does not adequately handle cases where API responses are required for the successful creation of profiles (lines 556-576). The failure to check for valid profiles can lead to misleading assumptions about the user's state. _Fix:_ Implement more robust error handling particularly around API calls with better messaging and checks to prevent proceeding with invalid data.
- **[medium]** line 562 (error-handling) — **Potential unhandled errors**: The catch block in asynchronous function does not specifically handle different types of errors that can occur during API interactions. This might lead to uncaught errors being misrepresented. _Fix:_ Introduce error categorization and handling strategies for various expected errors, providing clearer feedback to the user regarding what went wrong.
- **[low]** line 709 (performance) — **Potential inefficiency in tool categorization logic**: The `adminTools` useMemo (lines 671-722) iterates through `adminToolsList` multiple times (once per filter() call on lines 678, 683, 688, 693, 698, 703, 708, 713, and 718). For a large tool list, this is O(n*categories) instead of O(n). The categorized Set is populated and checked repeatedly. _Fix:_ Refactor to a single pass: iterate through `adminToolsList` once, determine each tool's category via a switch/if-else chain, and push to the appropriate group in an object. This reduces time complexity from O(n*k) to O(n).
- **[low]** line 1176 (error-handling) — **Restored input value may differ from trimmed sent value**: Line 1176 restores the full input (before trim) when send fails: `setInput(trimmed)`. But `trimmed` is already trimmed, so this line sets input to the trimmed version, not the original. The variable name is misleading. _Fix:_ Store the original input before trimming: `const original = input; const trimmed = original.trim(); ... setInput(original)` so the user sees exactly what they typed if the send fails.
