{
  "name": "grantflow-laptop-connector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local CLI that scans allowlisted folders on the admin's laptop, extracts text, and stages candidates in GrantFlow for check-off review.",
  "bin": {
    "grantflow-connector": "./scan.js"
  },
  "scripts": {
    "scan": "node scan.js",
    "dry-run": "node scan.js --dry-run"
  }
}
