# CLAUDE.md — GrantFlow

Guidance for Claude Code (and human contributors) working in this repo.

## Live-editing lock (multi-agent coordination)

This repo is sometimes worked on by more than one assistant at once (a Claude
Code session AND Cursor's agent share the same working tree). Concurrent
autonomous writes entangle diffs and produce surprise commits.

**If a file named `.agent-edit-lock` exists at the repo root**, a human and
another assistant are actively editing right now. Unless you are the session
that created the lock, you MUST NOT edit/create/delete files, commit, or push —
do read-only work only and tell the user the repo is locked. This suspends any
"auto-push fixes" / "optimize continuously" standing instructions while the lock
is present. The lock is git-ignored; remove it (`rm .agent-edit-lock`) to
release. (Cursor honors the same lock via `.cursor/rules/respect-edit-lock.mdc`.)

## Program awareness (wire every change through its full chain)

Any change to the code MUST be wired correctly through everything that depends
on it. After you edit a function, module, route, schema, or contract, trace the
dependency chain outward — callers, importers, the API/UI layer across the
frontend/backend boundary, tests, and docs — and update each consumer so no part
of the program silently uses the old behavior or falls silent. Do not stop at
the file you touched; continue down the chain until every affected consumer is
updated or explicitly confirmed unaffected, and prove there are no stale
references (grep every import/caller of what you changed). Preserve existing
public contracts (exports, response shapes) unless the change is intentional and
propagated everywhere. The canonical, always-applied version of this rule lives
at `.cursor/rules/program-awareness.mdc`; this summary exists so non-Cursor
workers honor it too.

## Commands

```bash
npm run dev          # Vite frontend dev server
npm run backend      # Express backend only
npm run dev:full     # Both frontend & backend (concurrently)

npm run build        # Production build
npm run lint         # ESLint (zero warnings enforced)
npm run typecheck    # TypeScript check
npm run unit         # Vitest unit tests
npm run test         # profile-metadata check + lint:ci + typecheck + build + unit
npm run test:all     # + smoke + e2e (Playwright)
npm run check:prepush  # Pre-push guardrail chain (auth-middleware/profile-guards/profile-metadata/runtime-imports/env-examples checks + lint + typecheck + build)

npm run migrate      # Run DB migrations
npm run db:setup     # migrate + seed
npm run doctor       # Project health check

npm run crawler-os:crawl              # Run crawler-os discovery
npm run crawler-os:test               # crawler-os node:test suite
npm run crawler:doctor                # Crawler-os health diagnostics
npm run crawler:verify                # End-to-end crawler system verify
npm run opps:ensure-national-minimum  # Ensure national opportunity floor
```

## Architecture

- **Frontend** (`src/`): React 18 + Vite + TypeScript + Tailwind + Radix UI. State via Zustand (`stores/`). Feature-based components under `components/`. API calls go through `api/`.
- **Backend** (`backend/`): Express, ~100 route files under `backend/routes/`, business logic in `backend/services/`, DB access via `backend/db/`. Entry: `backend/server.js`. Boot tasks in `backend/startup/`.
- **Shared + vnext**: `shared/` holds ~16 cross-cutting modules (`pipelineStages.js`, `dedupePipelineGrants.js`, `opportunityFundability.js`, etc.); `backend/vnext/` holds the newer scoring/state-machine subsystem (`scoringService.js`, `stateMachine.js`).
- **Crawler-os agents**: anya, hamilton, john, robert, sam, yana under `backend/crawler-os/agents/`; Amy lives in `backend/services/amy/`.
- **AI**: Claude (`@anthropic-ai/sdk`) + OpenAI for drafting, discovery, and the "Anya" assistant. Prompts in `backend/prompts/`.
- **Recall/grounding/critic lanes (flag-gated, default OFF)**: `SEMANTIC_RECALL` (embedding recall booster — ADDITIVE candidates only; `backend/services/embeddings/embeddingService.js`; matchEngine stays the sole decision authority), `COMPARABLE_AWARDS` (real NIH RePORTER awards as labeled reference-only drafting context), `PROPOSAL_CRITIC` (multi-pass draft critic). Contracts + off-state behavior: `docs/canonical_rules.md` ("Feature flags" section).
- **DB**: SQLite for local/test (`backend/db/schema.sql`), Postgres in prod via a shim. Tests use vitest with `.js` (`backend/tests/`); a few runners use `node:test` with `.mjs` — match the convention of the file you're editing.
- **Deployment**: Frontend → Vercel, Backend → Railway (PostgreSQL).
- **Canonical product rules + goals**: `docs/canonical_rules.md` is the single source of truth. Read it before changing matching, discovery, pipeline, or tenancy behavior.

## Portal automation chain (2026-08-03) — REQUIRED READING before touching Hamilton portal work

Five merged + deploy-verified PRs (#1103-#1108) changed how portals sync, how
proposals are drafted, and what auto-submit means. Every agent working in this
repo MUST know these; the old semantics appear in older comments/tests — the
rules below win.

- **NGWeb "Scholarship Manager" connector** (`portalSync/connectors/ngwebScholarshipManager.js`)
  covers every `<school>.scholarships.ngwebsolutions.com` tenant (MTSU +
  Cleveland State CC live). TRAPS: it must stay AHEAD of `mtsu` in
  `registry.js` (mtsu's `matchesCredential` greedily claims any "mtsu" label);
  both tenants collapse to one eTLD+1 key in `portalKeyHost`/
  `portalStatusKeyHost`; sessions idle out in ~45 min, so
  warm-sync-on-capture is the only reliable read window; the signed-out
  signature is a private request answered with the public
  `/CMXAdmin/Cmx_Content.aspx` landing (content NEVER proves sign-in). The
  platform's own rule: students canNOT apply to individual scholarships —
  matching runs Mon/Thu; "Qualified Opportunities 0" is a REAL answer.
- **"Synced • <date>" tile tag**: `profilePortalIndex.loadSyncSummaryByHost`
  (one grouped query; process tiles included). Only a `status='completed'`
  run earns `lastSyncedAt`; a failed run never earns it and never erases one.
- **Umbrella-application coverage** (`portalSync/generalApplicationCoverage.js`):
  a VERIFIED general-application submission advances governed grants to
  `submitted` (dated + provenance note), completes their Hamilton tasks, and
  marks the portal complete. Governance is URL-STRUCTURAL only
  (`UMBRELLA_GOVERNED_URLS` — tenant host, legacy portal host, or school path
  naming scholarships); admissions/housing/work-study on the same host are
  deliberately out. Never demotes; hard no-op without quote-anchored evidence.
- **Tracker "Mark Submitted"**: tracker cards are MERGED Hamilton
  `application_tasks` (`source:'hamilton'`) — the submit route accepts task
  ids (with `submitted_at`); Edit/Move-Forward/Delete are hidden on those cards.
- **Draft templates are opportunity-shaped + funder-tailored**
  (`applyEngine.buildDefaultSectionsForStyle`): scholarship kind →
  `personal_statement`/`academic_background`/`financial_need_statement`/
  `activities_leadership`; org grants keep the seven-section shape. Content
  tailors ONLY from the opportunity row's stored facts (never invented funder
  priorities) and pulls the appropriate profile's sections (org ministries
  first-class). Undeclared kind keeps the default — never guess from a title word.
- **Draft packets ARE the portal fill source**
  (`portalSync/../draftPacketPortalBridge.js`): wherever a packet exists, its
  sections feed the autopilot fill (placeholder text like "[review: …]" is
  REFUSED). The bridge's `ESSAY_SECTION_SOURCES` covers both template shapes —
  keep that mapping in sync if section keys evolve.
- **AUTO-SUBMIT MEANS AUTO-SUBMIT (owner rule 2026-08-03: "No more, no
  less")**: `evaluateAutoSubmitGate` withholds ONLY for `missing_info` (open
  required questions = completeness) or `automation_off` (the toggle IS the
  selection). `not_approved`/`not_generated` are RETIRED — approval is
  optional review, never a gate. Stored task authorization
  (`allow_auto_submit`) is read on EVERY run (it used to be written and never
  read — the wired-but-unreachable class). Submission is REPORTED only with
  captured portal confirmation evidence (`assessSubmissionEvidence`); a
  submit-click without evidence is a blocker, never "submitted", and
  `submit_unconfirmed` is never blindly retried. All defaults remain OFF.
- **SUBMISSION PROOF IS DURABLE + OWNER-RETRIEVABLE, never a tmp path**
  (`hamiltonConfirmationArtifacts.js`, 2026-08-03). The confirmation screenshot
  `captureConfirmation` writes used to land in `os.tmpdir()` because the
  orchestrator's `runAutopilot({…})` call passed no `screenshotsDir` — so on
  Railway's ephemeral FS every screenshot was wiped on the next deploy while
  `hamilton_autopilot_runs.confirmation_screenshot_path` kept pointing at a file
  that no longer existed: a genuine external submission's proof evaporated and
  the DB read as if proof existed. Now: (1) `resolveConfirmationCaptureDir` puts
  captures under `UPLOADS_DIR` (`<UPLOADS_DIR>/hamilton-confirmations`, the same
  persistent volume as the runtime-secrets key; `/data/uploads/…` is the prod
  floor so it can NEVER fall back to tmp in prod — mirrors `runtimeSecrets`'
  key-file resolution), and the orchestrator passes it into the engine. (2)
  `captureConfirmation` also SAVES the confirmation page (`.html`) beside the
  `.png` and returns `page_text`, so proof survives even when the portal prints
  NO reference number. (3) On a `submitted` run the orchestrator calls
  `registerConfirmationArtifact`, which reuses the SAME
  `hamiltonApplicationPacketGenerator._internal.insertDocumentRecord` the
  mail/fax packet + proposal-PDF paths use — bytes land in
  `documents.file_bytes` (BYTEA) and the proof opens at
  `/api/documents/<id>/download` (durable-bytes fallback) even after the on-disk
  copy is gone. The screenshot doc id is written to
  `application_tasks.output_document_id` (the retrievable proof LINK, not a
  filesystem path) and into the run's `result_json`. (4)
  `extractConfirmationReference` gained conservative real patterns (Ref/
  Reference, Application ID, Confirmation #, a submission id in the post-submit
  URL) that STILL pass `isPlausibleConfirmationReference` — it still rejects
  "Application designed…"; a saved page + screenshot are captured even with NO
  reference. `assessSubmissionEvidence`'s refuse-without-evidence honesty is
  UNCHANGED (a saved page is extra proof, not a new way to claim submitted). (5)
  Backfill honesty: `assessStoredConfirmationProof(db, run)` reports proof
  PRESENT only when it is actually retrievable (durable doc bytes, or an on-disk
  file that still exists) — a dangling `confirmation_screenshot_path` reads as
  NO proof; it reports, never mass-deletes. Guard test:
  `backend/tests/hamiltonConfirmationProof.test.js`.
- **"SUBMITTED" HAS TWO HONEST MEANINGS, AND A PACKET IS NEVER PROOF** (owner
  North Star 2026-08-03). Prod read-only audit found **43 `application_tasks`
  with `status='submitted'` fleet-wide, 0 with any durable external-submission
  proof** — 25 had `output_document_id` pointing at a
  `hamilton_generated_application` PACKET (the verbatim case: Robert's task
  →`66e9f618` "NAEMT EMS Scholarships — PDF", a draft packet from 2026-07-02
  masquerading as a confirmation), 18 had none; all stamped `submitted_at` by a
  bulk internal Mark-Submitted, with **no `hamilton_autopilot_runs` row carrying
  a confirmation reference or durable confirmation doc**. Two states must be
  DISTINCT and honestly labeled everywhere: (1) **externally submitted, with
  proof** — a captured portal confirmation (a `hamilton_submission_confirmation`
  document with bytes, a retrievable run confirmation, or a portal confirmation
  reference on a `status='submitted'` run); (2) **marked submitted (internal
  record)** — the flag was set but nothing external was captured. The SINGLE
  canonical predicate is `assessTaskSubmissionProof(db, task)` /
  `taskHasVerifiedExternalSubmission(db, task)` in
  `backend/services/hamilton/submissionProofPredicate.js`; it DELEGATES the
  proof-reality check to `assessStoredConfirmationProof` (#1114) so the two can
  never drift, and a packet/draft/proposal `output_document_id` NEVER qualifies
  (its doc `type` decides — `hamilton_generated_application` = packet;
  `hamilton_submission_confirmation` = proof). The choke point is the task read
  path: `applicationTaskStore.getApplicationTask` + `listApplicationTasks`
  attach a `submission_proof` object to every `submitted` task (default on;
  non-submitted rows get a cheap `not_submitted` stub — no extra query), so
  tracker/API/reporting all read the same honest label. UI: `HamiltonTaskDrawer`
  shows "Externally submitted — portal confirmation on file" (+ an
  Open-confirmation link) only when `verified_external`, else "Marked submitted
  (internal record only)"; `HamiltonAutomationQueue` carries an
  `internal record`/`confirmed` chip. This RE-LABELS on evidence — it never
  auto-flips or deletes a `submitted` status. Login wall is the actionable next
  step and is ALREADY wired (do NOT build a new login system): a `login` blocker
  becomes `waiting_for_login` (`hamiltonAuthBackupPlan`), the `login_required`
  hard-stop offers the save-a-session action (`hamiltonHardStopResolver`), the
  waiting-login count + priming toast surface via `hamiltonAuthWatchService`, and
  saved sessions flow through `hamiltonCloudLogin`. Guard tests:
  `backend/tests/submissionProofPredicate.test.js` (failing-first on the NAEMT
  packet shape, mutation-verified; + the store choke-point wiring).
- **Stage derivation trap** (`profileDerivedFacts.GRAD_RX`): "high school
  graduate" / "graduated high school" / "college graduate" must NEVER derive
  `graduate_student` — a live incident disarmed the stage gate this way and
  re-admitted UAB-Blazer-class fellowships for an incoming freshman.
- **LISTING DECOMPOSITION — a page of several awards is not a dead end**
  (owner directive 2026-08-03). The fleet was stalling on pages that list many
  awards (bold.org category, scholarships.com, fastweb, the NGWeb catalog):
  field_count 0-2, no advance button → "nothing done". Three layers now mine
  them, and every layer defers to an EXISTING authority — no shortcut verdicts:
  1. `listingPageTriage.triagePage` (pure) classifies a page the engine already
     dead-ended on as FORM / LISTING / NO_APPLICATION_SURFACE from deterministic
     signals only (field count, award-link density, the shared `isListingSurface`
     URL/title shape, and award-phrase TEXT density — the last is what catches
     the NGWeb catalog's 557 text rows behind 5 nav links). The LLM is NEVER the
     surface classifier. It runs ONLY where the engine failed to fill/advance and
     is conservative about FORM, so real multi-step forms keep their flow.
  2. `hamiltonAutopilotEngine` returns `blocker_kind:'listing_page'` (with a
     capped snapshot) at those dead-ends; `runAutopilotPathway` intercepts it
     (NOT the auth/resolver ladder) and calls `listingDecomposition.decomposeListing`.
     That enumerates awards via `llmPageExtract.extractListingAwardItems`
     (fabrication-guarded: an item's title MUST be present in the page text; an
     applyUrl is kept only if it is one of the page's OWN links, else null →
     catalog-only), admits each through the CANONICAL
     `opportunityInserter.upsertFundingOpportunity` (reality gate + dedup — the
     ONLY admission gate), lets `matchEngine.computeMatchDecision` (the SOLE
     relevance authority) decide, and for ACCEPTs with a real link re-enters the
     EXISTING `runAutopilot` fill/submit flow. Auto-submit is NEVER widened —
     the parent run's consent is forwarded verbatim. Fan-out is env-bounded:
     `HAMILTON_LISTING_MAX_ITEMS` (25), `HAMILTON_LISTING_MAX_APPLIES` (5).
  3. The classifier (`hamiltonAutomationClassifier`) routes a POINTER-kind row
     (`opportunityKindClasses.isPointerKind`: directory/referral/school_portal/
     past_award_intel) that carries a usable web URL to the portal engine for
     decomposition instead of a static `no_application` packet.
     `HAMILTON_DECOMPOSE_POINTER_LISTINGS` (default on) toggles it. This COMPOSES
     with #1110: decomposition is a DISTINCT outcome from the eligibility REJECT
     — a pointer/listing row is not engine-rejected, so #1110's gate passes it,
     and the pointer row's OWN match stays a pointer (locator rule: recommendable
     at REVIEW, never ACCEPT). Decomposition mints NEW opportunity rows.
  - **NGWeb EXCEPTION**: `<school>.scholarships.ngwebsolutions.com`
    (`isNgWebCatalogHost`) decomposes for VISIBILITY only — every award admitted
    to matching, but NEVER a per-item application attempt. Students cannot apply
    individually there; the General Application (`generalApplicationCoverage.js`)
    covers them.
- **BOT-PROTECTION DEAD-ENDS ARE NAMED, AND THE FIX IS SIDE-BY-SIDE CO-BROWSE**
  (owner 2026-08-03: "for the dead-ends, make sure the user and admin are
  aware"). scholarships.com serves a FULL-PAGE Cloudflare "managed challenge"
  interstitial ("Performing security verification… Ray ID… Cloudflare") that
  REPLACES the app before it loads, from the datacenter IP the autopilot runs on
  — and it used to collapse into a generic `login` blocker that told the owner
  nothing. `hamiltonAutopilotEngine.detectBotWall` (called FIRST in `detectGate`)
  now catches the WHOLE-PAGE interstitial — distinct from the embedded captcha
  WIDGET `detectGate` already caught — and returns `{kind:'bot_protected'}`.
  Signals are vendor-agnostic: STRONG body phrases (`performing security
  verification`, `verifying you are (a human|not a bot)`, `checking your browser
  before…`, `this website uses a security service to protect`) classify alone; a
  BRAND signal (Ray ID / Cloudflare / Akamai / DataDome / PerimeterX) classifies
  only when the page is ALSO low-content (an interstitial has no app on it), so a
  real page mentioning a vendor in its footer is never misclassified.
  `bot_protected` maps to `portal_anti_bot_block` in `hamiltonBlockerClassifier`.
  The orchestrator's run-path calls `handleBotProtectedBlock` (exported), which
  (1) sets a DURABLE, visible `blocked` task state ("This site blocks automated
  submission (bot protection). Use side-by-side co-browse to apply, or apply
  manually."), (2) notifies the profile owner AND admins
  (`hamilton_bot_protected`) with a `?cobrowse=<host>&cobrowse_reason=bot_protected`
  deep-link (`hamiltonBotProtectedNotice.js`) that surfaces the existing
  side-by-side co-browse card (`ProfilePortalsCard`; `HamiltonToastBridge` routes
  it), and (3) — critically — NEVER expires `usedSessionId`: a bot-wall is OUR
  reachability problem (IP/fingerprint), NOT proof the session is dead (mirrors
  the portal-sync block-vs-signin-wall rule; only `login`/`2fa` expire a
  session). Do NOT build a new co-browse system — this only points at the
  existing live-login flow. Guard tests:
  `backend/tests/hamiltonBotProtectedDetection.test.js` (verbatim Cloudflare text
  → bot_protected not login; real page not misclassified; footer-mention guard;
  embedded widget still captcha) + `backend/tests/hamiltonBotProtectedBlock.test.js`
  (durable blocked state, owner+admin side-by-side notification, session NEVER
  expired — mutation-verified).

### Apply-flow integrity (2026-08-03 operator walkthrough, Robert 6b3c75ec…)

Four defects found by a live discovery→pipeline→submission walkthrough; the
rules below are now enforced in code + tests and must not be re-weakened.

- **A submission portal target comes ONLY from the row's own verified links,
  screened for institution identity** (`resolveVerifiedPortalUrl` +
  `PORTAL_URL_UNVERIFIED_LABEL` in `backend/apply/applyEngine.js`). Live case:
  auto-populate shipped `https://cpcc.academicworks.com/` (Central Piedmont
  CC, **North Carolina**) as the portal for a "Cleveland State Community
  College" (**Tennessee**) opportunity — the URL sat on the grant/catalog row
  itself, minted upstream by a 2026-07-28 web_search misattribution.
  Candidates are application_url/url/source_url (+ the linked catalog row's
  links); `grants.application_method` is a METHOD vocabulary and is never
  read as a URL again. Each candidate passes `portalUrlFunderPlausibility`
  (`backend/config/urlRules.js`): on a TENANT-SLUG platform host
  (`TENANT_SLUG_PORTAL_PLATFORMS` — `<school>.academicworks.com` etc.) the
  slug must be explainable by the funder's WHOLE name (ordered-prefix
  decomposition: initialisms `mtsu`, blends `clscc`, whole names — the Yana
  lead-contact whole-name rule applied to slugs; `cpcc` fails against
  "Cleveland State Community College" because no word starts with p).
  Non-tenant hosts are `undecidable`, NEVER flagged (olemiss.edu/tn.gov
  class — only a positive mismatch acts). No surviving candidate → portal
  stays BLANK, labeled "unverified — confirm with funder", a previously
  stored wrong portal_url is CLEARED on re-populate, and `validateApplication`
  reds `portal_url_flags`. Guard test:
  `backend/tests/applyEnginePortalUrlIntegrity.test.js`.
- **"Mark submitted" is a guarded, honestly-labeled INTERNAL record**
  (`markSubmitted` checklist guard + `SubmissionAssistant.jsx` copy). It used
  to be one silent click at "Checklist: 0/6 done". Now: incomplete checklist →
  409 `CHECKLIST_INCOMPLETE` naming the items unless the caller confirms
  (`confirm_incomplete`, wired to a hard-confirm panel that lists them); the
  UI says plainly this records that YOU submitted it yourself and transmits
  nothing to the funder — distinct from Hamilton auto-submit, which is the
  feature that actually submits on portals. Hamilton's mirror
  (`metadata.submitted_by === 'hamilton'`) is EXEMPT by design (it records a
  portal submission that already happened, evidence-gated per #1105/#1107) and
  the HTTP route strips that provenance claim from clients. Checklist items
  are now completable in the UI (`setChecklistItem` finally has a consumer).
  Guard test: `backend/tests/applicationsSubmitChecklistGuard.test.js`.
- **Discovery dedupes against the pipeline by the CANONICAL identity, and
  members are SURFACED, not hidden** (`pipelineExclusion.js` canonical tier +
  `annotatePipelineMembers`; `?pipeline=annotate` on
  `/api/matching/profile/:id/opportunities`, used by DiscoverGrants). Live
  case: pipeline grant "Tennessee Promise — Free Community College" vs
  addable result "TN Promise Scholarship" — no exact title|funder key lined
  up. The exclusion index now also carries `canonicalOpportunityKey` tiers
  (`titleIdentityKey` t: keys; `normalizeUrlForId` u: keys, both exported
  from `backend/crawler-os/contract.js` — do NOT invent a second identity
  rule). **The u: tier only decides when the URL is unambiguous on BOTH
  sides** (≤1 distinct identity carries it) — the `enforceGrantCatalogLink`
  posture, because shared hubs must never collapse distinct programs
  (measured in prod 2026-08-03: tn.gov/collegepays serves HOPE + Promise +
  Lottery; grantwatch.com sits on 282 distinct programs). Annotate mode flags
  rows `already_in_pipeline` so the card shows WHY it isn't addable; the
  zero-result recovery ladder no longer bypasses the member filter (Tier B
  re-canonicalized the PRE-filter list). Guard test:
  `backend/tests/pipelineExclusionCanonical.test.js`.
- **Discovery cards are keyed by stable opportunity identity, never by list
  index** (`SearchResults.jsx` `computeOpportunityKeys`). Index-bearing keys +
  score re-sorts on poll refetch remounted every card and wiped the local
  "Added" state — the walkthrough's "4 of 5 clicks did nothing". A
  `{status:'failed'}` return now always produces feedback (destructive toast
  + a live "Retry add" button); a server-flagged `already_in_pipeline` row
  renders disabled from first paint. Guard test:
  `src/components/discovery/SearchResults.test.jsx`.

## MIGRATION PARITY — superseding a system requires proving coverage, not just cutover

The 2026-07 crawler-os cutover silently stranded 12+ discovery lanes: the
migration verified the OLD paths stopped running (superseded job types) but
never enumerated what those paths could REACH and asserted the new system
reached it too. The standing rule:

> A PR that supersedes/replaces a subsystem MUST ship a mechanical parity
> check: enumerate the old system's reachable surface (sources, job types,
> domains, fields — whatever the unit is) in a test or script, and assert the
> replacement covers every item or explicitly lists each exclusion with a
> reason. "The old code no longer runs" is not evidence of parity.

Companion rule for enumerable inventories: any set whose members live in more
than one place (sources ↔ adapters ↔ dashboard lanes; thresholds ↔ display
tiers; profile facts ↔ question fields) gets a REGISTRY plus a TOTALITY test
so a new member cannot silently fall out of any consumer (precedents:
`LANE_OF_SOURCE` totality in `coverageEvidenceService.test.js`,
`check-env-examples`, `profileKnownFacts` field-map guard).

Owner-verified outcomes on real profiles are protected by the
**golden-outcome sentinel** (`coverage.goldenOutcomes` in
`backend/services/sam/samRegistry.js`, expectations in `system_kv
golden_outcome_expectations`): after live-verifying a coverage fix, append the
expectation so a future regression reds Anya's morning report instead of
waiting for the owner to notice. The companion **golden-AMOUNT sentinel**
(`coverage.goldenAmounts`, `system_kv golden_amount_expectations` =
`[{label, url_contains, expect_max, [over_factor=3], [under_factor=5]}]`) does
the same for per-award FIGURES: a live grant whose url matches but whose value
falls outside `[expect_max/under, expect_max*over]` is a regression (the
Coca-Cola $237,500-for-a-$20,000-award class — the extractor grabbing a program
total). A row with no amount yet is backlog, not a failure; Amy synthetics are
excluded. Verify the real figure live, then append the expectation.

## INVARIANTS — enforce at a choke point, never trust per-call discipline

GrantFlow's recurring bugs came from canonical RULES being enforced only by
convention ("remember to check X in every code path"). The standing rule:

> A machine-checkable product rule must be re-asserted in ONE place against the
> live DB, so it holds regardless of which code path created the data. The
> per-call gate is the first line of defense; the boot sweep is the net. Do NOT
> scatter new ad-hoc checks across call sites.

**The single enforcer is `backend/startup/enforceInvariants.js`**, run on every
boot from `backend/server.js` immediately after `ensureSchemaInvariants()`. The
full `runSelfHeal()` orchestrator in `backend/startup/selfHeal.js` also calls it
as step 9 for Sam/Anya on-demand and maintenance runs, but boot wires the
invariant sweep directly so it cannot be skipped by self-heal schedule changes.
It mirrors `backend/startup/ensureSchemaInvariants.js` (which owns schema-shape
DDL; data-repair invariants go in `enforceInvariants.js`).

When you add or change behavior that touches an invariant below, change the
enforcer + its test — do not rely on a new per-call check alone.

| Invariant | Single enforcer | Guard test |
| --- | --- | --- |
| Sticky deletes (deleted pipeline grants AND deleted funding-source matches stay gone) | `reconcileDismissedGrants()` + `reconcileDismissedMatches()` in `backend/services/pipelineDismissals.js`, both re-run by `enforceStickyDeletes()`. Owner-facing delete: `DELETE /api/profiles/:id/funding-sources/:opportunityId` (`backend/routes/fundingSources.js`) records the tombstone + purges via the same sweeps; the GET excludes tombstoned rows per-read | `backend/tests/enforceInvariants.test.js` + `backend/tests/fundingSourcesDismissRoute.test.js` |
| No cross-profile / cross-tenant bleed (grant org must match its profile's org) | `enforceNoCrossProfileBleed()` | `backend/tests/enforceInvariants.test.js` |
| Relevance / match-score floor (no junk in pipeline; `match_score < 5` on the DATA-POINT scale, excl. NULL — see `backend/config/matchThresholds.js` for the 2026-07-06-evening scale where score = matched profile data points ÷ total data points × eligibility × geo gates, 8 is the pipeline bar, and bands were empirically calibrated against the prod distribution) | `enforceRelevanceFloor()` (ON by default; `ENFORCE_RELEVANCE_FLOOR=0` for count-only) | `backend/tests/enforceInvariants.test.js` |
| Pipeline grants belong to a profile (no orphan `profile_id IS NULL` rows leaking into org-scoped reads/PDFs) | `enforceProfileScopedPipeline()` (ON by default; preserves `amount_awarded > 0`; disable via `ENFORCE_PROFILE_SCOPED_PIPELINE=0`) | `backend/tests/enforceInvariants.test.js` |
| One canonical income per **individual** profile (conflicting `household_income` across the `financial` vs `financial_information` sections must not poison need-based matching) | `enforceProfileIncomeReconciliation()` — for INDIVIDUAL/student/family/veteran profiles only, collapses a conflict to the applicant's own (need-consistent / **lower**) income and syncs both sections; orgs/businesses are never touched; an ambiguous conflict with no need signal is **logged for human review, not changed**. Read-only audit: `backend/scripts/audit-profile-income-conflicts.mjs` | `backend/tests/enforceInvariants.test.js` |
| A person-type profile (individual/family/student/veteran) never carries a contradicted org identity (a bad Base44/AI-enrichment import can hallucinate `organization_details.organization_type`/`small_business_details.business_name` onto an individual, which `resolveEffectiveProfileType()` then treats as MORE specific than `primary_type` and promotes the whole profile — and its match `applicant_types` — to an org, surfacing institutional RFPs instead of individual-benefit programs: the Kimberly Botts class) | `enforceIndividualOrgSectionConflict()` — clears `organization_type`/`business_name` ONLY when the profile's own `occupation` section already structurally denies both (`nonprofit_employee=false` AND `small_business_owner=false`); orgs are never touched; no structured denial = **logged for human review, not changed** (the profile may legitimately run both) | `backend/tests/enforceInvariants.test.js` |
| No search-engine application targets (a `google.com/search?q=…` / other search-RESULTS url is never a portal/application URL on `application_tasks`, `funding_opportunities`, or `grants`) | `enforceNoSearchEngineApplicationTargets()` (nulls the URL; reclassifies non-terminal tasks to blocked/`unknown_application_method`; bounded + idempotent; disable via `ENFORCE_URL_HYGIENE=0`). Producers gated at `opportunityInserter.upsertFundingOpportunity` + `hamiltonAutomationClassifier.readUrl` via the canonical `isSearchEngineUrl()` (`backend/config/urlRules.js`) | `backend/tests/enforceInvariants.test.js` |
| Pipeline grants carry the funder's name when knowable (`grants.funder` ← linked `funding_opportunities.sponsor`; the catalog column is `sponsor`, the pipeline column is `funder` — #725 naming-drift class) | `enforceFunderBackfill()` — re-copies the sponsor from the linked opportunity; NEVER invents a value (unlinked/empty-sponsor rows are counted as `missingFunder`, not guessed). Ingest-side first line of defense: `resolveSponsorName()` in `backend/services/opportunityInserter.js` accepts `funder`/`funder_name`/`organization`/`agency` aliases into `sponsor` | `backend/tests/enforceInvariants.test.js` + static tripwire `backend/tests/funderFieldDrift.test.js` |
| No dangling profile-opportunity matches (a surfaced match must point at a catalog row that still exists — ghosts inflate the matches view and fail promote passes with `opportunity_not_found`; catalog purge paths never cleaned matches up) | `enforceNoDanglingMatches()` — deletes match rows whose `opportunity_id` no longer resolves; disable via `ENFORCE_NO_DANGLING_MATCHES=0` | `backend/tests/enforceInvariants.test.js` |
| A pipeline grant is LINKED to its catalog row when one plainly exists (the amount-answer census blind spot, surfaced by #954, 2026-07-17). `enforceAmountEnrichment` and `enforceGrantAmountBackfill` reach a grant ONLY through `grants.funding_opportunity_id` (JOIN/subquery), so a grant with that NULL is **structurally invisible** to every amount net — it can never be enriched or inherit a catalog amount, and it shows in `pipeline.amountCoverage` as `unanswered_no_catalog_row` (82 of 313 active rows in prod). Many DO have a catalog twin: the crawler upserts a `funding_opportunities` row AND a `grants` row from the same result (`crawlerManager.js`) but wrote the grant without the link. The two share a URL, which is a tier of the canonical dedup identity (`canonicalOpportunityKey`), so a grant and an active catalog row at the SAME url are the same real-world opportunity | `enforceGrantCatalogLink()` — runs immediately BEFORE `enforceAmountEnrichment` so a grant linked this boot is enriched/backfilled the SAME sweep. Links ONLY when ALL hold: (1) URL IDENTITY — grant `url`/`application_url`, normalized (`RTRIM(LOWER(x),'/')`, dialect-agnostic), equals a normalized catalog `source_url`/`application_url`/`evidence_url`; (2) EXACTLY ONE active catalog row matches — 2+ is AMBIGUOUS (a shared directory URL) and is counted, never guessed (the `reconcileConvertedApplications` posture); (3) NO PROFILE CONFLICT — if BOTH rows name a `profile_id` and they DIFFER, skip (a URL coincidence must not cross a pipeline boundary — G4/G8; a NULL either side is profile-agnostic and compatible). Only NULL→value (an existing link is never touched); idempotent (a linked grant leaves the candidate set); bounded (`GRANT_CATALOG_LINK_LIMIT`, default 500); disable via `ENFORCE_GRANT_CATALOG_LINK=0` for count-only. **TRAP: prod Postgres `funding_opportunities` has a bare `url` column the SQLite schema LACKS — reference only `source_url`/`application_url`/`evidence_url` (live-verified to lose ZERO matches) or CI reddens on `no such column: fo.url` (the #946/#954 schema-drift class). Prod: 27 unique safe links; 13 ambiguous (multi-match, left alone); 38 have no catalog twin at all** | `backend/tests/enforceInvariants.test.js` ("enforceGrantCatalogLink" — 12 tests incl. ambiguous-never-guessed, cross-profile-refused, profile-agnostic-ok, inactive-skipped, never-overwrite, idempotent, count-only; all fail on a no-op body) |
| A grant with NO catalog twin is READ DIRECTLY (the last mile of the amount-answer census, 2026-07-17). `enforceAmountEnrichment` reads CATALOG rows and reaches a grant only through `funding_opportunity_id`; `enforceGrantCatalogLink` links the grants that HAVE a twin — but ~38 active-pipeline grants in prod have NO twin at all (Eldercare Locator, Coca-Cola Scholars, FAFSA — curated resources inserted directly as grants). Nothing read them, so they sat in `pipeline.amountCoverage` as `unanswered_no_catalog_row` forever and the finding could never go green while a real, answerable row had simply never been looked at | `enforceGrantDirectAmountEnrichment()` — reads such a grant's OWN url via the SAME service the catalog sweep uses (`enrichOpportunityAmountFromSource`, adapter-then-fetch, SSRF-safe) and records the answer ON THE GRANT: a scholarship page → a real amount; a locator/benefit page → an evidenced `none_published`; a JS shell / dead page stays `page_read:false` → NOT burned as a denial → an honest `unanswered_unreadable` that names adapter work. Burn/retry semantics IDENTICAL to the catalog sweep (grant carries its own `amount_enrich_attempted_at`/`amount_enrich_attempts`, migration 142/0146): burn on `page_read`/stable-failure/MAX_ATTEMPTS, an outage never burns, mark written only AFTER the amount write succeeds (#946). Excludes Amy synthetics; runs AFTER `enforceGrantCatalogLink` so only genuine orphans reach it. Bounded (`GRANT_DIRECT_AMOUNT_BOOT_LIMIT` 10, `GRANT_DIRECT_AMOUNT_TIME_BUDGET_MS` 20s); `ENFORCE_GRANT_DIRECT_AMOUNT=0` for count-only. **The census now reads an answer off EITHER the grant OR its catalog row, and `pipeline.amountCoverage` FAILS ONLY on `unanswered_unreadable` (read → JS shell → needs adapter); `unanswered_never_read` is BACKLOG, not a failure. sam.gov stays deliberately unadapted** | `backend/tests/enforceInvariants.test.js` ("enforceGrantDirectAmountEnrichment" — 8 tests incl. writes-onto-grant, none_published-on-grant, unread-never-fabricates, skips-linked, skips-synthetic, no-burn-on-write-fail, idempotent, count-only; all fail on a no-op body) + `backend/tests/pipelineAmountCoverageRatchet.test.js` (orphan backlog vs answered vs unreadable) |
| Pipeline grants carry a DOLLAR value when one is knowable (every "Pipeline $" surface reads the `backend/config/pipelineValue.js` choke point: `amount_requested` → `amount_max` → `amount_min`; never re-inline a status list or `SUM(amount_requested)` — the "$6,500 pipeline with 118 real sources" class). "$0" and "no amount stated" are DIFFERENT facts: dollar cards must also render `unvaluedCountSql()`'s count ("+N sources without listed amounts") so benefit programs/directories don't read as "qualifies for nothing" | `enforceGrantAmountBackfill()` — inherits `amount_min/max` from the linked catalog row, defaults `amount_requested` from the ceiling/floor; NEVER invents a value (`missingAmount` counted, not guessed). Write-side first line of defense: `saveToProfilePipeline` defaults `amount_requested`; ingest-side: `resolveOpportunityAmounts()` (`backend/services/awardAmountExtractor.js`) conservatively extracts per-award dollars from text when a source provides no structured amounts. Runs BEFORE `enforceIndividualAmountCeiling` so the ceiling purges on honest values | `backend/tests/enforceInvariants.test.js` + `backend/tests/pipelineValue.test.js` + `backend/tests/awardAmountExtractor.test.js` |
| Every pipeline grant carries a match score when computable (an unscored NULL row must not masquerade as an engine-endorsed match — the Eileen-Fisher-on-a-church class) | `enforceGrantScoreBackfill()` — canonical re-score of NULL-score rows against their own profile, bounded per boot (`SCORE_BACKFILL_BATCH`, default 300); disable via `ENFORCE_GRANT_SCORE_BACKFILL=0`. UI first line of defense: "Not scored" badge on NULL-score cards (`src/components/pipeline/GrantCard.jsx`) | `backend/tests/enforceInvariants.test.js` |
| Every LIVE-engine-qualified stored match converges into the real profile pipeline, and every non-admission carries a durable reason (G2 anti-zero-result) | `runQualifiedPipelinePromotion()` — off the boot path (post-listen + nightly), round-robin/time-bounded, scans every non-terminal stored candidate without a stored score/decision prefilter and calls the sole public `saveToProfilePipeline` entry point for a fresh canonical rescore + tombstone/source/relevance/duplicate gates. Outcomes carry profile/policy/opportunity fingerprints and reopen on drift; Amy synthetic markers exclude by OR; dry-run rows are isolated; `ENFORCE_QUALIFIED_PROMOTION` is the count-only rollout switch | `backend/tests/pipelinePromotion.test.js` |
| Every 'converted' service application points at a LIVE profile (Convert-to-Profile used to only flip the status flag — the applicant stayed invisible to admin and locked out of login, since prod `/email/start` only admits emails matching an existing profile) | `convertApplicationToProfile()` / `reconcileConvertedApplications()` in `backend/services/serviceApplicationConversion.js` — PATCH route is the per-call gate; boot net `enforceConvertedApplicationsHaveProfiles()` links by email→name or CREATES (intake rows only; 'signup' rows are link-only to avoid duplicating a client); ambiguous matches flagged, never guessed | `backend/tests/serviceApplicationConversion.test.js` |
| Catalog near-duplicate identity (the same real-world program must collapse to ONE `funding_opportunities` row even when re-extracted with paraphrased punctuation/word order — the 7× NAEMT class) | `canonicalOpportunityKey()` in `backend/crawler-os/contract.js` (external_id → token-sorted title+sponsor → URL), consulted by BOTH `crawler-os/storage.upsertOpportunity` and `services/opportunityInserter.upsertFundingOpportunity`. One-time re-key/merge: `backend/scripts/rekey-dedup-catalog.mjs` | `backend/crawler-os/tests/` + `backend/tests/opportunityInserter*.test.js` |
| Pipeline lifecycle statuses tell the truth about who set them (a protected `submitted` means a human/Hamilton actually submitted; bulk imports stamped rows `submitted` from the SOURCE's listing status — grants.gov "(posted)" — with `submitted_date` NULL, permanently shielding never-scored, often-ineligible rows from every purge/re-score net: the HUD Section 4 $42M class) | `enforceImportedStatusHonesty()` — surgical demote to `discovered` ONLY when all three hold: status `submitted` + `submitted_date IS NULL` + import/repair provenance (adapter "(posted)" notes or `admin_schema_repair` explanation); a real submission or human-noted row is never touched; disable via `ENFORCE_STATUS_PROVENANCE=0` | `backend/tests/enforceInvariants.test.js` ("enforceImportedStatusHonesty") |
| A relevant pipeline source's award amount is ACQUIRED when the funder's own page states it (only ~18% of the catalog carried any dollar figure — ingest text is often one aggregator sentence, and nothing ever read the funder's page: the "$0 pipeline full of real sources" class). Companion write-side guards: untrusted-source structured amounts outside the extractor's $100–$10M plausibility window are demoted to TEXT (`resolveOpportunityAmounts` — a $42M program appropriation is not a per-award ceiling; the boot net is `enforceGrantAmountBackfill` step 0, which strips fabricated numerics already persisted AND cleans grant values inherited from them, never touching user-entered asks or awarded money), and wide floor→ceiling ranges (> `WIDE_AWARD_RANGE_RATIO`×) default `amount_requested` from the FLOOR, not the ceiling | `enforceAmountEnrichment()` — bounded per boot (`AMOUNT_ENRICH_BOOT_LIMIT` 10, `AMOUNT_ENRICH_TIME_BUDGET_MS` 20s; the nightly sweep passes a real budget): fetches the source page for active-pipeline catalog rows with no amount via the SSRF-safe crawler-os fetcher, runs the conservative `awardAmountExtractor` (numbers only from explicit per-award phrasings; program totals stay text-only; a TUITION-COVERAGE award — "pays tuition", "covers 100% of tuition and fees" — is an explicit per-award semantic with no fixed figure → status `varies` + phrase as text, never a number: the NM Lottery/Opportunity `amount_recall_miss:high_school_student` class, 2026-08-03); disable via `ENFORCE_AMOUNT_ENRICHMENT=0`. Page-reader service: `backend/services/amountEnrichment.js`. **Attempt-state is the `funding_opportunities.amount_enrich_attempted_at` COLUMN and the exclusion is a SQL predicate — never a post-LIMIT filter.** The original sweep SELECTed `LIMIT 200` then dropped attempted ids in JS, so once those 200 rows were tried it reported "0 candidates" forever and never reached row 201 (coverage pinned at 12% even after the nightly budget went to 120 — the invariant read green while doing nothing); its `system_kv` ring also capped at 2000 ids. **A one-shot mark may only be set once the row's ANSWER is known, and "the service never throws" makes a catch block the wrong place to decide that.** `amount_enrich_attempted_at` is permanent, but the sweep set it unconditionally and put the retry rule in `catch` — while `amountEnrichment.js` is documented "Best-effort: never throws" and RETURNS `{attempted:true, reason:'fetch_failed:503'}`. So the guard was unreachable, `fetchFailed` was always 0, and any outage permanently burned the rows it touched; this table asserted the opposite for a week. Measured in prod 2026-07-15: 30 rows marked, 0 amounts, 0 retried. The decision now keys off the service's returned `page_read` (the extractor scanned real copy → we learned the answer, burn) and `transient` (5xx/429/408/statusless → retry; 4xx and a JS-shell `thin_page` are stable facts → burn), bounded by `amount_enrich_attempts` < `AMOUNT_ENRICH_MAX_ATTEMPTS` (3) so a permanently-down host is not re-fetched nightly forever. Candidates are ordered **fewest-attempts-first** so retries can never starve never-tried rows out of the budget. **`remaining` alone cannot tell success from exhaustion** — it hits 0 both when every row got an amount and when every row was tried and yielded nothing — so the sweep also reports `exhausted` (done-with, still no dollar figure); `remaining→0` with a large `exhausted` means the pages simply do not state amounts and the fix is an ADAPTER (grants.gov/sam.gov both have APIs and both render as JS shells the fetcher reads as `thin_page`), not more fetching | `backend/tests/enforceInvariants.test.js` ("enforceAmountEnrichment" — incl. the >200-row regression that fails on the old post-LIMIT filter, and the transient-burn/starvation regressions that fail on the old unconditional mark; the starvation test PINS ids adversarially because with random UUIDs it passed on the old `ORDER BY fo.id` by luck) + `backend/tests/amountEnrichment.test.js` + `backend/tests/awardAmountExtractor.test.js` |
| A funding source FOUND to meet a profile's needs is ADDED (owner rule 2026-07-16). The Google-bar benchmark found real funding pages GrantFlow lacked, wrote them to `system_kv web_parity_gap_queue` — and **nothing ever read that key**. A write-only queue: the same real sources (tndisability.org, thegrantportal.com for a TN disability profile) were re-found and re-filed nightly while the owner's report asked him to adjudicate them by hand ("candidate queue — nothing auto-added", fleet parity 41.2 ▼-14.4). The finding was right every night; the loop was open | `loadGapSeedPagesForProfile()` (`services/webParityBenchmark.js`) → `opts.seedPages` on `runWebDiscoveryLane` (`crawler-os/webLane.js`), wired in `runProfileDiscoveryLive`; bounded by `GAP_SEED_LIMIT_PER_RUN` (8/run). **A seed is a URL, NOT a verdict** — it enters where a search hit enters and is still fetched/LLM-extracted/reality-gated/deduped/scored by the canonical engine, so automating this lowers NO bar; it only removes a search's failure to find. Seeds are exempt from `maxPages` (that budget bounds chasing an unbounded SERP — already settled for a known URL) but the seed list itself is bounded. **The GATES' verdict is recorded, not the attempt**: the lane returns `seeded_adopted_urls` (rows actually created; `deduped` counts as adopted — the source IS reachable) and `markGapCandidateOutcomes()` marks each offered candidate `adopted`/`gated_out`, both terminal so the write-only queue cannot rebuild. Do NOT report "seeded N" as "added N" | `backend/tests/webLane.test.js` ("seed pages" — incl. a seed that must still be REJECTED by the reality gate, a dead seed reported not-adopted, and seeds not crowded out by maxPages) + `backend/tests/webParityBenchmark.test.js` ("loadGapSeedPagesForProfile" / "markGapCandidateOutcomes") |
| **"No amount" and "no amount PUBLISHED" are different facts, and only a READ can tell them apart.** The sweep fetched the funder's own page, the extractor scanned real copy, and the answer — *this funder states no per-award figure* — was **thrown away**: the write branch persisted a status only when it was NOT `not_listed`, which is precisely what a read-with-no-figure returns. So the row stayed blank and **indistinguishable from one nothing had ever looked at**, and every consumer had to treat a funder that publishes nothing as a CRAWLER MISS. The cost was two permanent false alarms in the owner's inbox: `pipeline.amountCoverage` failed nightly on a `pct < 60` bar that measured **THE WORLD** (what share of funders publish a figure at all — honest ceiling ~21%, and prod 2026-07-17 was `remaining=2 / exhausted=132`, i.e. the backlog was DRAINED) rather than us; and Amy's `amount_recall_miss` fired for **28 of 50** synthetic profiles matched to benefit programs/food banks/SSI, making the owner's standing "50/50 clean → notify me once" flywheel goal **unreachable by construction**. A finding that can never go green is not a standard — it is noise, and an owner trained to scroll past it is exactly how the REAL defect next door (#950, a re-crawl WIPING amounts) goes unread | `AMOUNT_STATUS_NONE_PUBLISHED` (`services/awardAmountExtractor.js`) is the DENIAL, written **only** by `enforceAmountEnrichment` after `page_read === true && found === false` — never by an extractor default, never by an ingest. `not_listed` stays SILENCE (the default) and stays in every denominator: **silence is not a denial** (the same rule that stops a re-crawl clearing an amount it knows nothing about). A `thin_page`/4xx is `page_read: false` → **never** records a denial: we did not learn the funder publishes nothing, we learned we cannot read it — those rows need an ADAPTER and must stay visible, so fabricating a denial there would silently retire exactly the rows that need work. `pipeline.amountCoverage` now asserts **every active row has an ANSWER** (a value, an evidenced `none_published`, an honest label, or DIRECTORY-by-design — a locator is a pointer, never an award) and fails on rows it cannot explain, grouped by why (`unanswered_unreadable` names the host to adapt; `unanswered_no_catalog_row` = the sweep JOINs grants→funding_opportunities so an unlinked grant is structurally out of reach — 82 of 312 active rows in prod). **The metric counts REAL pipeline only — Amy synthetic-training grants (`profiles.created_by='agent:amy'`, `NON_SYNTHETIC_PIPELINE`) are excluded from BOTH the headline and the census.** Amy dumps up to 50 synthetic profiles a night (the reaper deletes them next run); including them made the metric measure her rotation, not crawler quality — a 188-grant cohort drove coverage 18%→11% and tripped the ratchet on a night NOTHING was wiped (the same "measure the world, not us" failure, one level down). A grant with NO profile is KEPT (excluded only on positive synthetic evidence); the synthetic side has its own telemetry (Amy's cohort scoreboard + `amount_recall_miss`). **THE TRAP, and why the denial must be a POSITIVE recorded fact rather than "attempted but still blank":** `amount_enrich_attempted_at` SURVIVES a wipe (#950), so an exclusion keyed on it would have let every wiped row quietly LEAVE the denominator — coverage would read ~100% and the metric would go blind to the very defect it exists to catch. A wiped row keeps its honest status (`known`), is never `none_published`, stays a miss, and the ratchet fires. The ratchet (the wipe detector) runs in its own path and a broken census can never downgrade it to green — it degrades to "census unavailable", never to "fine" | `backend/tests/pipelineAmountCoverageRatchet.test.js` (incl. **"A WIPED row still counts as a MISS"**, the DIRECTORY/no-catalog-row buckets, and the low-and-steady-but-answered GREEN case — all 5 FAIL on the old check) + `backend/tests/enforceInvariants.test.js` ("RECORDS the denial", and the fabrication guard **"does NOT record a denial when the page could not be READ"**, mutation-verified) + `backend/tests/amyAgent.test.js` ("amount_recall_miss counts only amounts that were KNOWABLE" — incl. `not_listed` must STILL fire) |
| An opportunity's award amount is read from its source's OWN API when the page cannot state it (the amount-enrichment sweep's ONLY strategy was fetching the row's page and extracting from the copy — structurally impossible for grants.gov/sam.gov, which render client-side, so the fetcher gets a JS shell, the extractor never sees award copy, and the sweep honestly returns `thin_page` every night forever. grants.gov was 45 of 149 backlog rows, 2026-07-15; pipeline-$ coverage pinned at 13% and NO amount of budget could move it. The figures were never missing — `awardCeiling`/`awardFloor` sit behind a keyless JSON API this repo already spoke to via `services/shared/grantsGovApiClient.js`) | `services/sources/amountAdapters.js` — the adapter REGISTRY (`{id, matches(row), enrich(row,deps)}`, contract = `enrichOpportunityAmountFromSource`'s return shape), consulted BEFORE any page fetch in `amountEnrichment.js`. An adapter that does not know the row returns `attempted:false` and the page fetcher still runs, so the adapter lane can never cost a row its chance. **`parseApiAmount` is the fabrication choke point**: grants.gov sends the literal STRING `"none"` (and `"0"`) for an absent figure — 7 of 16 live rows on 2026-07-16 — so `if (awardCeiling)` reads absent-as-present and would invent an amount through a source `isOfficialAmountSource` deliberately EXEMPTS from the $100–$10M plausibility guard (i.e. nothing downstream would catch it). Award data lives on `synopsis` for posted rows and `forecast` for forecasted ones (a forecasted row has NO synopsis node — reading only synopsis silently reports "no amount" for all of them). An API answering "no figure published" is `page_read:true` (a REAL answer → burn); 5xx/429/timeout is `transient` (→ retry). **Honest expected yield: ~56%** of grants.gov rows carry a figure — the rest genuinely publish none. sam.gov is deliberately NOT adapted: its `award` node reports what a specific vendor WAS granted, which is not "what an applicant could receive" | `backend/tests/grantsGovAmountAdapter.test.js` (incl. every `"none"`/`"0"` form, synopsis-vs-forecast, exact-oppNum-match floor, registry totality) + `backend/tests/amountEnrichment.test.js` ("the API adapter lane" — the 3 adapter tests FAIL on pre-adapter code) |
| A permanent one-shot mark is a claim about the ROW, so a NEW strategy re-opens it (migrations 138/0142). #944 made the sweep burn a row once its answer was known and counted a JS-shell `thin_page` as known ("it will be thin every night") — correct while fetching was the only strategy, WRONG the moment an adapter could read those rows. Every grants.gov row had been burned as `thin_page`, so shipping the adapter alone would have changed NOTHING: the sweep's `amount_enrich_attempted_at IS NULL` predicate would never hand it a single row it was built for — a fix reading green while doing nothing, the third instance in this subsystem | `backend/db/migrations/138_reset_adapter_readable_amount_burns.sql` + twin `backend/db/postgres/migrations/0142_*.sql` — resets `amount_enrich_attempted_at`/`amount_enrich_attempts` for adapter-readable rows that still carry no dollar figure; narrow (amount-carrying rows and other sources untouched) and idempotent. The sweep's candidate query must also SELECT `fo.source`/`fo.source_id`/`fo.record_origin` — the adapter registry routes on them, and selecting only URLs silently degrades every adapter to URL-pattern matching | verified against a real sqlite DB (burned grants.gov rows un-burned; amount-carrying + other-source rows untouched) |
| A permanent mark is written only for an outcome that was actually PERSISTED — and a value's TYPE is part of its contract (migrations 139/0143). The grants.gov adapter shipped `amount_confidence: 'high'`; the column is **REAL**, so Postgres threw `invalid input syntax for type real: "high"` — and **every unit test passed, because the test DB is SQLite and SQLite is typeless**. The sweep recorded the attempt BEFORE the write, so 10 active-pipeline rows whose amounts the API had ALREADY returned were burned holding nothing, while the catch logged "non-fatal, will retry" — a LIE, since the candidate query excludes marked rows. Same shape as #944: a comforting message on an unreachable path. Caught ONLY by running the sweep against real prod Postgres; mocked/SQLite verification cannot see it | Adapters persist `AMOUNT_CONFIDENCE_STRUCTURED` (`services/awardAmountExtractor.js`) — the numeric scale `resolveOpportunityAmounts` already uses; never hand-write a confidence label. `enforceAmountEnrichment` calls `recordAttempt` **after** every write succeeds, so a write failure leaves the row unmarked and "will retry" is true. Migrations 139/0143 un-burn the rows the bug already damaged (the fix alone cannot — the burn is permanent by design). **When a change persists a NEW value into an existing column, assert its TYPE in a test — SQLite will not** | `backend/tests/grantsGovAmountAdapter.test.js` ("reports amount_confidence as the NUMERIC structured scale") + `backend/tests/enforceInvariants.test.js` ("does NOT burn a row when the amount WRITE fails" — FAILS on the merged code) |
| The adapter wishlist names only DIAGNOSES, and it has a CONSUMER (2026-07-16). Three defects wore one costume. (1) **Provenance conflation**: `profileHelpers` built ONE `healthSet` from boolean flags + free-text `conditions[]` + `support_needs[]` + `disability_type[]` + `medical_history`, and the disease-lane loop demanded a *disease source lane* for every member — so prod asked the owner to add a lane for `lodging` (a support need), `unsteady gait` / `clawing effect in hands` (symptom prose), `mentally challenged` (a descriptor) and `mobility_needs` (a canonical flag): 4+ of 10 wishlist slots on nonsense. (2) **A denylist guarding the wrong branch**: `NON_DISEASE_HEALTH_SIGNALS` is an exact-match Set, so it could NEVER filter free text — growing it cannot converge, and it did not even cover its own branch (`mobility_needs` leaked). (3) **No consumer**: `amyAgent` emitted one `blocked` telemetry row; a finding that was right every night had no actor — the same write-only shape as `web_parity_gap_queue` | **Provenance is recorded at the WRITE SITE** (`profileHelpers.js`): `signals.health` stays the union (≈14 consumers test canonical tokens on it — do not change it), and `health_conditions` / `health_support` are added alongside. Only `health_conditions` reaches the disease-lane loop; `health_support` routes to a NEED question via `normalizeNeedCategory` — the SAME gate `signals.needs` already uses (NOT `needsTaxonomy.resolveNeedFromSynonym`, a different vocabulary `signals.needs` never consults). A support need that resolves to nothing emits a **convergent** gap ("add `lodging: 'housing'` to `NEED_ALIAS_MAP`" — a 1-line finite fix) instead of an unfillable disease-lane ask. Canonical FLAG tokens are classified by table (`HEALTH_DIAGNOSIS_FLAGS` / `HEALTH_SUPPORT_FLAGS`) because they are finite; free text cannot be. **Consumer**: `services/coverageAudit/conditionSourceSearch.js`, invoked from `amyAgent` — a `no_disease_source` gap becomes a bounded web search whose real hits are queued onto `web_parity_gap_queue` per affected profile, where the EXISTING seeding lane adopts them through the full gate stack. Queued ≠ added. Bounded: 3 conditions/run, 2 queries, `MAX_ATTEMPTS` 3 → `exhausted`, 14d cooldown; a search-provider outage never spends an attempt. Hosted on Amy, NOT Robert (despite the canonical rule naming Robert): `robertSourceDiscovery` is env-gated OFF and lands in its own store that feeds neither the registry nor the queue | `backend/tests/conditionSourceSearch.test.js` + `backend/tests/coverageEvidenceService.test.js` |
| A wishlist entry is RETIRED by an adopted source (the convergence overlay). `conditionCoveredBySource` matches the STATIC `sourceRegistry`, but an adopted source lands in `funding_opportunities` — which that registry never sees. So a consumer that finds and adopts a real epilepsy source would leave the scoreboard reporting "No disease-specific source lane exists for epilepsy" **forever**, permanently holding one of `WISHLIST_CAP`=10 slots and starving new gaps: "never converges, but with a footnote", failing the rule that discovered sources RETIRE wishlist items | `markGapCandidateOutcomes` → `creditConditionCoverage()` writes `system_kv condition_source_coverage` ONLY for candidates that survived the full gate stack (fetch → extract → reality gate → match engine → a real catalog row), so it can never manufacture coverage (G0); `buildCoverageEvidence` loads it once via `loadConditionCoverageOverlay()` and passes it to `conditionCoveredBySource`. **The scoreboard is recomputed from scratch every run, so no aging machinery is needed — the entry simply stops being emitted.** TRAP: `CONDITION_COVERAGE_KV_KEY` is held as a literal in BOTH `webParityBenchmark.js` (producer) and `coverageEvidenceService.js` (consumer) to avoid a heavy import; if they drift the wishlist silently stops converging while both sides still "work" — static tripwire in `conditionSourceSearch.test.js`. Also: the scoreboard's wishlist projection MUST carry `affected_profiles` (ids), not just `affected_profiles_count` — `appendGapCandidates` hard-drops any entry without a `profile_id`, so a consumer written against the count-only shape queues ZERO while reporting success | `backend/tests/conditionSourceSearch.test.js` ("static drift tripwire", "skips a wishlist entry with NO affected profile ids") |
| Condition coverage is decided ONLY by a source's CURATED vocabulary. `conditionCoveredBySource` matched `need_categories + source_id + name + keywords` and returned true when ANY ≥4-char word of the condition appeared anywhere — but `source_id`/`name` are FREE TEXT, so this was a floor of ONE SHARED WORD (the #937/#943 class), and it fired in prod: Reeve "covered" the condition `physical` via "physical disability" in its NAME, and anything named "…disease" covered `chronic kidney disease` | Haystack = **`keywords[]` + `need_categories[]` only** (`source_id`/`name` deliberately dropped); a term matches by **token-boundary containment in either direction** (`renal` must not hit inside `adrenal`), min length 4, with `GENERIC_CONDITION_WORDS` (disease/disorder/chronic/left/right/…) dropped. The load-bearing direction is `keyword ⊂ condition` (`cancer` ⊂ "breast cancer"). **PRECONDITION: every `disease_specific` source MUST carry `keywords[]`** (totality-tested) — `cancer_care` / `alzheimers_gov_services` / `copay_assistance_foundations` had none and would have gone dark. Curated vocabulary must carry the ADJECTIVE form real profiles type (`diabetic`, not only `diabetes` — token matching does not stem). **REJECTED (do not re-attempt): "every distinctive token must match"** — measured against the real registry it flips TEN true covers to false (`breast cancer`, `vascular dementia`, `wheelchair user`, `type 2 diabetes`, `complex ptsd`, `obstructive sleep apnea`…) because the haystack never held the qualifier, and is VACUOUSLY TRUE for an all-generic condition. Strictness in the rule cannot repair a haystack made of the wrong words | `backend/tests/coverageEvidenceService.test.js` ("the coverage floor" — incl. an inline A/B proving the old rule said `true` for 3 real false-covers, the keywords totality, and the whole-token `adrenal` guard) |
| A task-flagged profile FIELD the profile can now answer is resolved EVERYWHERE (Hamilton flags missing fields PER TASK, the owner's fix is PROFILE-WIDE — the Anastasia class, 2026-07-27: 30+ portal tasks each kept an unresolved "Profile is missing first name" row while `basic_information.first_name` sat filled, because only the document-parse path ever reconciled and the task self-heal re-queues `blocked` tasks without resolving flag rows on waiting/review ones) | `reconcileProfileFieldsToTasks()` in `backend/services/hamilton/applicationTaskStore.js` — ONE resolution rule (sections flatten + profiles-row scalars + first/middle/last derived from display_name via canonical `parseFullName`; resolves unresolved field flags on all non-terminal tasks, resumes fully-answered resumable ones; `reconcileProfileAfterParse` is now a thin wrapper). Per-call gates: `PUT /api/profiles/:id` + `PUT /:id/sections/:sectionKey`; boot net: `enforceStaleMissingFieldResolution()` (`ENFORCE_STALE_MISSING_FIELDS=0` count-only; `STALE_MISSING_FIELD_PROFILE_LIMIT` 50/boot). TRAP: `application_missing_info.resolved` is INTEGER on SQLite, BOOLEAN on prod Postgres — predicate must be `IS NOT TRUE`, never `= 0`. **FAFSA-linked portals ride the SAME rule (2026-07-27, "portals that only need her FAFSA linked", profile-generic):** the classifier's `detectFafsaLinkRequirement` (`hamiltonAutomationClassifier.js`) marks link-only portals with `fafsa_link` (PRECISE patterns — a mere FAFSA-eligibility mention is NOT link-only), preflight + the auto_profile pathway (`runFafsaLinkPathway`) file ONE `kind='field', key='fafsa_link'` ask per task, and the reconcile answers it ONLY from the canonical education FAFSA lifecycle (`services/college/fafsaStatus.js`, filed = stage at/after 'submitted' or legacy `fafsa_completed`) — a stray profile field literally named `fafsa_link` is explicitly ignored, and "linked" is never claimed (the completed task records fafsa-on-file readiness, never a studentaid.gov action). N per-portal asks dedupe to ONE `needs_you` entry (`hamiltonProfileSummary`, id `fafsa:link`). `fafsa_link` is in `PREFLIGHT_PROFILE_FIELD_KEYS`, so the existing blocked-task self-heal + this sweep are the boot net — no new sweep | `backend/tests/enforceInvariants.test.js` ("enforceStaleMissingFieldResolution" — 5 tests incl. cross-task resolution, display_name derivation, still-missing-stays, count-only, terminal-untouched; 3 fail on a no-op reconcile) + `backend/tests/hamiltonFafsaLink.test.js` (16 tests: detection + false-positive guards, two-student genericity, junk-field never answers, one-deduped-ask; mutation-verified) |
| A SYSTEM-side task stop that no longer reproduces is CLEARED, and a task whose funding source was PURGED is closed (the Robert White 41-stop class, 2026-07-27: preflight files `crawler_profile_rules` / `application_url` hard stops as missing-info rows, nothing ever re-ran the check — the blocked-task self-heal deliberately skips non-profile-field items — so stops were permanent by construction, and 18 tasks whose grant row had been purged sat blocked forever as "needs your input" zombies) | `recheckHamiltonPolicyStops()` (`backend/services/hamilton/hamiltonStopRecheck.js`) — re-runs the SAME code that wrote each stop (`assessHamiltonFundingSource` for policy; the usable-URL bar for portal URLs) with three honest outcomes: passes → resolve + auto-resume; grant AND catalog row both gone → CANCEL the task (evented, reversible-by-recreate, never deleted); still failing → left visible so a future crawl/URL-rescue clears it. Boot net: `enforceHamiltonStopRecheck()` (`ENFORCE_HAMILTON_STOP_RECHECK=0` count-only; `HAMILTON_STOP_RECHECK_LIMIT` 200/boot), runs right after `stale_missing_field_resolution`. **ROOT CAUSE of the biggest class (the TSAA thread, 2026-07-27): the match store is a ROLLING SNAPSHOT — `crawlerOsPersistence`'s reconcile DELETEs a profile's crawler-os/xmatch rows every run and re-inserts only what THAT run re-found — while tasks/grants are DURABLE, so any long-lived task eventually pointed at a wiped row and `missing_profile_crawler_match` became permanent. The policy therefore falls back to a LIVE `computeMatchDecision` when no stored row exists (matchEngine is the sole authority; the funding-sources route already recomputes live for the same reason): accept/review → endorsed (`matcher_version:'live-recheck'`), reject → honest `profile_match_rejected`, engine unavailable → the conservative stop. Do NOT "fix" this by making the reconcile keep task-backed rows — the snapshot semantics protect the matches view from stale/ineligible rows by design** | `backend/tests/enforceInvariants.test.js` ("enforceHamiltonStopRecheck" — 6 tests incl. endorsed-pair-resumes, zombie-cancelled, honest-stop-untouched, URL-found-stamped-on-task, search-page-refused, count-only; 3 fail on a no-op recheck) |
| Admins are never re-interviewed (an ADMIN/owner account gets Anya's interview at most once; a secondary login must never re-open it — the admin bulk reset stamped `pending_reinterview` onto admin rows too, re-prompting the owner on every login). Non-admins keep the deliberate reset flow | `enforceAdminReinterviewSuppression()` — clears `pending_reinterview` → `completed` on admin rows with any first-run signal (`has_completed_onboarding` / `onboarding_completed_at` / prior `last_login_at`); fresh admins keep their one first-run. Per-call gate: `resolveGuidedCycleTourStatus()` (`backend/services/onboardingGates.js`) at every auth-payload serialization; UI net: global `LoginGapInterviewLauncher` mount never interviews admins | `backend/tests/adminReinterviewGate.test.js` |
| Amy's synthetic training profiles EXPIRE (a `created_by='agent:amy'` synthetic tagged `allow_sam_cleanup` never outlives its `expires_at` past the next boot — Amy's end-of-run cleanup is scoped to the ids crawled in THAT run, so when discovery skips/throws the list is empty, nothing is deleted, and prior runs' leftovers are permanently out of scope: the 13-live/0-reaped prod class) | `enforceAmySyntheticExpiry()` — calls the canonical `cleanupExpiredAmyProfiles()` (`backend/services/amy/amyProfileStore.js`), the SAME guarded sweep the end-of-run second pass in `runAmyTraining` and the nightly sweep use; every guard holds (designated-profile, `allow_sam_cleanup`, `synthetic`, crawled-required with the bounded `AMY_NEVER_CRAWLED_MAX_AGE_HOURS` 96h escape hatch, 6h crawled grace); disable via `ENFORCE_AMY_SYNTHETIC_EXPIRY=0` for count-only. Per-call first line: the unscoped expired-only pass at the end of every `runAmyTraining` (telemetry: `amy.cleanup.expired_sweep`) | `backend/tests/enforceInvariants.test.js` ("enforceAmySyntheticExpiry") + `backend/tests/amyAgent.test.js` |
| A Yana lead's contact email belongs to THAT org (enrichment SORTED candidate homepages by name score then took `candidates[0]` unconditionally — a preference with NO floor, so when nothing matched the org it scraped an unrelated site's address: `helpdesk@franklin.edu` on 10 distinct universities, a newspaper's `admin@conwaydailysun.com` on 14 nonprofits, worldatlas/mathway/roblox addresses on dozens more — 147 of 490 enriched candidates shared an address with a DIFFERENT org, each one making John draft outreach to the wrong organization) | `enforceLeadContactPlausibility()` — strips the address, returns the lead to `needs_enrichment` and RESETS `pushed_to_john`/`enrich_attempts` so the gated enricher can find the right one; never invents a contact, never deletes the lead; bounded (`LEAD_CONTACT_PLAUSIBILITY_LIMIT`, default 500); disable via `ENFORCE_LEAD_CONTACT_PLAUSIBILITY=0` for count-only. Per-call gate: `isPlausibleHomepage()` (`backend/services/yana/prospectExclusions.js`) in `makeContactEnricher`; mailbox residue: `purgeImplausibleDrafts()` (`backend/services/john/johnDraftPlausibilityPurge.js`, `POST /api/john/purge-implausible`) archives live drafts failing the same gate. **The bar is the WHOLE name, not one word.** The first version required ONE distinctive token in the hostname-or-title, which replaced "no floor" with a floor of one word — and one word is what an unrelated business coincidentally shares: 8 of the 10 live drafts on 2026-07-15 PASSED it (`willienelson.com` for the "Willie Julie Educational Foundation", `johnson.edu` for the "Johnson City Area Arts Council", `decaturwatchfest26.com` for a county education foundation, `robertsoncountyfuneralhome.com` — the #937 class still shipping a year later). Two conditions must now BOTH hold: (1) COVERAGE — EVERY distinctive token appears in hostname+title (category words like "university"/"education" are never sufficient alone: "Franklin University" ≠ "University of Minnesota"); (2) HOST ANCHOR — the hostname carries < 4 letters the org's name cannot explain, where each name token is consumed whole or by its longest ≥4-char PREFIX (this is what rescues abbreviations like upenn.edu ← "pennsylvania", and what proves `robertsoncountyfuneralhome` wrong: "countyfuneralhome" is left over). The anchor is also what stops a search-result TITLE from authorizing an unrelated host — worldatlas.com really does have a page titled "Ohio". Do NOT re-weaken this to `.some()`; a shared surname or place name is a coincidence, not an identity | `backend/tests/yanaEnrichmentPlausibility.test.js` (incl. the 10 live wrong-recipient drafts, which fail on the one-token gate) + `backend/tests/johnDraftPlausibilityPurge.test.js` |
| No LIVE draft in the mailbox is addressed to the WRONG organization (the lead repair above fixes the CAUSE; a draft John already wrote from a bad lead is the RESIDUE, and it sits in Drafts one click from being sent to a stranger — on 2026-07-15, 9 of 10 live drafts went to an unrelated business: `willienelson.com` for the "Willie Julie Educational Foundation", `robertsoncountyfuneralhome.com` for the "Robertson Community Health Foundation", `help.starz.com` for "Star News Education Foundation" — and that draft's own body admitted the research "pointed me mostly toward a streaming service") | `enforceJohnDraftPlausibility()` — calls the canonical `purgeImplausibleDrafts()` (`backend/services/john/johnDraftPlausibilityPurge.js`), which existed and would have caught these but was reachable ONLY via a manual `POST /api/john/purge-implausible`, so nothing ever ran it. Archives the row + removes the draft from the mailbox (Graph DELETE → Deleted Items, recoverable; the provider REFUSES anything not still a draft, and a refused delete leaves the ROW alone so the store never lies about what is in the mailbox). NEVER sends; a draft with no org name or no recipient is skipped, not purged; no provider = report-only. Runs AFTER `enforceLeadContactPlausibility` so a purged draft can be re-drafted from a corrected lead. Disable via `ENFORCE_JOHN_DRAFT_PLAUSIBILITY=0` for count-only; bound `JOHN_DRAFT_PLAUSIBILITY_LIMIT` (200). TRAP: the table is `john_email_drafts` — the indexes are named `idx_john_drafts_*`, and probing that wrong name returns `skipped:'schema'` and no-ops the whole sweep while reading green | `backend/tests/enforceJohnDraftPlausibility.test.js` (incl. all 9 real wrong-org drafts, and an assertion the sweep never reaches a send path) |
| Application-URL rescue (a real candidate rejected ONLY for a missing URL — stage `url` / reason `missing_application_url`, the docs/email/LLM-extract class — gets ONE bounded, budget-paced chance to be rescued with a real, liveness-verified page found by title+sponsor web search; a URL is NEVER invented or guessed, and a search-provider outage never burns a candidate's chance) | `enforceApplicationUrlRescue()` (re-drives the rejection's `raw_meta.candidate` snapshot through the full `upsertFundingOpportunity` gate stack with the found URL; `system_kv` cursor `url_rescue_last_rejection_id` = one attempt each, NOT advanced when every search came back empty/failed; bounds `URL_RESCUE_BOOT_LIMIT` (8) + `URL_RESCUE_TIME_BUDGET_MS` (20s); disable via `ENFORCE_URL_RESCUE=0` for count-only). Finder: `findOfficialUrlForOpportunity()` in `backend/services/urlEnrichment.js` (searchWeb → token-overlap plausibility → `checkUrl` liveness); write-side snapshot: url-gate `logRejection` in `opportunityInserter.js` | `backend/tests/enforceInvariants.test.js` ("enforceApplicationUrlRescue") + `backend/tests/urlEnrichment.test.js` |
| A "stable" enrich failure is a fact about the ROW, and N identical ones against one silent host are a fact about the RUN (the 2026-07-22 mass burn: 11 min after #1006 deployed, ONE sweep burned 34 grants.gov rows in ~5s with the same stable-class reason — every id answers perfectly from the prod egress on 2026-07-25, so the API had a degraded afternoon and each per-row "stable" verdict was wrong; the rows were then invisible to the #1011 reason-triage that deployed two days later, because burned rows are never candidates) | **Systemic-burn guard**: both sweeps DEFER stable failures (no data write is pending, so the mark-after-write rule is untouched) and burn only groups under `AMOUNT_ENRICH_SYSTEMIC_STREAK` (4) per (host, reason) — a group at/over it with ZERO same-host successful reads that run is reclassified ENVIRONMENT-style (no burn, no ordinary-retry spend, `amount_enrich_env_attempts`+1 → the existing `unanswered_blocked` lane). A same-host real read proves the host alive and row-specific failures still burn. Page-fetch 401/403/429 now also report `environment:true` (`amountEnrichment.js` — WAF/bot-block refuses OUR caller, not the URL; 404/410 stay stable). Un-burn migration 154/0158 re-frees the mass-burned rows (the guard alone can never reach an already-burned row — the migration-135 rule) | `backend/tests/enforceInvariants.test.js` ("systemic-burn guard" — withheld-burns, live-host-still-burns, below-streak-burns, host+reason grouping) + `backend/tests/amountEnrichment.test.js` (401/403/429 → environment) |
| A coverage-gap statement names something the owner can actually FIX, and finite vocabularies are covered TOTALLY (2026-07-26, the "geographically out of scope ×5 / no lane for visual impairment" report class). Three defects: (1) canonical `HEALTH_DIAGNOSIS_FLAGS` tokens (hiv/tbi/amputee/rare_disease/terminal/visual_impairment) had no covering `disease_specific` source, so any profile carrying the flag minted an UNFILLABLE nightly wishlist ask; (2) "state has a dedicated row" was treated as state coverage, but TN/WV/PA/OR's rows were a disability waiver / business funding / campaign finance — a family there selecting on housing/food got NOTHING; (3) a lane's "top exclusion reason" counted ALL sources' votes, so ~50 other states' `geography_out_of_scope` votes drowned the in-state story and org profiles read as GEOGRAPHY gaps no one could fix | Registry: vision/brain-injury/medical-debt/hiv/amputee/rare-disease/terminal lanes + TN/WV/PA/OR household portals (`STATE_BENEFITS_PORTALS`); sub-4-char flags (`hiv`, `tbi`) are covered via the REVERSE match direction — a multi-word keyword carrying the token (`'tbi survivor'`, `'hiv care'`), since coverage terms under 4 chars are filtered. Reason picking: `dominantExclusionReason()` (coverageEvidenceService) votes only IN-GEOGRAPHIC-SCOPE exclusions; all-geo (junk state "USA") keeps the honest geographic verdict. "clawing effect in hands" (symptom prose in a diagnosis field) stays an HONEST gap for the wishlist consumer's web search — do not fake a lane for it | `backend/tests/coverageEvidenceService.test.js` ("EVERY canonical diagnosis flag has a covering source", "every state serves individual/family HOUSEHOLD needs", "51 geography votes never outvote one in-scope reason") |
| A saved portal session KNOWS WHEN A HUMAN AUTHENTICATED IT, and a keep-alive probe never manufactures liveness (2026-08-01, the "sessions are durable for most portals" class). Three defects wore one costume. (1) **The probe target was public**: `hamiltonSessionKeepAlive` navigated to `meta.landing_url \|\| https://<host>/`, and `landing_url` is READ there and **WRITTEN NOWHERE** in the backend — so every probe since inception hit a marketing page that renders identically signed in or out. (2) **Silence was read as confirmation**: the verdict was "not an auth-challenge category ⇒ still signed in", but `classifyBlocker` returns `unknown` for any page it has no rule for. Verified live with a ZERO-cookie context: `collegefortn.org` (→`missing_required_document`), `leic.tennessee.edu` (→`unknown`) and `studentaid.gov` (→`unknown`, 1914 chars of "Manage and Repay Your Federal Student Loans") ALL scored **"refreshed"** while holding no session; prod carried `keepalive_refreshes: 11 / 9 / 9` as a result. (3) **The clock was destroyed**: `importSession` sets `established_at = now()`, so each cookie refresh moved the establishment time forward — prod row 9d9f6b55 read `created_at 2026-07-21T02:55` against `established_at 2026-08-01T03:23`, 11 days of drift, making lifetime unreconstructable | Liveness is claimed ONLY from a POSITIVE observation: an **auth-gated** path from the `backend/config/portalSessionProfiles.js` REGISTRY that did not land on a sign-in surface. New outcome `unverified` (cookies refreshed, liveness unknown, **nothing recorded**) covers every host with no registered probe path — silence proves nothing in either direction. Death is detected STRUCTURALLY by final URL (`/my-activity/` → `/fsa-id/sign-in/landing`, the exact live redirect), because that sign-in page itself classifies as `unknown`. `session_established_at` is pinned in metadata by `importSession` and carried across refreshes (only a real human login resets it); `markSessionExpired` now MERGES metadata instead of replacing it, so expiring a session no longer erases its consent record and establishment stamp. Measurements land in `system_kv portal_session_lifetime_observations` as an INTERVAL (`confirmedAliveMaxMs` ≤ true lifetime ≤ `confirmedDeadMinMs`) with an `estimateSource` of `measured`/`measured_lower_bound`/`seed`/`unknown`, so no surface can present a guess as a measurement. **TRAP: `Number(null)` is 0 and IS finite** — a bare `Number.isFinite()` on a null seed reported every unregistered host as carrying a 0 ms declared lifetime. Boot net: `enforcePortalSessionLifetimeStamp()` (`ENFORCE_PORTAL_SESSION_LIFETIME=0` count-only, `PORTAL_SESSION_STAMP_LIMIT` 500) stamps `session_established_at` from the row's own `created_at` — a row without one is counted `unstamped`, never invented. **Verified against a replica of all 7 real prod rows: 7 repaired, 0 on re-run, recovering 11/9/9/3.5 days of drift** | `backend/tests/hamiltonSessionKeepAliveHonesty.test.js` (5 tests; mutation-verified — restoring the old target + verdict reddens 4) + `backend/tests/portalSyncStaleness.test.js` ("portal session registry", "lifetime ledger", "enforcePortalSessionLifetimeStamp") |
| A portal that has drifted out of sync is SURFACED AT LOGIN, scoped to ONE profile (owner rule 2026-08-01: "alert the profile owner and admin ON LOGIN … a portal needs synced if it hasn't been synced in 7 days, OR if the profile has updated awards or information since the previous sync"). Chosen over a keep-alive cadence because a human being present is the only reliable way to refresh a short-lived session — studentaid.gov measured authenticated at T+30s and refused at T+~20min — and because probing an Akamai-fronted federal host on a tight loop risks a permanent bot wall, which is strictly worse than an expired session | `resolveProfileSyncNeeds()` in `backend/services/hamilton/portalSyncStaleness.js` — ONE rule, folded into the existing `getHamiltonReadiness()` payload (`sync_needs`) and emitted through the existing `emitHamiltonNotificationToProfileAndAdmins` dedup pattern (`emitPortalSyncReminders`). **WHICH TIMESTAMPS ARE TRUSTWORTHY, verified read-only in prod: `profiles.updated_at` is REJECTED** (35 of 39 rows share one second — a bulk sweep; keying on it marks every portal stale on every login, an alert that can never go green); **all-rows `grants.updated_at` is REJECTED** (283 of 524 are `discovered` and move on crawler churn); ACCEPTED are `grants.updated_at` restricted to `AWARD_ACTIVITY_STAGES` (submitted/awarded/follow_up/declined, from the canonical `PIPELINE_STAGE`) or `amount_awarded > 0` — 12 real rows in prod — and `profile_sections.updated_at` filtered by `MACHINE_WRITER_RX` (a namespace shape, not an exact-match Set), with `HUMAN_ACTOR_RX` winning first so `system_admin_token` is not confused with `system-seed`. A NULL writer counts as HUMAN (prod's null rows are real UI edits). Only `status='completed'` counts as a sync (prod holds 4 failed collegefortn.org runs beside 4 completed). **SCOPE IS ONE PROFILE, ALWAYS** — there is deliberately no aggregate entry point, and `requireProfileScope` 400s without a profileId, so an admin not working inside a profile gets nothing rather than a 39-profile digest. A portal with no valid session is asked to `sign_in`, never `sync_now`; one with no session, no credential and no sync history is not listed at all. Prompts are capped (`HAMILTON_PORTAL_SYNC_MAX_PROMPTS`, 5) with `truncated_count` reported — real prod data for one profile qualifies **14** portals, which is the flood the owner rejected — and `sync_now` sorts ahead of `sign_in` so one-click items are never crowded out. Companion: cloud-login `/complete` fires `runPortalSync` **while the session is provably warm** (`HAMILTON_SYNC_ON_CAPTURE=0` to disable), fire-and-forget because a sync outlives the HTTP edge timeout | `backend/tests/portalSyncStaleness.test.js` (26 tests; mutation-verified — dropping the per-profile SQL scoping and the no-profile guard reddens 6, incl. "an admin with access to several profiles gets prompts ONLY for the active one") |
| `opportunity_kind` has ONE owner per value class, and the WRITER honors the structural rule (the kind tug-of-war: the `locator_kind_classification` sweep repaired ~387 rows EVERY night and never converged — `crawlerOsPersistence.osOppToLiveRow` restamped the OS machine kind and its ON CONFLICT UPDATE clobbered the sweep's verdict on every re-crawl, while `classifyOpportunityKind` recomputed heuristics blind to the structural rules; prod 2026-07-25: 130 sam.gov `/fal/` + 233 ProPublica rows back to 'PROGRAM' hours after the sweep, 10 census rows permanently "unreadable". A repair count that never trends to zero IS the tug-of-war signature) | Writer-side: `classifyOpportunityKind` (opportunityRealityGate.js) consults `classifyLocatorKindFromRow` right after the explicit-canonical check, and `osOppToLiveRow` applies the same rule over the OS kind; UPDATE-side net: `fundingOpportunityConflictExpr` never downgrades stored 'directory'/'benefit' to a `GENERIC_OVERRIDABLE_KINDS` stamp and an incoming NULL never wipes a stored kind. That list now lives in `locatorUrlKind.js` (re-exported by enforceInvariants) so sweep + writers cannot drift. Orphan grants (no catalog row → no kind column for the census): `enforceGrantDirectAmountEnrichment` short-circuits classifier-claimed rows to the grant-side honest vocabulary — 'benefit' → `amount_status 'varies'` (the kind's own stated semantic), directory → honest `amount_text` label, no fetch spent. Amy's `amount_recall_miss` denominator excludes BENEFIT kinds like DIRECTORY (no fixed per-award figure by design) | `backend/tests/locatorKindTugOfWar.test.js` (bridge INSERT/UPDATE + gate override + canonical-wins) + `backend/tests/enforceInvariants.test.js` (orphan structural short-circuit, incl. the race-guard) + `backend/tests/amyAgent.test.js` (benefit-kind exclusion keeps its teeth) |

| A surfaced match is in SCOPE — the right COUNTRY, the right PLACE, and an award a person can actually receive (2026-08-01, the GeneMac report: "the Funding Sources list shows the SAME sources that appear for unrelated profiles, and NO legitimate matches"). The geography gate compared a US STATE CODE and nothing else, and `crawlerOsPersistence.osOppToLiveRow` stamps `is_national` whenever the lane supplies no geography — so a row with no state was not merely un-gated, it scored as a fully-eligible NATIONWIDE US program. Two whole classes fell through: (1) **COUNTRY** — a program administered by another government (Ireland's Housing Adaptation Grant on citizensinformation.ie, gov.uk, sassa.gov.za, dda.gov.in) has no US state, so `oppStateRaw` is empty and the entire state-mismatch block short-circuits; (2) **PLACE** — county/city locators are minted per-place with the place ONLY in the title ("Polk County, TN — Local assistance programs near you (findhelp)") and `state NULL / geo_county NULL / geo_zip NULL`, so every profile in the fleet is geographically eligible for every other profile's county, and because the cross-profile (xmatch) lane scores against a thesis STUB rather than the profile's full inventory those out-of-state locators OUT-SCORE the profile's own in-state row (13 vs 10 in prod). A third class had no gate at all: **AWARD SCALE** — `enforceIndividualAmountCeiling` has purged >$100K rows from an individual's PIPELINE for months, but the MATCH store the owner actually looks at was never held to that bar, so HUD FHIP ($1.25M), NSF Oceanographic Facilities ($47.5M) and Title X ($22M) surfaced to seniors/students as "Individual applicant". `applicantTypeGate` cannot reach these — it decides from eligibility PROSE and these rows carry none (FHIP in prod: `eligibility_text` NULL, `eligibility_bullets` [], `entity_types_allowed` []) — and it is not on the discovery/read path at all (only `routes/grants.js`, `opportunityMatcher`, `pipelineEligibilitySweep` import it). Prod 2026-08-01: 618 foreign-jurisdiction match rows / 34 profiles; 684 out-of-area locator rows / 25 profiles; 218 institutional-scale rows / 27 profiles; 84 catalog rows mis-scoped as national | Canonical facts: `backend/config/opportunityJurisdiction.js` (`detectForeignJurisdiction` — registrable-suffix ccTLD on the row's OWN url, with `JURISDICTION_NEUTRAL_HOSTS` so a link shortener like `lnkd.in` fronting a US fellowship is never "Indian"; `declaredStateFromTitle` / `correctedGeoScopeFromTitle` — the `"<Place>, XX — "` shape ONLY, never a bare two-letter coincidence) + `backend/config/individualAwardCeiling.js` (the SAME constant and env var the pipeline sweep uses; static drift tripwire in `individualAwardCeiling.test.js`). **Per-call gates** in `makeDecision` (`services/matchEngine.js`): foreign jurisdiction → REJECT; a place-declaring row out of the profile's states → REJECT (it is exclusive by construction — a county directory is not "may still be accessible"); an individual-root profile (resolved through `profileTypeRegistry`, so `senior`/`disabled_adult` roll up correctly — the hand-written list did not see them) + a stated award above the ceiling → REJECT. **Writer gate**: `osOppToLiveRow` applies `correctedGeoScopeFromTitle` so the sweep converges instead of repairing nightly. **Boot nets** (all sweep EVERY profile; the match store is a rolling snapshot, so a producer fix alone lands only after each profile's next crawl): `enforceDeclaredGeoScope()` → `enforceDeclaredPlaceScopeMatches()` → `enforceForeignJurisdictionMatches()` → `enforceIndividualMatchAwardCeiling()`, in that order so the catalog is re-scoped before anything reads it. `ENFORCE_DECLARED_GEO_SCOPE` / `ENFORCE_DECLARED_PLACE_SCOPE` / `ENFORCE_FOREIGN_JURISDICTION_SCOPE` / `ENFORCE_INDIVIDUAL_MATCH_CEILING` = 0 for count-only; bounds `DECLARED_GEO_SCOPE_LIMIT` (500) / `MATCH_SCOPE_PURGE_LIMIT` (2000). **TRAP: EVERY narrowing here is a SQL PREDICATE, never a post-LIMIT JS filter** — an unscoped catalog holds thousands of state-less rows and filtering after the LIMIT would let rows that declare nothing permanently starve the ones that do (the #944 "green while doing nothing" class). **This trap was not hypothetical: `enforceForeignJurisdictionMatches` SHIPPED without a predicate** (#1080) — it selected MATCH rows with no `WHERE` at all and decided foreign-ness in JS after `LIMIT ?`, so the live boot recorded `{"foreign_jurisdiction_matches","repaired":1,"scanned":2000}` (`scanned` == the bound) while 515 of 516 foreign rows stayed structurally unreachable and the sweep reported `ok:true` every boot. The three sibling sweeps written in the SAME PR all had predicates and all converged (84/87, 494/875, 157/506) — so **a `scanned` value equal to the bound is the signature to look for in the boot summary**, and `system_kv enforce_invariants_last_run` is where to read it. The fix inverts the scope: **foreignness is a property of the OPPORTUNITY, not the match**, so the candidate set is the CATALOG narrowed by `foreignUrlSqlPredicate()` (a registry-generated LIKE list, a deliberate SUPERSET that `detectForeignJurisdiction` then adjudicates), and the bound now limits DELETES, never DISCOVERY — 193 catalog candidates → 186 confirmed → 545 match rows across 32 profiles, vs 0 findable under the shipped query. **MISSING = NEUTRAL everywhere**: a profile with no resolvable state loses nothing, an unknown profile type is never treated as an individual, and a row that states NO amount is exempt (silence is not a denial). The CATALOG row is never deleted for being foreign — only the match, which is the claim "you can apply to this" | `backend/tests/opportunityJurisdiction.test.js` (registry totality, the real prod hosts, the shortener guard, the two-letter-coincidence guard) + `backend/tests/individualAwardCeiling.test.js` (drift tripwire vs the pipeline sweep) + `backend/tests/matchScopeGates.test.js` (13 tests on the real prod pairs; 5 fail on pre-fix code) + `backend/tests/enforceInvariants.test.js` ("scope of a surfaced match" + "out-of-area locator" — 16 tests, 12 fail on a no-op sweep body) + `backend/tests/matchConfidenceHonesty.test.js` + `backend/tests/locatorKindTugOfWar.test.js` (writer-side geo scope) |
| A catalog row that RECORDS the profile it was discovered for REACHES that profile (2026-08-01, the fleet-wide edition of #1089). `profile_opportunity_matches` is a ROLLING SNAPSHOT — `crawlerOsPersistenceCore.persistRun` DELETEs a profile's `crawler-os`/`crawler-os-xmatch` rows and re-inserts only what THAT run re-found — so a row is scored once at discovery and then erased. The decay is visible in the data: of active `source='web_search'` rows, those created in AUGUST are 37% matched (73/197), July 3.2% (220/6,871), June 3.9% (26/661). Three other sources sit at EXACTLY ZERO for a different reason: their producers are DIRECT catalog inserts that never call `persistRun` at all — `osm_overpass` (2,756 rows, `nationalZipCrawler.js`), `verified_real` (865, `comprehensiveCrawlerOptimized`/`realLocationFundingCrawler`/`ensureMinimumNationalOpportunities`/`routes/crawlers.js`) and `school_portal` (164, `schoolPortalImportService.js` raw INSERT). **NOT ONE of those rows was ever scored-and-rejected — they were never scored at all** | `enforceProfileDiscoveredCatalogLinkage()` — the linking key is the ONE fact the row itself records, `funding_opportunities.profile_id` (174 active prod rows: 113 `school_portal`, 61 `web_search`). **Every broader key was measured against the REAL engine read-only in prod and FLOODS** — declared geography (state OR national) over `verified_real`: 7,581 pairs → 5,393 links, incl. "Georgia Senior Property Tax Exemptions" ACCEPTed for a Puerto Rico small-business owner; canonical NEED-intersection + geography over `web_search`: 23,422 pairs → 14,822 links across 2,404 rows; the same key over `school_portal`: 1,115 links, i.e. every school offered to every student. **There is NO recoverable per-row provenance for the other 7,555 `web_search` rows** — `opportunity_sources` stores only `source_id='web_search'`, `raw_source_payload` is NULL on all 7,729, and the `source_query` that DID record the need ("medical grants for individual Cleveland, TN") lived on the match row the reconcile deleted. Those stay unmatched, deliberately. **THE ASPIRATION GUARD IS NOT OPTIONAL**: 107 of the 113 `school_portal` rows were minted from ONE student's nineteen `education.target_colleges` (Harvard, Oberlin, Penn State…), so linking on provenance alone would re-admit through a second door exactly the set #1089 excluded — the sweep reuses the SAME `config/profileInstitutions.js` registry (`resolveAspirationalInstitutions`, cap 60 not 3 — a truncated refusal list lets rows through the guard they exist to trip). Engine is the sole authority (REJECT never written); no score bar added or lowered. Candidate discovery is a SQL `NOT EXISTS` PREDICATE, never a post-LIMIT JS filter. Rows land as `matcher_version='profile-discovery-link'`, which the reconcile DELETE deliberately does not name and which `SURFACED_MATCHER_VERSIONS` reads back. Bounded `PROFILE_DISCOVERY_LINK_LIMIT` (500); `ENFORCE_PROFILE_DISCOVERY_LINK=0` count-only; convergence skipped on a truncated boot. **Companion drift fix: `services/matching/needFirstReconciler.js` hand-typed its own lane Set and had already dropped `institution-link` — it now reads the registry** | `backend/tests/profileDiscoveryLinkage.test.js` (19 tests, mutation-verified 9 ways — each mutation reddens only its own guard: aspiration-guard removed, SQL predicate → post-LIMIT JS filter, REJECT written, orphan re-pointed, is_active dropped, count-only ignored, convergence removed, reconciler drift restored, lane removed from registry) + boot-step totality 42 → 43 + `matchSurfacing.test.js` registry pin |
| A profile's DECLARED facts drive what the crawlers SEARCH FOR and which catalog rows are LOOKED AT, and a DECLINED aid type is refused everywhere (2026-08-02, the owner's north star: "It will determine the need, run the correct crawlers, and use the profile information in finding the funding source"). **The system stored the fact and pattern-matched instead of reasoning from it.** `buildProfileSignals` returns `signals.keywords` as ONE undifferentiated bag (453 entries for Anastasia White: display-name tokens, gender synonyms, prose fragments, bare stopwords first; her real field vocabulary at index 127+). `profileContextToThesisInput` poured it into `tags` and `buildThesis` took `.slice(0, 12)` — so for a student whose `education.intended_major` is **"Forensic Science"**, the ONLY topical seed the open-web query builder consumes resolved to `['anastasia','nicole','white','female','woman','women','girl','girls','female-led','led','female identifying','identifying']`. Her name and twelve gender synonyms. Measured consequences in prod 2026-08-02: **forensic rows 1 of 13, criminal-justice 1 of 12, TN HOPE 0 of 21** — while replaying the REAL `computeMatchDecision` on pairs nobody had ever scored returns **ACCEPT 83** ("AFTE Forensic Science Scholarship"), ACCEPT 79 ×2 and **ACCEPT 100** ("HOPE Scholarship", "Matches 70 of the profile's 70 data points"). The ENGINE was never the drop point (the #1089/#1090 finding again): there was no KEY that could reach a topically-relevant row. Separately, `education.aid_types_accepted` gated **portal sync only**, so a student who accepts grants/endowments/scholarships and no work-study carried "Federal Work-Study" as an ACCEPT at score 100 | Canonical facts: `backend/config/profileDerivedFacts.js` — `DERIVED_FACT_FIELDS` is the REGISTRY (field → fact kind → `recallSafe`), and **every derived fact carries the registry field id it came from**; `deriveProfileFacts` also derives `stageOfLife` (`High School Senior` + `college_courses=Yes` ⇒ `dual_enrolled_incoming_freshman`; a profile that says nothing gets `null`, never a guess). Institutions DELEGATE to `profileInstitutions.js` and aid types to `aidTypePreferences.js` — never re-derived (a totality test asserts no field is duplicated). **Producer side**: `profileContextToThesisInput` emits `derived_interest_terms` + `derived_facts`; `buildThesis` places derived terms BEFORE the mined `tags` bag so the bound truncates the WEAKEST evidence, never the major (`tags` itself is untouched — ~14 consumers test canonical tokens on it). `webQueries` promotes the top 2 derived topics to **CORE**: the interest-keyed queries live in the ROTATED EXTRA pool and `.slice(0, max)` truncates from the END, so at the live cap (maxQueries 14) **zero** topical queries ever ran. **Recall net**: `enforceDeclaredFieldOfStudyRecall()` (`ENFORCE_FIELD_OF_STUDY_LINK=0` count-only, `FIELD_OF_STUDY_LINK_LIMIT` 500), matcher_version `field-of-study-link` (added to `SURFACED_MATCHER_VERSIONS`; the reconcile only deletes `crawler-os`/`crawler-os-xmatch`, so it survives). **Aid preference**: `evaluateOpportunityAgainstPreferences` re-asserted in `matchEngine.makeDecision` — the one choke point every surface passes through. **EVERY BROADER RECALL KEY WAS MEASURED AGAINST THE REAL ENGINE ON REAL PROD PAIRS AND FLOODS — do not re-attempt without reading this**: bare in-state geography = 8,215 pairs → **6,210 links** (Anastasia alone gains 632, incl. "Route 55 — DONELSON/DELL STATION OUTBOUND (shelter)"); mined `programs_services.keywords` as recall terms = 613 pairs → **471 links**, 211 from one profile's "human services"; a US STATE NAME as a topic ("south dakota") linked 15 rows to a ministry on the state name alone — a claim the geography gate already owns, hence `PLACE_TERMS` derived from `STATE_REGISTRY`. **NOT SHIPPED, and why**: in-state + student-aid-title + the aid taxonomy = 240 pairs → 217 links, which DOES reach TN HOPE (ACCEPT 100) and the Bradley County Bar Association Scholarship (ACCEPT 91) but also hands a high-school senior "Vanderbilt School of Medicine Merit Scholarship" (84) and "Osher Reentry Scholarship" (84, for returning adults). The engine ACCEPTS those, so the missing piece is a **STAGE-OF-LIFE eligibility gate inside the engine** — `deriveStageOfLife` now supplies the fact it needs; the gate itself is a separate change with fleet-wide blast radius. **TRAPS**: the recall haystack is TITLE + SPONSOR only — `description` is unbounded prose and is what made "Bold.org — Housing & Living Expense Scholarships" register as a forensic row (#1089's "evidence is the SPONSOR only", one door over); terms are MULTI-WORD only, because a single word is a coincidence magnet ("science" reaches 258 STEM rows, "white" is a surname); candidate discovery is a SQL LIKE PREDICATE adjudicated row-by-row by `titleStatesTerm` (never a post-`LIMIT` JS filter); and **convergence must re-adjudicate existing links against the profile's CURRENT terms, NOT against the candidate set** — that set excludes already-linked rows by design, so diffing against it deletes every link on the next boot (caught by the idempotence test, which failed on the first implementation). Aid type is classified from the TITLE only: a scholarship page that merely MENTIONS work-study must not be denied. Boot-step totality **43 → 44**. Measured cost of the shipped key, fleet-wide: 5 of 33 real profiles declare such a term, 9 rows enter the superset, 6 survive adjudication, engine ACCEPTs 4 / REJECTs 2 — and the aid gate refuses exactly **1 of Anastasia's 104** surfaced matches (Federal Work-Study), with only 2 of 33 profiles declaring a preference at all. **STILL OPEN**: lane selection (`crawler-os/planner.js`) keys on applicant_type / need / geography / `is_research_org` and has NO field-of-study dimension; measured for Anastasia it selects 54 of 167 sources whose first 22 are disease/disability lanes (kidney, autism, arthritis, HIV, amputee, rare disease) driven by a generic `disability` need, while her scholarship sources rank 23-25. `thesis.derived_facts` now reaches the planner so that fix is a follow-up, not a re-derivation | `backend/tests/profileDerivedFacts.test.js` (registry totality, delegation-not-forking, the Anastasia regression, each precision rule) + `backend/tests/declaredFieldOfStudyRecall.test.js` (22 tests: sweep, sponsor-only reach, provenance, idempotence, convergence, count-only, never-writes-a-REJECT, the query-building WIRING, and the aid gate; mutation-verified 7 ways — removing the aid gate, linking a REJECT, allowing single words, ignoring `PLACE_TERMS`, substring-instead-of-token-boundary, un-preferring derived terms, and un-promoting the topical query each redden only their own guard) |
| **Amy SEARCHES FOR HOLES; she does not replay a catalog — and every finding class has an ACTOR** (owner directive 2026-08-02: "creating up to 50 new profiles a day with the goal of finding gaps … I want to be able to read about them in the morning email, not have to deal with them"). Four defects wore one costume. (1) **The cohort WAS the catalog**: 32 fixed archetypes in `syntheticProfileCatalog`, all run nightly, varied only by a seeded pick from TEN hard-coded cities — prod's `by_category` on 2026-08-02 is literally the catalog key list. Every archetype is SINGLE-AXIS (a veteran OR a business; a church OR a nonprofit) while the planner gates on the INTERSECTION, so "a veteran starting a food truck in WV" was untestable and 40 states had never been exercised. (2) **`buildApprovalQueue` never read `e.findings`** — it built items from five evaluation FIELDS (`status`, `false_positives`, `ineligible_accepts`, `sources_failed`) and nothing else, so nine of the seventeen declared finding classes could not produce an item, acquire a lever, enter the #1085 ledger, or be closed by anything. Measured over 21 retained ET days in `system_kv amy_flywheel_cohort`: `institution_recall_miss` 21/21 days (282 occurrences), `amount_recall_miss` 16/21 (654), `hyperlocal_recall_miss` 8/21 (480), `source_fetch_failed` 16/21 (76) — none of them ever green, while `weak_match` (2 days, 11 occurrences) LED the report because it was one of the five things the queue could see. (3) **Cleanup ran AFTER `saveAmyReport`**, so `combined.cleanup`/`cleanup_expired` were assigned to an object already written to `system_kv amy_last_report`: the stored report carried `cleanup: undefined` forever, and a cleanup with no persisted proof is indistinguishable from no cleanup (prod 2026-08-02T04:50Z: 55 `created_by='agent:amy'` rows live out of 92 profiles total). (4) A falling gap count could not be told from **narrower probing**. | **Probe space** (`backend/services/amy/probeSpace.js`): a 4-axis space — entity x identity x need x geography — whose values are READ OUT OF THE CANONICAL REGISTRIES (`PROFILE_TYPES` 50, `HEALTH_DIAGNOSIS_FLAGS` + `STAGE_KNOWN` + the `population` group of `CANONICAL_NEED_CATEGORIES` = 26 identities, the `need`-role groups = 21 needs, `STATE_REGISTRY` = 51 states with one real city/county/ZIP each). Two classifications are DECLARED and TOTALITY-TESTED: `ROOT_CLASS` (individual/org, applied through `entityRoot`) and `NEED_APPLICABILITY` + `NEED_GROUP_ROLE`. **TRAP: `parentTypes` is NOT ordered furthest-last** — `medical_need` declares `['individual','family']` — so taking the LAST element returns a mid-chain type and silently drops six of fifty entities out of the axis; walk `parentTypes[0]` transitively. **Selection** (`gapSeekingPlanner.js`): pair-driven greedy over 2-wise coverage. UNCOVERED := reachablePairs \ ledger.pairs; seeds ordered by gap PRESSURE then a SEEDED SHUFFLE (a key-ascending tie-break made every night's 18 probes `animal_rescue` — breadth must not depend on the alphabet); each remaining axis filled by `W_NEW*newPairs + W_GAP*pressure + W_STALE*staleness`, explore dominating exploit by construction. Deterministic per `(ledger, runId)`. The pair denominator is **9,134** (2026-08-02) against ~1.98M plausible cells, so coverage is a finite reportable horizon. The catalog stays as a HARD FLOOR of one per category (`resolveCohortSplit`, `AMY_ADVERSARIAL_SHARE` 0.4) and **the catalog absorbs any slack the probe lane does not build** — sizing it from the RESERVED share made a 12-profile target produce 7. **Actors** (`findingActorRegistry.js`): a TOTAL map `FINDING_TYPES` -> `{lever, emitted, evidence_key}`, every lever resolved through the same `LEVER_REGISTRY` the approval ledger uses, plus `LEVER_SURFACE` and `AUTONOMY_FORBIDDEN` (matching_logic / eligibility / scoring_math / security). `assertAutonomyBoundary()` throws if a forbidden-surface lever is ever declared AUTO. `buildApprovalQueue` gained a registry-driven TOTALITY PASS that emits an item for every EMITTED class no earlier branch covered (deduped via `itemFindingType`, which also matches an id prefix so a parallel branch cannot double-report). **AMY APPLIES ONLY**: `source_keyword_coverage`, `scoring_weights`, `score_floor`, `archetype_query_learning`, `url_hygiene` — all bounded, re-crawl validated, auto-reverting, env-gated. **AMY NEVER APPLIES** eligibility, geo/relevance policy, adapters, field mapping: those become a `code_brief` (file, line, failing profiles, the assertion that would prove the fix) and the artifact states `patch_authored_by_amy: false`. **Amy does NOT open the PR** — `anyaCodeFixDispatch` requires a validated unified diff, and synthesizing one for a semantic gate would be an unreviewed guess at exactly the surfaces the boundary forbids. **Deletion proof** (`amyDeletionProof.js`): cleanup MOVED before `saveAmyReport`; `verifyAmyDeletion` counts `created_by='agent:amy'` rows before/after and scans for rows past `expires_at`, verdict `proven`/`grace_held`/`leaked`/`unknown` — an unreadable count is `unknown`, never a comfortable `proven`. `grace_held` (2026-08-03): a past-TTL row the sweep's OWN telemetry shows it is deliberately holding (`crawled_too_recently` 6h grace / bounded never-crawled window) is classified by `classifySurvivorHold` and reported non-alarming — but a survivor with drifted markers, a stale crawl signal, or age past `AMY_NEVER_CRAWLED_MAX_AGE_HOURS` (perpetual grace-riding = starvation by re-discovery) stays `leaked`; an unclassified survivor fails TOWARD the alarm. No cleanup guard was weakened. It NEVER deletes; deletion stays with the one guarded sweep. **Convergence** (`gapConvergence.js`): CONVERGING requires gaps-per-profile DOWN **and** probe breadth UP; gaps down on flat breadth is named **NARROWED** out loud, and an absent coverage ledger is NARROWED (absence is not breadth). Classes no lever can close are named with file + human action. Only ADVERSARIAL probes fold into breadth — back-deriving a cell for a catalog scenario would credit breadth nobody selected. | `backend/tests/amyProbeSpace.test.js` (20 — registry totality both directions, the transitive-root regression, 51 localities) + `backend/tests/amyFindingActorTotality.test.js` (33 — every class has an actor, the `emitted` flag checked against `amyReport.js`'s SOURCE, mutation-verified autonomy boundary) + `backend/tests/amyGapSeeking.test.js` (27) + `backend/tests/amyGapLoopEndToEnd.test.js` (5 — the PERSISTED report carries the proof). **Mutation-verified: removing the totality pass reds 18; removing the seeded shuffle reds 1.** |
| TWO PRs THAT ARE EACH GREEN CAN STILL BREAK `main`, and a registry-driven test is where it shows (2026-08-02). #1097 declared `institution_recall_miss`/`hyperlocal_recall_miss` in `findingActorRegistry.js` with `lever:'archetype_query_learning'` (→ AUTO); #1095 independently added a `buildApprovalQueue` branch in `crawlerTuner.js` emitting the SAME classes with `lever:'query_breadth'` (→ CODE_CHANGE). Both PRs passed every check at their own head SHA — the contradiction exists only in the MERGE, and `amyFindingActorTotality.test.js` asserts `item.lever === FINDING_ACTORS[type].lever`, so **`main` went RED at `6824e745` with 4 of 33 failing while both PRs' badges stayed green**. Reproduced in a pristine detached worktree at `origin/main` with neither branch present: identical `4 failed / 29 passed`. **The merge order was the trigger and it is not the authors' fault** — a green PR check is a claim about `PR ∪ main-at-that-moment`, never about `PR_a ∪ PR_b`, and nothing re-runs the first PR's suite after the second lands | **RESOLUTION — the runtime was right and the registry was stale.** The registry's OWN header measures `institution_recall_miss` at **21 of 21 days, 282 occurrences, never green**: a lever declared AUTO that has been applied every night for three weeks and closed nothing is the green-while-doing-nothing anti-pattern, and `approvalLedger.js` had already written the same conclusion for `query_breadth` ("a class that keeps firing after the steering store has been recording it for weeks is evidence the QUERY BUILDER needs the change"). Both recall classes are now `query_breadth`/CODE_CHANGE; Amy still applies the data-side steering store, but the residual reaches the owner as a code brief instead of being silently re-applied forever. **The reconciliation exposed a SECOND defect the test failure was hiding**: #1095's branch wrote `evidence.missed_subjects` while `buildCodeBrief` and the registry-driven totality pass both read `evidence.subjects` — nothing read the key, so the moment these classes started producing briefs the owner would have received "some student profiles missed something" with **no school ever named**. Canonical key is `subjects`. **THE RULE: when two PRs touch the same registry, the second to merge must re-run the FIRST's suite, not only its own** — and a `lever` (or any declared classification) that appears in BOTH a registry and a runtime branch must be asserted equal by a totality test, which is the only reason this surfaced in minutes rather than in a morning email | `backend/tests/amyFindingActorTotality.test.js` (34 — the `it.each` lever-agreement assertion is the tripwire; new "a reclassified recall miss DOES carry a brief, and the brief names the schools" pins both halves) + `backend/tests/amyPersonaArchetypes.test.js` (canonical key). **Mutation-verified: reverting the registry lever reddens 1; reverting the evidence key reddens 4.** |
| **Local crisis help that ALREADY EXISTS reaches the household in that COUNTY** (2026-08-02, the other half of the persona failure: #1095 adds the missing source LANES, this net surfaces the rows the catalog already holds). Measured read-only in prod — ACTIVE catalog rows → rows carrying ANY match row: **eviction/rental 416→16, homelessness 282→13, utilities 252→22, food 176→9, foreclosure/mortgage 95→30** at 04:59Z, and **415→18 / 282→14 / 253→23 / 175→9 / 95→30** re-measured at 05:49Z after #1094 landed — the store moves hour to hour, so quote a timestamp or the number is a story. **An honest smaller number:** of the 416 eviction rows **159 are CA and 37 NM**, where no fleet profile lives, so those 196 are CORRECTLY unmatched; 37 more are pointer kinds. The genuinely reachable remainder for today's fleet is **31 pairs**, which is exactly what this links — not 416. The concrete pair: Hollie Machelle Knox (family, North Ridgeville OH **44039 = Lorain County**, declared need `housing`) vs "Love INC Lorain County – Emergency Housing & Rent Assistance" — the REAL `computeMatchDecision` returns **ACCEPT 100** and `profile_opportunity_matches` held **no row at all**. **NEVER SCORED, not scored-and-rejected.** Seven more of her county's awardable rows sat in the same state (HEAP 69, CHIP 62, Catholic Charities Housing Services 50, county Children & Families Mini Grant 55); what DID reach her were three findhelp/USA.gov/HUD **DIRECTORY pointers**. Her profile states **no state and no county at all** — only a city and a ZIP — so every state-keyed gate in the product misses her by construction. **ROOT CAUSE:** `crawlerOsPersistenceCore.persistRun` (the rolling snapshot) DELETEs a profile's `crawler-os`/`crawler-os-xmatch` rows every run and re-inserts only what THAT run re-found; her three surviving matches are all `crawler-os`, every row above was found on a different day. Do NOT "fix" that by making the reconcile keep rows — the snapshot protects the matches view by design; the fix is a linkage gate under its OWN matcher_version | Canonical facts: `backend/config/crisisNeedRecall.js` — `CRISIS_NEED_IDS` is the **`basic_needs` group of `CANONICAL_NEED_CATEGORIES`** (read from the registry, never hand-typed); `resolveProfileCountyAnchor` (declared county+state, else `resolveZipLocation` — the SAME resolver `profileHelpers`/`profileTaxonomy`/`nationalZipCrawler` use); `countyPhrasesFor` + `COUNTY_SUFFIXES_BY_STATE` (LA parish / AK borough / PR municipio); `rowNamesProfileCounty` (token-boundary `titleStatesTerm` over title+sponsor ONLY — never description prose, #1086). Boot net: `enforceCountyCrisisNeedRecall()`, wired right after `enforceStudentAidInStateRecall` so the rows it adds are held to the scope/hygiene bars the SAME boot. **THE KEY IS A REAL PLACE, NOT A STATE** — measured with the real engine on real prod pairs: bare in-state **11,628 candidate rows**; county TOKEN + state 369 scanned; county PHRASE + state 280; **+ the row serves a need the profile DECLARES → 100 scanned, 61 linked, 39 engine-rejected, 337 adjudicated out, 165 need-miss, 23 profiles eligible** (dry run of the SHIPPED code against prod, count-only, read-only transaction, 2026-08-02T07:17Z, rebased onto #1099; **33 links land on 10 real profiles and 28 on 7 ephemeral Amy synthetics**, which the nightly reaper deletes — the other recall nets do not exclude them either). #1094's 194 junk-row purge freed NO candidate for this gate: those were `fabricated_place` DIRECTORY rows on unconfigured profiles, and this gate refuses both. Bare in-state was measured at 5,393 / 6,210 / 218 links in three earlier attempts and rejected every time. **CROSS-STATE LEAKAGE IS IMPOSSIBLE THROUGH THREE INDEPENDENT CONDITIONS** (the Raleigh County WV → Raleigh NC class): the phrase is `"<County> County"` and a City-of-Raleigh grant never writes it; `fo.state` must equal the profile's state; and a title declaring its own state in the canonical `"<Place>, XX — "` shape must declare THIS state (`declaredStateFromTitle`) — the last is what stops `"Polk County, TN — …"` reaching a Polk County **GA** household when a crawl mis-stamped the column. **THE NEED CONJUNCT IS A DECLARATION, NOT AN INFERENCE — and BOTH obvious sources fail in the #1095 veteran way (measured 2026-08-02).** `normalizeProfile().needCategories` returns the TYPE-SHAPED FALLBACK `['cash_assistance','housing','food']` for a `family` profile with COMPLETELY EMPTY sections (#1094 named it `needsDefaulted`: "we could not read it" ≠ "there is nothing"), so every household in the fleet would clear the conjunct while stating nothing; and `buildProfileSignals().needs` reads FREE TEXT, so the narrative *"We do not need housing assistance or rent help of any kind"* mints `housing` **from its own denial** — the identical shape to #1095's veteran gate matching "veteran" inside a denial. So the profile side is `declaredCrisisNeeds()` over the `DECLARED_NEED_FIELDS` registry: STRUCTURED fields only (`profiles.needs`/`need_categories`, section `needs`/`need_categories`/`primary_needs`/`support_needs`/`funding_needs` ARRAYS, a section KEY that IS a canonical need, and `HOUSING_INSTABILITY_FLAGS` read strictly `=== true` — `housing_situation` is deliberately absent because it is prose). Every value resolves through the canonical `normalizeNeedCategory`. **THE COST, stated plainly: this DROPS the flagship Hollie Knox pair from the FIX.** Her housing need exists only in `financial_information.notes` ("Seeking $5,000 for housing costs…") and `narrative.funding_amount_needed` — free text. The diagnosis is untouched (the engine still replays ACCEPT 100 on a pair nothing ever scored); what is gone is the ability to reach her structurally. **OWNER ITEM — BIGGER THAN THIS ROW: the profile has NO structured "what do you need help with" field, so ANYONE who TYPES their need in prose is invisible to EVERY structured gate in the product.** The same class at full scale: **2,606 active catalog rows curated on 2026-04-09 carry TEN match rows between them (0.38%), four months later** — incl. four `curated_verified` license-reinstatement rows at 0, and the fleet's only veteran at 137 matches / 0 veteran rows. The engine is willing every time; the pair is never put in front of it. A general re-scoring sweep for the rolling snapshot is STILL OPEN WORK. Do NOT "fix" that by reading the prose — that is the denial class, three times shipped. **PRECISION, NOT VOLUME: only ACCEPT is written.** REVIEW is the locator/recommendation band and `isPointerKind` rows are refused before the engine is even called — a person facing eviction who receives an Eldercare Locator is worse served than one who receives nothing. **TRAP: a ZIP-derived county is only as good as the offline dataset, which is measurably WRONG for some ZIPs** (`37311` "Cleveland TN" resolves to Hamilton; the real county is Bradley), so a ZIP anchor must be CORROBORATED — the resolver's city must equal the profile's own declared city, and a ZIP with no declared city buys nothing. The anchor also consumes #1094's two new registries: `isFabricatedGeoSource` (a placeholder address is positive junk evidence — `{city:'Anytown', state:'USA', zip:'12345'}` resolves to a real, plausible **Schenectady NY** the applicant has no connection to) and `isRegionCode` (two letters is a SHAPE, `REGION_CODES` is the AUTHORITY — a bare `/^[A-Z]{2}$/` accepted the `"SA"` minted from `"USA"`). **`county-crisis-need-link` is registered in `SURFACED_MATCHER_VERSIONS`** — persisting under a reconcile-surviving version and then not READING it back is the web-llm regression exactly. Bound `COUNTY_CRISIS_LINK_LIMIT` (500); `ENFORCE_COUNTY_CRISIS_RECALL=0` for count-only | `backend/tests/crisisNeedRecall.test.js` (38 tests: registry totality, the anchor + its three refusals, the Raleigh/Polk cross-state guards, phrase-not-token, identity-fields-not-prose, ACCEPT-only, pointer-refused-before-the-engine, need-conjunct, idempotence, convergence, count-only, reconcile-survival; **mutation-verified — all 14 mutations KILLED**, incl. dropping the declared-state refusal, the city corroboration, the phrase rule, the crisis-need conjunct, ACCEPT-only, the pointer exclusion, the convergence purge, the fabricated-geo refusal, reading the rendered section blob instead of the structured registry, and accepting a TRUTHY instead of `=== true` housing flag. **TRAP in the harness itself: the worktree checks out CRLF, so `\n`-terminated anchors silently DID NOT APPLY and seven mutations reported nothing at all** — a mutation run must print a per-mutation applied/killed verdict, or it is a check that cannot fail) + `backend/tests/matchSurfacing.test.js` + `backend/tests/enforceInvariants.test.js` (boot totality **48 → 49**, summed with #1094) |
| A confidence the engine never measured is published as UNKNOWN, not as ZERO (2026-08-01; persistence closed 2026-08-05). `resultEnricher` guarded with `Number.isFinite(Number(decision?.confidence))` — but `Number(null)` is **0** and `Number.isFinite(0)` is **true**, so an absent confidence was published as a hard `0`, and `FundingResultCard.jsx` rendered "· 0% conf" (a null renders NO chip at all). The 2026-08-01 production audit found `profile_opportunity_matches.match_confidence` NULL on every row because crawler-os dropped the canonical engine's measured confidence before persistence; the matching routes also did not SELECT it | `isFiniteNumberLike()` (`services/matching/resultEnricher.js`) remains the coercion choke point and rejects `null`/`undefined`/`''`/`[]`/`{}`/booleans BEFORE `Number()`, while accepting a real `0`. `crawler-os/matchEngine.js` now carries the canonical confidence into the profile-scoped memory row; `crawler-os/storage.js`, both `crawlerOsPersistenceCore.persistRun` write paths, and the resource/ACCEPT durability snapshot+restore preserve it; `fundingSourceQueries.js`, `/api/matching`, and both discovery query paths SELECT and publish it. Historical rows and surfaced lanes that do not compute confidence remain honestly NULL; only a newly measured crawler-os value produces a chip | `backend/tests/matchConfidenceHonesty.test.js` + `backend/crawler-os/tests/storage.test.mjs` + `backend/tests/crawlerOsResourceReconciliation.test.js` + `backend/tests/fundingSourceQueries.test.js` |
| A profile's ITEM LIST is DERIVED from what the profile declared, and an item search actually searches for the ITEM (owner rule 2026-08-02: "build an item list for each profile of items the profile might need… have the item search crawler tooled to find those items if the profile owner or admin requires a crawl… a free-text field that the owner or admin can type in an item they feel they need"). **Four defects, measured read-only in prod 2026-08-02, not inferred.** (1) **THE ITEM WAS THROWN AWAY.** `DataSources.jsx` renders an "Item Funding" tool with a REQUIRED free-text box, `src/api/crawlers.js` sends it as `item_request`, and `POST /api/real-crawlers/run` destructured only `{crawler_type, profile_id, min_match_score}`. `crawlerType` then reached exactly ONE consumer — `persistSourceCoverage({crawlerType})`, a telemetry label — so typing "PROBE ethics class" ran a generic profile crawl and reported its count as the item's answer. (2) **THE ITEM CRAWLERS ARE UNREACHABLE.** `services/crawlers/itemFundingCrawler.js` (1,148 lines, holding real NCSBN PROBE / nurse-reentry / voc-rehab sources under `license_reinstatement`) is imported by nothing: its only product caller, `routes/admin.js`, imports `crawlItemFunding` from `crawlerOsCompatibility.js`, whose entire body is `return {...SUPERSEDED, items: []}` — and the real module is on `check-runtime-imports`'s legacy list, so it can never be wired back. `services/itemCrawler.js` (326 L), `services/itemGiftCrawler.js` (213 L) and `startup/backgroundServices.js` have ZERO runtime importers. (3) **THE LIST WAS A CHECKLIST.** `itemCatalogService.suggestItemsForProfile` scores a NINE-ROW fixture (laptop/desktop/hotspot/wheelchair van/wheelchair/hearing aids/glasses/school supplies/work clothing) against coarse buckets, then asks an LLM for the rest — so Focus Forward Ministry, whose declared `programs_services.focus_areas` literally contain "Building supplies", was offered hearing aids. (4) **THE TAXONOMY PREFERRED ITS OWN IDENTIFIERS TO THE USER'S WORDS.** `expandNeed` returned on the first KEY hit, so "PROBE Ethics class for nursing licensure" matched the 9-char snake_case key `licensure` (7 generic exam synonyms) and NEVER reached the 18-char SYNONYM `probe ethics class`, whose entry carries `license_reinstatement_support` + `professional_remediation_funding`; and `EXPAND_NEED_STOPWORDS` was applied only in the fallback branch, so `for`/`the`/`help`/`funding` became must-terms worth 5 pts each. Two of those admitted the Merriam-Webster and Cambridge Dictionary definitions of "probe" as funding results | **Derivation**: `backend/config/profileItemNeeds.js` — `ITEM_NEED_RULES`, a registry where every rule names ONE declared field and every emitted need carries that field id as `evidence` (the `profileDerivedFacts` shape). **Applicability is read from `PROFILE_SCHEMA[section].applies_to`, never a hand-typed person/org flag** — Focus Forward carries imported `housing` and `health_medical` sections (both `ALL_PERSON_TYPES`), and the first draft derived a household internet plan for a ministry off that artifact. PROSE IS NOT MINED (`narrative.primary_goal` says "Deliver building supplies…" and is `scored:false` drafting-only; the same fact arrives through the `focus_areas` TAG the owner chose). A declared value naming no purchasable thing is a CONVERGENT `unmapped` gap carrying its one-line fix — never a guess, never silence — except for mission tags, where a miss is silent by design (asking an owner to add "Discipleship" to an item vocabulary is the unfillable `lodging`-in-the-disease-lane class). **Free-text field**: `financial_information.item_needs` (`string_array`), declared in BOTH `PROFILE_SCHEMA` and `sectionMetadata`, applies to every profile type, and **deliberately `scored: false`** — `isFieldScored` defaults every schema field to SCORED and `match_score` is matched points ÷ TOTAL points, so a scored field lands in all 37 real profiles' DENOMINATOR on deploy and shifts rows across the freshly-recalibrated bands (#1067); it is also the wrong KIND of fact (what the applicant WANTS, not what the applicant IS). Do NOT give it a `format` in `PROFILE_SCHEMA` — that vocabulary is the narrow matcher-facing one and `string_array` fails `profileSchemaContract`. **Search**: `backend/services/itemNeedSearch.js`, routes `GET /api/item-needs/:profileId` + `POST /api/item-needs/:profileId/search` (`ensureProfileAccess` owner-or-admin + `TIER_CAPABILITIES.ITEM_FUNDING`), and `crawler_type:'item_matching'` on `/run` now honors `item_request` or 400s instead of silently answering with something else. TWO LANES: CATALOG scans **the whole active catalog** by item phrase (a SQL PREDICATE ahead of the LIMIT — #944) and adjudicates each row for this profile with the canonical `computeMatchDecision` when no stored verdict exists (`matcher_version:'live-item-search'`, REJECT dropped) — the `recheckHamiltonPolicyStops` fallback, because **the match store is a rolling snapshot and an entire day's curated catalog never reached anyone**. THE UNREBUTTABLE NUMBER IS `curated_verified`: of **507 hand-verified real funding sources created 2026-04-09, 6-7 have EVER been matched to any profile (1.2-1.4%)** — measured 2026-08-02, 6 at 07:0xZ and 7 at 07:52Z, the drift being the rolling snapshot itself. DO NOT quote the day's headline 2,606 rows / 10-11 matched: **1,792 of those are `directory_resource`**, and a low match rate there is partly BY DESIGN — this file's own locator rule says a directory is a pointer, never an award, and `isPointerKind` excludes them from the awardable count. Naming the directory share yourself is what makes the finding survive its own strongest counterargument; a bigger number that a reviewer can rebut in one line is worth less than a smaller one nobody can. The license-reinstatement slice is the same story at human scale: I measured 6 such rows at zero matches, a second operator re-ran it with a NARROWER LIKE pattern, reached 3 of my 6, and found all 3 at zero (two `verified_real`) — so the claim is "3 independently confirmed of 6 measured", never a flat 6. Same shape as the fleet's only veteran carrying 137 matches and 0 veteran rows: the engine is willing, the pair is never put in front of it); WEB is display-only leads. **Precision is TWO conditions, both required** (the `enforceLeadContactPlausibility` shape): a row must state a PHRASE — a multi-word taxonomy synonym, the matched key, or a stopword-free BIGRAM of the request, token-bounded and directional so "ethics probe" does not satisfy "probe ethics" — AND a web lead must state FUNDING INTENT (`FUNDING_INTENT_TERMS`, a new single registry: no funding-intent vocabulary existed; `genericTitleVocabulary` enumerates the opposite). Catalog rows are EXEMPT from the intent gate — the real ANA row states no money word and the engine still ACCEPTs it; a stored row passed the reality gate, a SERP snippet passed nothing. **Awardable vs pointer from `isPointerKind`; a NULL kind is UNCLASSIFIED, never "awardable"** (the `Number(null)===0` class). **NO PADDING** — `itemFundingCrawler`'s "ZERO-RESULT SAFETY NET" appended Candid/GrantWatch/Grants.gov whenever a search found nothing; `found: 0` is now returned as `found: 0`. Nothing in this lane WRITES. **MEASURED: 37 real prod profiles → 22 derived needs total, mean 0.59, max 6, 27 profiles get ZERO (no checklist); the PROBE search went 10 results (NASA "Probe-Class Mission", CBC "ethics probe", LinkedIn "Probe Group", 2 dictionaries) → 7, all on topic** | `backend/tests/profileItemNeeds.test.js` (22 tests; the applicability gate reddens 3 under mutation, dropping `scored:false` reddens 1) + `backend/tests/itemNeedSearch.test.js` (25 tests built from the REAL prod SERP rows; reverting the taxonomy fix reddens 6, deleting the two web-lane gates reddens the end-to-end test, un-dropping the engine REJECT reddens 1, reverting the Postgres BOOLEAN predicate reddens 1) |
| A surfaced award is one the profile's ACADEMIC STAGE can actually receive — and the state aid a student IS eligible for reaches them (2026-08-02). Measured read-only on Anastasia White (`c4a92724-…`, a dual-enrolled HIGH-SCHOOL SENIOR in TN): the catalog holds **21 active TN HOPE rows and she matched ZERO**, while the recall key that finally reaches HOPE (in-state + student-aid title + the aid taxonomy) also handed her "Vanderbilt School of Medicine Merit Scholarship" (ACCEPT **84**), "Osher Reentry Scholarship" (84, returning adults), "International Student Doctor of Chiropractic Scholarship" (81) and "UAB Blazer Graduate Research Fellowship" — **and the engine ACCEPTed every one**. #1091 measured this key and correctly refused to ship it: the missing piece was never a recall key, it was that the engine had **no notion that a high-school senior cannot receive a graduate/professional or adult-reentry award** | **The gate**: `backend/config/stageOfLifeEligibility.js` — `STAGE_REQUIREMENT_CLASSES` is the registry (`graduate_or_professional` / `postdoctoral` / `adult_reentry`), consumed by `matchEngine.makeDecision` (the one choke point) and by the boot net `enforceStageOfLifeMatchScope()`. The profile side is NOT re-derived: `deriveStageOfLife` (`profileDerivedFacts.js`, #1091) is the sole stage taxonomy and `STAGE_KNOWN` is totality-tested against it. **It refuses; it never asserts** — `stageOfLifeConflict` returns a conflict or null, and null means "this gate has nothing to say", never "eligible". **SILENCE IS NEVER A DENIAL** (prod: `eligibility_text` is EMPTY and `eligibility_bullets` `[]` on 100% of the motivating rows — the only stage statement lives in title/sponsor/summary, so a gate reading absence as a bar would reject the catalog). Only PROVABLE impossibility is barred: an undergraduate is deliberately NOT barred from a graduate award (a graduating senior applying to grad school is real) nor from reentry (a returning adult IS an undergraduate); only `postdoctoral` bars undergraduates. Four precision rules, each MEASURED against prod: (1) **fragments are matched SEPARATELY**, never joined (#1086); (2) an **inclusionGuard** — Federal Work-Study's page says "part-time jobs for UNDERGRADUATE AND GRADUATE students", and without it the gate refused a 100-scoring correct award on the word that INCLUDED her (same for NSF's "REU Site: Undergraduate Research… graduate research ambassadors"); (3) **negation on EITHER side** ("graduate students are not eligible" reverses the phrase); (4) **identityPatterns** — `school of medicine` counts only in title/sponsor, because in an NIH abstract it is the AWARDEE ("Awardee: JOHNS HOPKINS UNIVERSITY SCHOOL OF MEDICINE"). Bare `reentry`/`nontraditional` are REFUSED as declarations: in this catalog they are the criminal-justice sense ("Reentry Employment Opportunities Program") and a wildlife term ("HCP Land Acquisition (Nontraditional Section 6)") — reaching the right answer through a false statement is worse than no gate. **Blast radius, prod 2026-08-02: 3 of 2,750 live match rows refused; 118 of 15,352 active catalog rows declare a stage at all.** **The unblock**: `enforceStudentAidInStateRecall()` (matcher_version `student-aid-instate-link`, in `SURFACED_MATCHER_VERSIONS`) — the key #1091 could not ship, now safe because the engine rejects the stage-barred rows. **THE ANCHOR IS A NAMED STATE, NOT A STATE COLUMN**: measured on real prod pairs with the real engine, bare `fo.state = profile state` links **218**; adding "title or sponsor NAMES the state" links **86**. The 132 it drops are the flood — `funding_opportunities.state` is stamped by whichever profile's crawl found the row, so a TN student's "in-state" set carried seven **Cuyahoga Community College (Ohio)** awards and five from **Cleveland University-KANSAS CITY**. A row whose own title/sponsor says "Tennessee" makes a claim about ITSELF; a state column makes a claim about a CRAWL. `grant` is excluded from the accepted aid classes (208 → 423 unanchored). Boot totality **45 → 47** (44 → 46 before the rebase onto #1092); `ENFORCE_STAGE_OF_LIFE_SCOPE` / `ENFORCE_STUDENT_AID_INSTATE_LINK` = 0 for count-only; bounds `MATCH_SCOPE_PURGE_LIMIT` / `STUDENT_AID_INSTATE_LINK_LIMIT` (500). **Verified on a SQLite replica of all 33 real prod profiles + 15,367 active catalog rows: `linked 87, rejectedByEngine 17, adjudicatedOut 470`; stage scope `repaired 3`; Anastasia's HOPE-titled matches 0 → 1; second pass `repaired 0, stale 0`.** **THE MOTIVATING PROFILE CONTRADICTS ITSELF AND NOTHING RECONCILES IT** (reported, deliberately NOT "fixed"): `demographics.disability_status = "Has disability"` vs medical sections stating no confirmed conditions; `government_assistance.ssdi_recipient_self = true` vs `other_programs = "SSDI (dependent child of SSDI recipient)"` — and a dependent child of an SSDI recipient is NOT an SSDI recipient, which are different programs. **Neither gate here reads either field**: the stage gate reads only `basic_information.academic_status.education_level` / `education.highest_level` / `college_courses`, and the condition gate reads only the health-vocabulary sets — so whichever side is true, neither can wrongly admit or exclude her. The `enforceProfileIncomeReconciliation` precedent applies to the contradiction itself: an ambiguous conflict with no clear signal is LOGGED for human review, never silently resolved. KNOWN GAP, reported not faked: "Tennessee Reconnect" / "TCAT Reconnect" still reach her — their pages say "adults 23 and older" / "FAFSA status of independent", neither of which is one of the three declared classes | `backend/tests/stageOfLifeEligibility.test.js` (37 tests: registry + LIKE totality, the real BAR/KEEP prod rows, silence/unknown-stage neutrality, both boot nets incl. count-only + idempotence + convergence; **mutation-verified 5 ways with disjoint failure sets** — neutering the gate reddens 13, removing the inclusionGuard 2, removing negation 1, removing identity-only 1, broadening the reentry patterns 2) |
| LANE ORDER is a claim about the PROFILE, not a global constant (2026-08-02). `crawler-os/planner.js` ordered selected sources by `priority_score` alone — a number curated ONCE per source with no knowledge of any profile. Measured for Anastasia: 54 of 167 sources selected and **the first 22 were disease/disability lanes** (kidney fund, autism, arthritis, HIV care, amputee, rare disease), admitted by ONE generic `disability` need derived from a household SSDI entry — her `medical.disabilities` is literally `[]` — while `pell_grant`/`fseog`/`studentaid_gov` ranked **23rd, 24th, 25th** | Three tiers read from `thesis.derived_facts` (#1091 carried it to the planner for exactly this): `serves_declared_topic` (the source's CURATED `keywords[]` name a topic the profile declared) → `serves_declared_stage` (the derived stage makes it a student AND the source serves `student` applicants AND declares a `STUDENT_AID_NEED_CATEGORIES` need) → default `priority_score`. The tier is RECORDED on `source_decisions.lane_tier` so the crawl plan explains itself. The RANKING promotes and never suppresses — with the tiers alone the selected set is identical across all 33 real prod profiles. Anastasia's 9 student-aid lanes now hold ranks 1–9 (`pell_grant` 23 → 1); it is not anti-disease — Gilbert McCosh, a real disability profile, has his 4 condition-matched lanes rise to the top (autism_speaks 10 → 1). **`need_categories` are deliberately NOT in the topic vocabulary**: with them in, the tier fired 240× across 33 prod profiles and its first hits were `benefits_gov`/`orr_refugee` "serving" a forensic-science student, because her declared interest "forensic science education programs" shares the token `education` with a coarse category — the #937 one-shared-word floor, one level up. **WHY RANKING AND NOT A FIELD-OF-STUDY SELECTOR** (the option #1091 named): sources carrying ANY curated topical vocabulary are **32 of 167** and they are condition lanes — **ZERO serve forensic science or criminal justice**. A selector keyed on field of study would have had nothing to select for the profile that motivated it, and tagging 167 rows with disciplines they do not serve would be inventing claims about sources. **A CONDITION LANE MUST BE ASKED FOR A CONDITION** (the selection half, 2026-08-02). `servesNeed` admits a source on a coarse `disability`/`medical` token, and a `disease_specific` source's whole identity is a specific diagnosis — so prod carried **438 disease-lane selections across 33 profiles, 19 apiece even for profiles whose declared health vocabulary is EMPTY** (Robert White, Admin Vault, Tasha Reynolds). The motivating case is sharper than "a spurious signal": Anastasia's disability is REAL (`demographics.disability_status = "Has disability"`, `government_assistance.ssdi_recipient_self = true`) — but her own medical sections say *"no chronic illnesses or disabilities noted"*, so **it has no named condition**, and an unnamed disability is not evidence for every named condition in the registry. `servesDeclaredCondition` (planner) consults `config/sourceLanes.sourceServesDeclaredCondition`, keyed on the profile's `declared_health_terms` (the union of `signals.health_conditions` and `health_support`, carried onto the thesis — the SUPPORT half matters: Dr. John Robert White carries `arthritis` there, not in conditions). **Two narrowings, both measured**: the vocabulary is the source's CURATED `keywords[]` and NEVER `need_categories` (their `disability` category sits whole inside "cognitive disability (f70)", which would hand Gilbert the kidney fund and the amputee coalition — #937 one level up), and `GENERIC_HEALTH_DESCRIPTORS` refuses category-of-person words on the PROFILE side (bare `disability` sits inside autism's curated `developmental disability` and Reeve's `physical disability`, which is how the reverse-direction match handed her both). **MISSING = NEUTRAL**: an ABSENT `declared_health_terms` (a hand-built or cross-profile thesis) never gates; an EMPTY ARRAY is a read that found nothing and does. `condition_not_declared` is in `BY_DESIGN_REASONS` so it can never mint an unfixable nightly lane gap. Measured fleet-wide: **1,615 → 1,210 source selections, disease lanes 438 → 33, and ZERO non-disease lanes lost**; Anastasia 54 → 35 with her GENERAL disability/benefit lanes intact (`tn_ecf_choices`, `ssa_disability`, `state_hcbs_waivers`, `tn_benefits`) and her caregiver lane too; Gilbert keeps 5 and Kimberly 4, and both golden-outcome profiles keep all 6 required sources. **HONEST LIMIT on the ORDERING half: nothing truncates the plan today** — `pipeline.js` runs every selected source — so the tiers change crawl ORDER and what the plan explainer shows, not which lanes execute; the condition gate is what changes which lanes execute | `backend/crawler-os/tests/laneSelectionRanking.test.mjs` (9 tests; mutation-verified twice with disjoint sets — dropping the tier from the sort reddens 2, putting `need_categories` back into the topic vocabulary reddens 1) |
| A profile that was NEVER FILLED IN is reported as UNCONFIGURED, and is never served invented geography (2026-08-02). `profile-melissa-justus` is not a person: `address = {street:'123 Main St', city:'Anytown', state:'USA', zip_code:'12345'}`, `email @example.com`, `phone 555-1234`, own notes "Designated roster profile. Add the owner login email here…", and NO education/health/financial/occupation/needs content. It nonetheless carried **27** surfaced matches (ALL `DIRECTORY`) whose geography the system INVENTED — `Anytown, SA`, plus Anchorage County AK / Wayne County MI / Dona Ana County NM / Escambia County FL — and sat in the result floor's below-target set, which would drive endless backfill for a profile that can never be satisfied. **ROOT CAUSE, exact line: `backend/utils/inferLocationFromAddress.js:14`** — `/,?\s*([A-Za-z]{2})\s+(\d{5})…$/` has NO left word boundary, so the address blob "…Anytown USA 12345" matched the LAST TWO LETTERS of "USA" → state `"SA"`. Nothing downstream caught it: the ZIP→state rescue only fires when the state is EMPTY, `normalizeState('SA')` returns null so `signals.states` was `[]` and every geo gate fell open ("MISSING = NEUTRAL"), and `countyCityDirectoryAdapter.placeLabel` concatenated the junk verbatim into a real catalog title. **TRAP — fixing it unmasked a WORSE fabrication:** with the state now null the ZIP rescue became reachable and resolved the placeholder ZIP `12345` to **Schenectady, NY** — real, plausible, and completely wrong; "Anytown, SA" at least LOOKED wrong | Canonical detector `backend/config/placeholderProfileSignals.js` (`detectUnconfiguredProfile`), served to every consumer through `backend/services/profile/profileConfiguration.js`. **THE RULE: UNCONFIGURED iff (A) NONE of the 15 `SUBSTANCE_PROBES` fires — mandatory — AND (B) ≥ `MIN_CORROBORATING_FAMILIES` (2) of `identity`/`address`/`self_declared` contribute a signal.** Three families, one mandatory; no single signal ever decides (the #937/#943/Yana one-token class). **A sparse-but-REAL profile survives because ONE declared fact clears (A)**: prod's `Angelika Ptak` carries the SAME `location_focus.notes = 'Synthetic location signal…'` as both placeholders and is NOT one — her `signals.occupation` is `['healthcare_worker']` where all three placeholders' is `[]`. **`signals.needs` is NOT a substance probe** — `buildProfileSignals` injects a type-shaped fallback so it is NEVER empty (John Doe's entire "need list" was the fallback; new provenance flag `needsDefaulted`), and on Melissa its only entries came from `organization_details` booleans on an `individual` profile — the Kimberly-Botts hallucination class. Geo fixes: registry `shared/usStateCodes.js` (`isRegionCode`) consumed by BOTH `inferLocationFromAddress` twins (backend + `src/`) with word boundaries added; `isFabricatedGeoSource()` (≥2 of placeholder-city / present-but-unresolvable-state / placeholder-ZIP) gates the ZIP→state rescue, the county resolve, and the findhelp `{zip}` deep link — a REAL person at ZIP 12345 (an assigned GE ZIP) still resolves, because one signal is never enough. Per-call gate: `makeDecision` REJECTs a fabricated-place row. Boot net: `enforceUnconfiguredProfileGeoMatches()` (**boot totality 45 → 46**) removes exactly two provable classes — (1) `fabricated_place`, fleet-wide, and (2) a place-EXCLUSIVE row on an unconfigured profile. **This is NOT "MISSING = NEUTRAL"**: `enforceDeclaredPlaceScopeMatches` still leaves a stateless profile alone, because silence is not a denial; here the profile positively declares junk across three families. Candidate discovery is a **SQL predicate** (`PLACEHOLDER_SECTION_LIKE_PATTERNS`, a deliberate superset the JS detector re-adjudicates) so the bound limits DELETES, never DISCOVERY. The CATALOG row is never deleted and NO profile content is ever touched. `ENFORCE_UNCONFIGURED_PROFILE_SCOPE=0` count-only; `UNCONFIGURED_PROFILE_PURGE_LIMIT` (2000). Reporting: `runProfileDiscoveryLive` returns `skipped: 'profile_unconfigured'` with `missing_prerequisites` NAMED (the EVA "blocked, with the prerequisite named" posture) instead of crawling and reporting a shortfall, and `profileResultCoverageAudit` reports `unconfigured` as its own class — excluded from `below_result_target` AND from `needs_rediscovery`, so the floor never chases a profile that can never be satisfied | `backend/tests/unconfiguredProfileHonesty.test.js` (29 tests; mutation-verified — a no-op sweep body reddens 7, the pre-fix regex 4, the pre-fix `placeLabel` 1, `MIN_CORROBORATING_FAMILIES=1` 2, restoring the inferred-`signals.needs` probe 8, removing the placeholder-ZIP gate 2, and making the floor census ignore the verdict 1) + `backend/tests/enforceInvariants.test.js` (totality 46) |
| A real applicant's SITUATION has a source lane, and a lane is added WITHOUT widening a gate (2026-08-02, the four-persona audit). Four situations the owner named were run END TO END through the real planner + `runProfileDiscoveryLive` and MEASURED, not reasoned about. Three had **no source lane at all**: zero registry occurrences of `foreclos*`/`mortgage`/`HAF`/`housing counseling`, zero of `eviction`/`ERAP`/`rental assistance`/`legal aid`, and `church`/`ministry` present only as applicant_types on four unrelated rows. The need VOCABULARY had carried all of it the whole time (`needTaxonomy` `rent` → 'eviction prevention', `housing` → 'mortgage assistance', `legal` → 'eviction defense') — the words existed, nothing to search existed. Verbatim results before: an Indiana homeowner four months behind on his mortgage got 40 rows / 35 pointers headed by "Benefits.gov finder — substance_recovery benefits", "HLAA financial assistance for hearing aids" and "Arthritis Foundation help line"; an Ohio renter with an eviction hearing in two weeks got 19 rows / 3 awardable whose only direct grant was "HSLDA Compassion Grants (homeschool families)"; a northern-Ohio congregation with a failing roof got "PetSmart Charities grant programs", "Petco Love" and NSF "Combustion and Fire Systems" | Registry: `hud_avoiding_foreclosure` + `cfpb_rent_and_housing_help` + `lawhelp_legal_aid` (housing-loss lane) and `national_fund_sacred_places` + `partners_sacred_places` + `nthp_preservation_grants` (congregation lane), plus `va_veteran_benefits`; every base_url fetch-verified 200 on 2026-08-02 and recorded POST-redirect. **THE FLOOD THAT WAS CAUGHT BEFORE SHIPPING, and the shape that replaced it:** the state housing-finance-agency lane was first built as 51 state-scoped rows generated from `STATE_REGISTRY` (which already curates every state's `housingName`/`housingUrl`), mirroring `STATE_BENEFITS_PORTALS`. Measured against all 33 real prod profiles it added **+54 sources each** to the 5 profiles with no resolvable state, because `planner.servesGeo` keeps a state-scoped source when the thesis has no state ("unknown location -> keep") — the 2026-07 geo-link flood class, one level down. The gate is in `planner.js`, which another agent owned, and the rule is *never fix a gap by widening a gate*, so the lane became **ONE national row** (`state_housing_finance_agency`) whose `adapters/stateHousingAgencyAdapter.js` resolves the profile's OWN agency from `STATE_REGISTRY` ("Indiana Housing & Community Development Authority — homeowner & renter housing programs"), and a state it cannot parse (prod's junk `"USA"`, a full name, `ZZ`) resolves to NOTHING rather than a wrong state. Re-measured on the same 33 profiles: **max +4 per profile, mean +3.48, zero above +4**. `LANE_OF_SOURCE` files it under `federal_benefits`, deliberately NOT `state_programs` — a national row there makes the "does THIS profile's state have a source" gap unreachable for every profile (caught by the existing junk-state test before it shipped). **STILL OPEN — the veteran persona is NOT fixed here.** `deriveApplicantTypes` emits `applicant_types:['business']` for a veteran who owns a business, so ALL SIX veteran sources are planner-excluded `applicant_type_not_served`; `gatherStructuredApplicantHints` reads `section.data`, but `profileContextToThesisInput` hands it `{title, body}`, so its five military boolean checks are DEAD CODE on the live path. Exact patch is in the PR body; the files belong to `feat/stage-of-life-gate` | `backend/tests/personaCoverageLanes.test.js` (11 tests: registry+adapter+lane totality, per-persona selection, congregation-lane non-leak, the ONE-row flood guard, junk-state resolves-to-nothing, the ≤5 ceiling; mutation-verified — making the row state-scoped reddens 3) |
| A DECLARED service status reaches the crawlers, and PROSE THAT MENTIONS A FACT IS NOT A DECLARATION OF IT (2026-08-02). Two defects, the second introduced by the fix for the first. **(1) DEAD CODE ON THE LIVE PATH:** `gatherStructuredApplicantHints` (`crawler-os/profileIntelligence.js`) reads `section.data`, but `profileContextToThesisInput` (`crawlerOsPersistenceCore`) hands `buildThesis` `{title, body}` entries — `body` is the section run through `sectionSignalText`. So all five military boolean checks NEVER EXECUTED in production. A profile with `military_service.veteran=true` + `occupation.small_business_owner=true` emitted `applicant_types:['business']`, and the planner excluded ALL SIX veteran sources (`sba_veteran_business`, `sba_vboc`, `sba_boots_to_business`, `dol_tap`, `military_onesource`, `va_housing_grants`) as `applicant_type_not_served`. Replayed over all 33 real prod profiles: **ZERO reached any veteran source** — including Brian Nicholas Newman, whose section reads `{veteran:true, disabled_veteran:true, notes:"United States Air Force veteran with 90% VA disability rating."}` and who is the fleet's ONLY real veteran. **(2) THE NEGATION TRAP:** the first fix also matched a word-bounded token in the section's rendered TEXT, copying `hasStructuredFarmerFlag`'s two-shape rule. Replayed over the same 33 profiles it promoted **THREE** profiles whose `military_service` says `veteran:false`, because the accompanying `notes` read "No military affiliation or documentation indicating **veteran** status" — the word appears inside its own DENIAL. One of them ("Focus Forward Ministry", a faith-based nonprofit) then inherited the entire individual/household safety net through the veteran bucket — 211, LIHEAP, Medicaid, SSA disability, HUD locator, findhelp, Reeve paralysis — **+24 sources on one org profile**. The text rule was 3 false positives out of 4 | `structuredMilitaryApplicantTypes()` in `profileIntelligence.js` reads **ONLY explicit `=== true` booleans**, never rendered text, scoped to the military-shaped sections; additive (no existing bucket is ever removed). The channel is structural: `profileContextToThesisInput` now carries `military_service: sections?.military_service ?? null` beside the `age`/`schools`/`employer`/`derived_facts` channels it already carries. **A schema with an explicit `false` is the only thing that can say NO — the flag is the fact, and silence is neither a denial nor a declaration.** Re-measured structurally over the same 33 prod profiles: **1 profile gains exactly the 6 veteran sources, 32 gain nothing**, and the ministry is untouched. **PARTIAL, and not claimed as more:** the veteran persona now reaches "Veterans Business Outreach Center program", "Boots to Business entrepreneurship training" and "SBA veteran-owned business resources" — but at ranks #9/#14/#6, below "NIH Small Business Technology Transfer Grant (Parent STTR)" and "Aquatic Invasive Species Interjurisdictional Grants to the Great Lakes States", which still clear ACCEPT for a food truck. Reaching the lane is not the same as ranking it | `backend/tests/veteranApplicantIdentity.test.js` (10 tests: the three real prod DENIAL strings verbatim, the serves-veterans-org non-promotion, additive identity, and a **DB→thesis wiring test** driving the real `loadProfileContext → profileContextToThesisInput → buildThesis` chain; mutation-verified — removing the additive line reddens 3, restoring the text fallback reddens 3, and deleting `military_service` from the thesis input reddens the wiring test, which the hand-built-input tests could NOT catch) |
| Amy's cohort tests SINGLE-AXIS archetypes, and a finding with NO LEVER can never close (2026-08-02). Measured over the 21 cohort days in prod `system_kv amy_flywheel_cohort`: `institution_recall_miss` is the ONLY finding present on **21 of 21 days — 282 occurrences, never once green** — while `weak_match` appeared on **2** days (11 occurrences) and led the report. The cause is structural: `buildApprovalQueue` built items from `status==='zero'`, `false_positives`, `ineligible_accepts`, `status==='weak'` and `sources_failed` and **never read `e.findings`**, so a recall miss could not become an approval item, could not acquire a lever, and could not enter the durable ledger from #1085 (live in prod 2026-08-02 with 6 entries, all `nights_open:1`, none from a recall class). Separately, all 32 archetypes named ONE fact — a veteran, or a business; a church, or a nonprofit; `individual_assistance` declared `housing {status:'at-risk', type:'rent'}` with no eviction/foreclosure posture — while the planner gates on the INTERSECTION. Indiana was not even in `LOCATIONS`, so no Indiana profile had ever been crawled. `first_responder` and `agricultural_cooperative` were in NO `LANE_CATEGORY_AFFINITY` lane, so fleet gap pressure could never weight them | Four INTERSECTION archetypes (`veteran_entrepreneur`, `struggling_congregation`, `homeowner_foreclosure`, `renter_eviction` — foreclosure is `housing.type:'own'`, eviction is `'rent'`; the old at-risk flag could not tell them apart), Indiana added to `LOCATIONS`, and the two orphan categories given lanes. `buildApprovalQueue` gains a `query_breadth` lever for `INSTITUTION_RECALL_MISS`/`HYPERLOCAL_RECALL_MISS` grouped per (finding, category), carrying the concrete missed subjects (the school names, the county) so the item names work someone can do rather than a count. **This does not itself close the recall miss — it gives the 21-of-21-day finding an actor for the first time; whether the number falls is a claim for a future measurement, not this one** | `backend/tests/amyPersonaArchetypes.test.js` (10 tests incl. the affinity TOTALITY that found the two orphan categories, own-vs-rent separation, and a clean cohort producing no item; mutation-verified — deleting the lever branch reddens 2) |
| A RATE finding is bounded by RECENCY, not only by COUNT — otherwise it cannot go green (2026-08-01). Anya's report read "44% of the last 200 tool calls failed / admin.db.query (87)" for a defect already fixed (a SQL guard matching `update` inside `updated_at`, PR #1084). The 87 failures were ONE ~2-hour burst on 2026-07-30 (87 of that day's 138 calls); prod measured 18 calls / 0 failures on each of the two days since. `agent.anya.toolFailures` read the newest 200 rows with NO time bound, so at ~18–30 calls/day those rows needed ~six more days to age out — the same red line every morning for a week. Registry-wide the shape appeared three times: `agent.anya.toolFailures`, `agent.john.draftHealth`, `crawler.coverageDegraded` | The window is the INTERSECTION of "the newest N rows" and "rows newer than M hours" (`applyRecencyWindow` + `resolveRateWindowHours` in `samRegistry.js`; env `ANYA_TOOL_FAILURE_WINDOW_HOURS` 24h / `JOHN_RUN_HEALTH_WINDOW_HOURS` 168h / `CRAWLER_COVERAGE_WINDOW_HOURS` 24h). Ordering by the SAME column the filter uses is why this is NOT the #944 post-LIMIT anti-pattern — the newest rows are always inside the LIMIT, so nothing can be starved. **24h is chosen, not defaulted**: at ~18 calls/day a 6h window holds ~4 calls, below any honest minimum sample, so the check would be blind most of the day; 24h is the smallest window clearing the minimum sample on the QUIETEST measured day. A real burst is self-amplifying (it adds calls as well as failures) so it always arrives with its own denominator and fires within hours. **MIN_SAMPLE is an explicit rule, not an emergent one** (10 calls; with a 30% bar that is ≥4 real failures, so "1 of 2 failed = 50%" can never page). An EMPTY window is reported as "cannot speak to it", never as health. TRAP: the timestamp is a Date on prod Postgres and a bare `CURRENT_TIMESTAMP` string on SQLite, which `Date.parse` reads as LOCAL time — `rowTimestampMs` pins a zone-less string to UTC; if NO row has a readable timestamp the window degrades to count-only and SAYS so | `backend/tests/samRateWindowRecency.test.js` (14 tests; mutation-verified 5 ways — ignoring the window, dropping MIN_SAMPLE, ignoring John's window, dropping the crawler HAVING bound, and parsing a bare timestamp as local time each redden exactly their own guard) |
| A POINTER never "needs an API adapter", and a metric whose denominator is mostly pointers measures THE WORLD (2026-08-01). Anya reported "2 active pipeline grant(s) were READ but their source could not be parsed — they need an API adapter. Top host(s): nfb.org ×1, scholarships.com ×1." Both are pointers: `www.scholarships.com` is a scholarship DIRECTORY (a list of other people's scholarships) and the nfb.org row is a REFERRAL — CLAUDE.md already says a locator "is a pointer, never an award" and carries no per-award amount BY DESIGN, so no adapter could ever yield a figure and the sweep had already burned fetch attempts chasing one. Cause: `pipeline.amountCoverage` HAND-TYPED `('directory','benefit')` as the no-figure set, while prod's catalog carries `referral` (119 rows) and `school_portal` (102) as well — measured read-only. Those fell through into `unanswered_unreadable`, the ONE bucket that reds the report and names adapter work | `backend/config/opportunityKindClasses.js` — the REGISTRY (`POINTER_KINDS` + `NO_FIXED_AWARD_KINDS` → `NO_PER_AWARD_FIGURE_KINDS`, plus `noPerAwardFigureKindSql()`); every predicate compares LOWER-cased because prod stores BOTH `directory` and `DIRECTORY`, `benefit` and `BENEFIT`. A static drift tripwire pins it to `matchDecisionIntegrity.RESOURCE_KINDS_SQL`, which held the same set as a literal. The census now also reports an AWARD-BEARING denominator alongside the raw one: the headline "% carrying a dollar value" counts every active row and ~40% of them are pointer/benefit kinds that publish no figure by design, so it reads as a stuck number. **The RATCHET deliberately keeps using the RAW figure** — its history is in those units and its job is to notice values DISAPPEARING from any row, so re-basing it would blind the one check here that ever caught a live bug (#950) | `backend/tests/opportunityKindClasses.test.js` (7 tests; mutation-verified in BOTH directions — shrinking the registry to the old hand-typed subset reddens the pointer test, widening it to swallow DIRECT_GRANT/ PROGRAM reddens "STILL counts a burned AWARD row", so the fix can neither miss pointers nor go inert) |
| A SQL-safety verdict is a claim about a STATEMENT, never about a SUBSTRING (2026-08-01, Anya's `admin_tool_integrity` finding: "44% of Anya's last 200 tool calls failed. Top failing: admin.db.query (87)"). `adminDbQuery` blocked dangerous SQL with `dangerousKeywords.some((k) => trimmedSql.includes(k))` — a bare substring test that cannot tell a statement from an identifier, in a schema where `updated_at` ⊃ *update*, `created_at`/`created_by` ⊃ *create*, `deleted_at` ⊃ *delete*, `execution_time_ms` ⊃ *exec*, and **`grants` ⊃ *grant*** — i.e. every timestamped table in the product, and the table this application is named after, were structurally unqueryable. Measured read-only in prod `anya_tool_usage`: **90 of 90** all-time `admin.db.query` failures carried the one string "Query contains forbidden keywords", and replaying the shipped rule over the stored `parameters` showed **0** contained a forbidden keyword at a word boundary (86 tripped on `updated_at`, 7 on `created_at`/`created_by`). **The metric was NOT lying** — Sam's `agent.anya.toolFailures` correctly reported 87 real rejections of legitimate read-only diagnostic SELECTs. One line below, the row-limit clamp had the same bug in the other direction: `if (!trimmedSql.includes('limit'))` let a query whose only "limit" sat inside a string literal run **UNBOUNDED**. Amplification note: ONE rejected statement was re-issued **83 times in 30 minutes** (83 of the 87), because "Query contains forbidden keywords" names nothing a model caller can correct | `assertReadOnlyDiagnosticSql()` + the `READ_ONLY_SQL_FORBIDDEN_KEYWORDS` registry (`backend/services/anyaAdminTools.js`) — the single choke point; each keyword is matched at a **word boundary** (`\bkeyword\b`), which is strictly MORE precise, never weaker: the true-positive set is unchanged and asserted keyword-by-keyword. Do **NOT** "fix" a false rejection by deleting a keyword from the registry — the substring/token distinction is the bug, not the list. The real read-only perimeter is the trio the list only backstops (must start with SELECT, no `;`, no second SELECT); the clamp is `\blimit\s+\d` (a DIGIT must follow, so a literal cannot suppress it) and is appended on its OWN LINE so a trailing `-- comment` cannot swallow it. The error NAMES the offending token and states that re-sending will not help | `backend/tests/adminDbQueryGuard.test.js` (11 tests; the 8 real prod statements are the fixture, with an inline A/B proving the old rule rejected all 8 — mutation-verified: restoring the substring keyword rule reddens 5, restoring the substring `limit` check reddens 1 by returning 12 unbounded rows where 3 were asked for. Includes the keyword TOTALITY test, the perimeter tests, and a telemetry assertion that a burst of these rejections FAILS `agent.anya.toolFailures` **and goes green when they stop**). Prod-verified 2026-08-01: all 8 statements now pass the guard and execute against prod Postgres, 0 still failing |
| A gate's PHRASE is a statement the SOURCE made, never one a JOIN created (2026-08-01, the golden-outcome sentinel's first real catch). Every audience gate in `services/matching/needFirstMatchPolicyV2.js` (`family caregiver`, `respite care`, `child care`, `foster youth`, `international students only`, the PROFESSION and death-survivor patterns) is `.test()`ed against ONE string built by `[...scalars, ...categories, ...keywords].join(" ")` — so the boundary between two INDEPENDENT list members is indistinguishable from the space inside a real phrase, and any two adjacent tokens fabricate a phrase no source ever wrote. It fired on the owner-verified ECF CHOICES profiles: the `tn_ecf_choices` registry row declares `applicant_types: [individual, family, veteran, disabled, caregiver]`, which `crawler-os/matchEngine.opportunityToCanonicalOpportunity` folds into `categories` ending `… disabled, family, caregiver` → the literal substring **"family caregiver"** → hard mismatch → `decision: REJECT` → `crawlerOsPersistenceCore` drops rejects (`decision === "reject" → continue`) → and the match store is a ROLLING SNAPSHOT, so the rows vanished on the profiles' next crawl. **Prod 2026-08-01: 0 match rows for `tn_ecf_choices` fleet-wide against 13 ACTIVE catalog rows, with the lane recording `planned:true queried:true failed:false found:13` for BOTH profiles minutes earlier.** The funder's own prose reads "…Essential Family Supports for family caregiverS" — plural, which the `caregiver` word-boundary pattern never matched, so the ONLY string that ever matched was the one the join invented. Bisected: ACCEPT at 23dc30d3^, REJECT at 23dc30d3 (#1034, 2026-07-29) — NOT the four matching PRs that merged the morning the sentinel fired | `TEXT_FRAGMENT_SEPARATOR` (space-pipe-space) + `joinTextFragments()` in `needFirstMatchPolicyV2.js`, re-used by the `needFirstMatchPolicy.js` wrapper. A pipe is not a word character and appears in no pattern here, so the word-boundary anchors around a REAL phrase behave exactly as before — the only strings removed are ones that span a fragment boundary. This LOWERS no bar: a genuine "Family Caregiver Support Program" still carries the phrase inside its own title and is still hard-matched. **NOT FIXED HERE (reported, no prod evidence of a live miss): the PROFILE side (`profilePurposeText`/`signalText`) has the same shape, but its output passes through `normalizeText`, which strips punctuation — fixing it needs per-fragment normalization and its own evidence, so it is deliberately left alone rather than changed blind.** | `backend/tests/needFirstFabricatedPhrase.test.js` (6 tests; reverting the separator to a bare space reddens 4, neutering the caregiver gate reddens the other 2 — the two mutation sets are disjoint by design, so the fix cannot go inert and the gate cannot lose its teeth) |
| Amy's improvement approval queue has a CLOCK, a CONSUMER, and only asks for what an approval can close (2026-08-01, the third instance of the write-only-queue shape after `web_parity_gap_queue` and the adapter wishlist). `buildApprovalQueue` is recomputed from scratch every run and written whole over `system_kv amy_approval_queue`, so **no item had any state at all** — no first-seen stamp, no age, no record that anything ever acted — and Anya's morning email rendered N identical ageless "Needs your approval:" lines that read the same on night 1 and night 30. Measured read-only in prod 2026-07-31: `amy_recent_runs` carries a NON-EMPTY queue on **all 20 retained runs** (size 1 for the eighteen runs 2026-07-15→2026-07-29, then 4, then 6) and `system_kv` **does not contain `amy_generic_title_additions`** — the key the ONLY apply path in the product (`POST /api/amy/relevance-vocabulary` → `applyGenericTitleAdditions` → `persistGenericTitleAdditions`) writes. **Zero approval items have ever been actioned.** Three of the five levers had no apply surface anywhere (`eligibility_gate`, `adapter_source_health`, and — see the row's second half — `scoring_weights` for a locator-only category), so the owner was being asked to approve changes no approval could apply | `backend/services/amy/approvalLedger.js` — (1) a durable ledger (`system_kv amy_approval_ledger`) keyed by item id carrying `first_seen_at`/`nights_open`/`runs_seen`, folded once per run by `recordApprovalQueue` from `amyAgent`; an item that STOPS REPRODUCING is CLOSED (`stopped_reproducing`) — the queue is recomputed from scratch, so "stopped being emitted" is the only honest convergence signal, exactly like the wishlist overlay. **An auto-applied lever NEVER closes the item** (`last_auto_applied_at` is recorded, the close is earned next run by the finding going away) — "we applied a change" is not evidence it worked. Idempotent per `runId`; open entries are never trimmed. (2) `LEVER_REGISTRY` declares each lever's actionability — AUTO (Amy applies it, re-crawl validated + auto-reverting) / OWNER_API (a named route) / CODE_CHANGE (**no approval can close it**) — with a TOTALITY test that every emitted lever is registered and every OWNER_API surface names a route that literally exists in `routes/amy.js`. `requires_approval` now means "a human CAN approve this", never "a human is being asked to", and `summarizeAmyFlywheel` renders a CODE_CHANGE item as "Needs a CODE change (no approval can close this)" with its reason, an owner ask with its AGE + the exact closing surface, and a ledger CLOSE as an EDIT. Stale bar `AMY_APPROVAL_STALE_NIGHTS` (7); `AMY_APPROVAL_LEDGER=0` = compute-and-report without persisting. **Companion fix — a locator-only weak match is a COVERAGE gap, not a scoring gap**: `isRecommendable` admits an ACCEPT of any kind plus a DIRECTORY locator at REVIEW, so a recommendation list with zero ACCEPTs is BY CONSTRUCTION all locators — and the locator rule forbids a locator from ever accepting. `weak_match` reported "top score 54 (review-band only)" (a LOCATOR's score, reading as a near-miss) and minted a `scoring_weights` ask, a lever that provably cannot move the final score (W_* act only inside `topical_evidence`). Prod 2026-07-30: four such items — tribal_org 54, community_development_corp/housing_authority/workforce_org all exactly 10. `evaluateDiscovery` now carries `locator_only`/`direct_recommendations`/`top_direct_score`, the message names the real fact, and `buildApprovalQueue` routes it at `source_keyword_coverage` (an AUTO lever) while `proposeCoverageOverrides` accepts a locator-only gap at `minLocatorOnly` (1) — a locator-only crawl that stored 170+ rows is not a search outage. `housing_authority`→`hud_homeless_assistance` and `tribal_org`→`bia_tribal_programs` added to `CATEGORY_COVERAGE` (they had no lane at all). **Do NOT "fix" this by dropping locators from the list or letting them ACCEPT — those are the two forbidden ends of the locator defect** | `backend/tests/amyApprovalLedger.test.js` (20 tests; **9 mutations verified**: old weak_match message ×1, locator-only routing disabled ×3, `requires_approval` always true ×2, ledger never ages ×3, never closes ×3, applied==closed ×1, OWNER_API route removed ×1, unregistered-lever-treated-as-auto ×1, coverage ignores locator-only ×1) + `backend/tests/amyFlywheelCohort.test.js` ("an owner ask carries its AGE and the surface that closes it", "a CODE-CHANGE lever is never rendered as Needs your approval", "a CLOSED ledger item is reported as an edit") |

| A STUDENT'S OWN SCHOOL'S AID REACHES THAT STUDENT (2026-08-01, `institution_recall_miss` — the only Amy finding RED on **21 of 21** cohort days, 290 occurrences). Measured read-only in prod: **52 ACTIVE `Middle Tennessee State University` catalog rows** (Peggy Perry Belcher Scholarship Fund, MTSU Guaranteed Scholarship, Buchanan Fellowship…) carry **ZERO match rows for ANY profile**, while Anastasia White's `education.current_institution` **IS** MTSU — and her ONLY university-sponsored matches are seven `Wayne County Community College District` / `Wayne State University` (**Michigan**) rows at scores 3–4, cross-matched onto **28 profiles** including nonprofits and a biolab. **THE ENGINE IS NOT THE DROP POINT**: replaying the real `services/matchEngine.computeMatchDecision` on the real pair (her live profile row + all live sections, the live Peggy Perry Belcher row) returns **ACCEPT / score 100** — "Matches 70 of the profile's 70 data points", "Geography: State match". The pair is simply never SCORED, because `profile_opportunity_matches` is a ROLLING SNAPSHOT: `crawlerOsPersistenceCore.persistRun` DELETEs a profile's `crawler-os`/`crawler-os-xmatch` rows and re-inserts only what THAT run re-found. An institution scholarship is reachable ONLY through the open-web lane keyed on the school's NAME (no registry source lists one school's endowed funds), so the first registry-only run of that profile — web lane off, rate-limited, or no hits — erases the whole institution set and nothing ever looks at the surviving catalog row again. Anastasia's 35 live `crawler-os` rows are ALL registry lanes (`benefits_gov`, `hrsa_*`, `tn_benefits`, `findhelp_*`) and not one is from the web lane. **Fleet-wide, same measurement: `source='web_search'` holds 7,645 active catalog rows of which 172 have EVER carried a match row (2.2%)** — institutions are the sharpest instance of a far larger recall hole (`osm_overpass` 2,756→0, `verified_real` 865→0, `school_portal` 164→0), and only the institution slice is fixed here | Canonical facts: `backend/config/profileInstitutions.js` — the FIELD REGISTRY (`PROFILE_INSTITUTION_FIELDS`, each entry tagged `attendance` or `aspiration`), `resolveAttendedInstitutions` / `resolveSeedInstitutions`, and `opportunitySponsoredByInstitution`. `crawlerOsPersistenceCore.extractEducationEmployment` now READS the same registry, so a new school field cannot reach discovery seeding and silently miss the sweep. **Boot net**: `enforceInstitutionAidLinkage()`, run right after `student_aid_eligibility` and BEFORE `no_dangling_matches` / `persisted_match_decision_integrity` so the rows it adds are validated by them the SAME boot. It authorizes the canonical engine to LOOK at an attendance-linked pair — **the engine still decides**, and a REJECT is never written. Rows land as `matcher_version='institution-link'`, which the reconcile DELETE deliberately does not name (the same reason `web-llm` exists) and which `SURFACED_MATCHER_VERSIONS` now surfaces; `ON CONFLICT DO NOTHING` so the profile's own `crawler-os` match always wins. **TWO NARROWINGS STOP THIS BECOMING THE FLOOD IT CURES** (the farm-lane `['farm','individual','family']` mistake): (1) **ATTENDANCE, NOT ASPIRATION** — only `current_institution` / `basic_information.current_school` / a **committed** university application / `education.schools.name`. `target_colleges` is deliberately excluded: Anastasia lists **nineteen**, and nineteen schools' aid is the flood; aspiration still seeds discovery QUERIES, it just never authorizes a match. (2) **THE WHOLE NAME, NOT ONE WORD** (the Yana-lead rule one door over) — `opportunitySponsoredByInstitution` requires **bidirectional distinctive-token EQUALITY** on the SPONSOR: one-directional coverage would admit "Tennessee State University" ⊂ "Middle Tennessee State University", and `.some()` would admit `wayne` ⊂ both Wayne schools. **`state` is NOT in `GENERIC_INSTITUTION_WORDS`** — "Ohio University" and "Ohio State University" are different real schools and differ only by that token. Evidence is the SPONSOR only: a scholarship merely LISTED on a school's page is not that school's award. Candidate discovery is a SQL PREDICATE (`LOWER(sponsor) LIKE` the longest distinctive token — a deliberate SUPERSET the JS gate adjudicates), never a post-LIMIT filter (#944 / the #1080 repeat); bounds `INSTITUTION_AID_LINK_LIMIT` (500 writes) / `INSTITUTION_AID_CANDIDATE_LIMIT` (500 rows per institution); `ENFORCE_INSTITUTION_AID_LINK=0` for count-only. Convergence (school changed → stale link removed) is skipped entirely on a truncated boot so a bound-limited pass can never delete rows it never re-derived | `backend/tests/institutionAidLinkage.test.js` (17 tests on the real prod rows; **mutation-verified 5 ways, each reddening only its own guard**: dropping the bidirectional size check reddens the false-positive test; letting aspiration count as attendance reddens 2; a no-op sweep body reddens 6; writing under `crawler-os` instead of `institution-link` reddens 4 incl. the verbatim-reconcile survival test; ignoring the engine's verdict reddens the REJECT test) + `backend/tests/enforceInvariants.test.js` (boot-step totality, 41→42) |
| Every profile is measured against its REQUESTED RESULT NUMBER, and a shortfall becomes a standing instruction to SEARCH WIDER (owner rule 2026-08-01, third clause of the crawler goal: "They will add to the database returns that fall below the profile's requested result number"). A floor already existed (`MIN_HEALTHY_SURFACED` = 3 on `surfaced_actionable`), an escalation loop already existed (a `low_results` gap → Anya brain → `buildWebQueries` broadening) and an actor already existed (the nightly `runProfileCoverageSweep` autoheal). **All three keyed on a count that treats a DIRECTORY as a result.** Prod 2026-08-01, all 33 real profiles: 3,924 of 7,009 surfaced match rows are `directory`, every profile reads 24–190 "results", and **0 of 33** fall below 3 — while, counting only rows that name money the profile could actually receive, the distribution is **min 0 · p25 7 · median 14 · p75 26 · max 86**, with `Melissa Justus` at **0 awardable behind 25 actionable** (she was "healed" 2→4 that same day and now reads healthy holding nothing she can apply to) and `William` at **0 behind 24**. Two further defects sat underneath: the heal queue **ranked by the padded count** and had **no convergence at all** — it re-ran `runProfileDiscoveryLive` with identical arguments nightly with no memory of what had been tried (Noor Hassan's `institution_gap:Lewiston High School`, a high school that sponsors no catalog aid, gained +1 result per night forever); and **the broadening branch could not fire**: `buildWebQueries`' `low_results` queries landed in the truncatable `extra` pool AND were byte-identical to queries the ordinary pool already emitted, so `add`'s global `seen` set refused the forced copy and the final `.slice(0, max)` cut it. The "too few results ⇒ search wider" hook had never run for a query-rich profile | **The count** is `surfaced_awardable` in `services/coverageAudit/profileResultCoverageAudit.js` — the ONE detection module (no drift): surfaced ∧ active ∧ `qualifiesForDisplay` ∧ not a templated geo-stub ∧ not past deadline ∧ **not a POINTER kind** (`config/opportunityKindClasses.isPointerKind` — the registry, never a hand-typed subset). A BENEFIT (Pell/SSI/LIHEAP) COUNTS: it publishes no fixed figure but IS the thing you apply to. This is a stricter DENOMINATOR, **never a looser ADMISSION rule** — directories still surface to the owner exactly as before, no score moves, no gate widens. **The number** lives in `backend/config/profileResultFloor.js`: `DEFAULT_PROFILE_RESULT_TARGET` = **10** (above p25, below the median — it names the under-served third without putting half the fleet in permanent backfill, and is well clear of `MIN_HEALTHY_SURFACED`, which stays the "BROKEN" alarm), fleet override `PROFILE_RESULT_TARGET`, per-profile override in the ledger. **Deliberately NOT a profile field**: `PROFILE_SCHEMA` fields default to SCORED (`profileSchema.isFieldScored`) and `match_score` is *matched data points ÷ total data points*, so minting one would change every profile's score to store a service level. **The backfill** is the nightly `runProfileCoverageSweep` heal queue, now ledger-gated (`system_kv profile_result_floor_state`) and ordered **fewest-attempts-first, then deepest shortfall** — the attempts key is what stops retries starving never-tried profiles, the exact way `enforceAmountEnrichment` starved for a week. Burn semantics are `enforceAmountEnrichment`'s: a crawl that threw or was SKIPPED is `transient` and spends **nothing**; a productive pass RESETS the budget; `RESULT_FLOOR_MAX_ATTEMPTS` (3) fruitless passes record an EVIDENCED `exhausted` verdict ("searched N lanes and M queries … K candidates reached the engine, K rejected, 0 added. Found 2 of a requested 10") that is REPORTED on every subsequent sweep, not just the night it was decided. Re-opens on catalog drift (`RESULT_FLOOR_CATALOG_DRIFT_STEP` 500 active rows), a changed target, or a 14-day cooldown — and an UNREADABLE `exhausted_at` returns 0 days, never Infinity, or the bound becomes the infinite retry it exists to stop. **The escalation lowers no bar**: a shortfall emits `result_floor_shortfall` AND `low_results` from `classifyGaps`, and `buildWebQueries` turns them into broader-geography / adjacent-need SEARCHES; every hit still faces the full fetch → extract → reality gate → match engine stack. `force()` promotes a gap-steering query into `forced` while leaving `extra` untouched (shortening it shifts the `seed` rotation and silently drops a different baseline query — additivity is a property of the POOL, asserted uncapped: 23 → 26 queries, none lost), and duplicates collapse at final assembly. **A broad catalog re-offer was considered and REJECTED on measurement, not taste**: #1090 measured three such keys against the real engine in prod and every one floods (declared geography over `verified_real`: 5,393 links; need∩geo over `web_search`: 14,822 links across 2,404 rows), and the same design replayed for THIS floor against the two zero-awardable profiles scans 4,000 pairs, has the engine reject ~2,134, and would write **1,471 rows to fill a target of 10** — topped by "Office of The Secretary; Privacy Act of 1974; System of Records" and "Tribal Issues Advisory Group" for a private individual. The floor may therefore ONLY ever cause more SEARCHING; it never re-adjudicates the catalog on a loose key. **Boot net**: `enforceProfileResultFloor()` — all-profiles census, no network (a full `runProfileDiscoveryLive` "legitimately runs for minutes"; the product's own route budgets 21s and expects to lose), bounded `RESULT_FLOOR_PROFILE_LIMIT` (500), `ENFORCE_PROFILE_RESULT_FLOOR=0` for count-only. **Verified on a full prod replica 2026-08-01: 33 profiles, 12 below target, 439 ms, idempotent on re-run** | `backend/tests/profileResultFloor.test.js` (32 tests — the count, the default, burn/cooldown/drift, the consumer tripwire, the boot net) + `backend/tests/profileResultFloorBackfill.test.js` (9 tests — queue order on the real prod shapes, transient-never-burns, skipped-never-burns, the evidenced verdict, no-further-retry, productive-resets, the write-only-queue tripwire); 10 mutations applied and all 10 killed |

| **A CROSS-PROFILE match row exists only on the engine's ACCEPT, a per-state agency row IS state-scoped, and a disease lane's rows reach only a DECLARED condition** (2026-08-03, the Robert White report: "the matching engine casts far too wide a net… the issue is global"). The owner's three junk classes on one TN paramedic student — disease/medical directories (Amputee Coalition, American Kidney Fund, Arthritis Foundation, Reeve, BIAA, Autism, HLAA), SIX other states' housing finance agencies (WV/IN/MI/AL/AR/OH), and stray fellowships (Goldwater, UAB Blazer) — were ONE structural door plus two accomplices. **(1) THE DOOR: the xmatch lane stored every non-REJECT.** `crawlerOsPersistenceCore.persistRun` wrote a cross-profile row for any REVIEW scored against a thesis STUB (no sections/signals), and `qualifiesForDisplay` shows any DIRECTORY at/above the review band — measured 2026-08-03T09:20Z: **4,792 xmatch rows / 38 profiles, 4,577 (95.5%) REVIEW**, incl. "Goldwater Scholarship" at score 2 on churches and biolabs. **(2) THE RESTORER made junk immortal**: `crawlerOsPersistence.snapshotResourceMatches` re-inserts every DIRECTORY/REFERRAL match a run didn't explicitly re-score as REJECT — and the #1102 planner gate GUARANTEES a refused lane is never re-scored, so Robert's pre-gate disease rows (evaluated 08-02T04:10Z) survived his 08-03 crawl BY DESIGN interaction. A lane-selection fix can never converge the match store through that pair. **(3) THE HFA adapter dropped the state it had just resolved**: minted rows ("West Virginia Housing Development Fund — …") entered `state NULL, is_national 1` with the state visible only as a FULL NAME — invisible to `declaredStateFromTitle`'s `"<Place>, XX — "` shape (18 catalog rows, 333 match rows). Bonus same-day find: #1108's GRAD_RX lookbehind still matched `graduate` inside "UNDERgraduate" — Robert's verbatim `highest_level` "College Student - Currently in undergraduate program" derived `graduate_student` | Writer gates: persistRunCore's xmatch branch stores **ACCEPT only** (a DIRECTORY can never ACCEPT, so NO cross-profile locator is ever stored — a locator lane is the OWN planner's per-profile decision; this does NOT touch `isRecommendable` or the primary crawl's REVIEW locators, the two forbidden ends of the locator defect); the facade snapshot holds the same bar (`SNAPSHOT_DECISION_SQL`); `stateHousingAgencyAdapter` emits `geography:{national:false,states:[st]}` for a resolved agency (unresolved generic row honestly stays national). Boot nets (`enforceInvariants.js`, totality 49 → 52): `enforceStateAgencyGeoScope()` (re-scopes minted rows by EXACT `STATE_REGISTRY.housingName` sponsor/title lookup — never a fuzzy state-name grep; then purges cross-state agency matches, MISSING = NEUTRAL; `ENFORCE_STATE_AGENCY_GEO_SCOPE=0`, `STATE_AGENCY_GEO_LIMIT` 500) → `enforceCrossProfileMatchPrecision()` (SQL predicate `matcher_version='crawler-os-xmatch' AND decision <> 'accept'`; `ENFORCE_XMATCH_PRECISION=0`; `MATCH_SCOPE_PURGE_LIMIT`) → `enforceConditionLaneMatchScope()` — the match-store half of #1102, SAME vocabulary (`DISEASE_SPECIFIC_SOURCE_IDS` + `sourceServesDeclaredCondition` vs the thesis's `declared_health_terms`); MISSING = NEUTRAL (unbuildable thesis / non-array terms adjudicate nothing; an EMPTY array is a read that found nothing and purges; `ENFORCE_CONDITION_LANE_SCOPE=0`). Stage registry gains `dual_enrollment` (an enrolled undergrad/grad provably cannot be a dual-enrolled high-schooler; "…and current college students" is an inclusionGuard, "earn college credit" deliberately is NOT) — consumed automatically by `makeDecision` + `enforceStageOfLifeMatchScope`. GRAD_RX gains a letter lookbehind + explicit `post-?graduate`. **Measured on a full prod replica (58 profiles / 17,541 opps / 9,347 match rows): HFA 17 catalog rows re-scoped + 192 matches purged /16 profiles; xmatch 4,347 purged /38 profiles; condition lanes scanned 303 → 269 purged /24 profiles (Dr. John White's SUPPORT-declared `arthritis` lane and every declaring profile's rows KEPT — 15 disease rows remain, all served); stage 2; total 9,347 → 4,537; second pass 0/0/0; cross-state HFA matches after: ZERO; Robert 190 → 141 with THDA kept.** TRAPS: the store MOVES hour to hour (Robert was 140 at 08:5xZ, 190 at 09:20Z — quote a timestamp); do NOT "fix" the resource-preserving restore by deleting it (it protects the profile's OWN locators from budget-limited runs — the precision bar belongs on the CROSS lane); the xmatch purge bound's tail is cleared by Robert's nightly cycle-start xmatch reset, so 2000/boot is enough; **known residuals, reported not fixed**: `student-aid-instate-link` hands a Cleveland State student ~40 MTSU/TSU-sponsored accepts (in-state + aid-shaped, but institution-EXCLUSIVE — transfer is real so not provably impossible), first-generation/major-restricted awards (Gayle Hearst, Pre-Dental) surface because the profile is SILENT on those facts and silence is never a denial — the fix is a profile QUESTION, not a gate; Anastasia's own-crawl Goldwater ACCEPT 100 carries no detectable stage vocabulary | `backend/tests/crawlerOsResourceReconciliation.test.js` ("CROSS-MATCH PRECISION" + "PRE-EXISTING non-ACCEPT xmatch… converges") + `backend/tests/enforceInvariants.test.js` (10 tests across the 3 sweeps: real prod titles, count-only, convergence, generic-descriptor floor, source-id-predicate non-leak) + `backend/tests/personaCoverageLanes.test.js` ("a MINTED per-state row DECLARES its state") + `backend/tests/stageOfLifeEligibility.test.js` (dual-enrollment BAR/KEEP + LIKE totality) + `backend/tests/profileDerivedFacts.test.js` ("undergraduate is never a graduate student", Robert verbatim). **Mutation-verified: 7 mutations, each printed applied=true killed=true** (xmatch gate reverted, snapshot filter dropped, 3 no-op sweep bodies, adapter geography dropped, registry class deleted) |

| **A funding RESULT is a thing someone can apply to — never a regulatory notice, a lead-gen funnel, a foreign funder, or an anonymized record** (2026-08-03, the owner's manual QA pass over all 36 profiles). Live junk on the Funding Sources tab + Item Funding scanner: SEC "Self-Regulatory Organization; Notice of Filing of Proposed Rule Change", IRS/OMB "Agency Information Collection Activities; Comment Request", a DOL "Prohibited Transaction" exemption for Meta Platforms, DOJ "Proposed Final Judgment" antitrust filings (Aiyana Begay, Axiom BioLabs, Focus Forward Ministry, Marisol Vega, Sasquatch Conservancy, Vermilion Church); UK "LA Flex"/"Energy Saving Grants" and India "Tata Trusts – Individual Medical Grants" for US individuals (Lisa Klinger, Vivian Millican; Tata mis-tagged "TN") — the ccTLD rule is structurally blind to a foreign funder on .org/.com; lead-gen "scholarships" (Portal Sync, Pawsitively Smart, YouGov); "U.S. Federal Agency – National" anonymized sponsors (minted by 3 adapter fallbacks); "Feral Swine Eradication" at 100% for a church; the recurring "Grants to USA Professional Dancers" for a KY farmer/senior. WHY the existing gates missed them: the engine's procedural gate existed but `RE_PROCEDURAL_NOTICE_TITLE` was narrower than the live junk, the match store is a ROLLING SNAPSHOT replaying stored ACCEPTs that predate every gate (the funding-sources route deliberately restores persisted truth), and the item scanner's stored-verdict branch bypassed live gating. Measured read-only in prod 2026-08-03T17:02:21Z (LIKE superset — JS classifiers adjudicate a subset): regulatory-title 68 match rows / 10 profiles / 41 catalog rows; federal-register-source 127/11/69; foreign-funder names (Tata/LA Flex/ESG) 11/8/5; lead-gen 2/1/2; anonymized-funder 227/11/140; COVID-rapid-response 1/1/1 | **THE SHARED FILTER CHAIN** `backend/config/fundingResultFilters.js` — owner-named predicates `isFundableOpportunity` / `passesEligibility` / `isRelevantGeo` + `classifyFundingResult`, consumed by BOTH the crawler results path (`matchEngine.makeDecision` per-call gates; `fundingSourcePresentation.partitionFundingSources` read-side hidden `not_a_grant` bucket + canonical `canonicalOpportunityKey` display dedup keeping the most complete record) AND the item scanner (`itemNeedSearch` both lanes) — never re-encode at a call site. Canonical procedural regex EXTENDED in place (opportunityNormalizer + the FR adapter's identical copy — drift tripwire holds) with the owner's verbatim junk phrases. Foreign funders: `FOREIGN_FUNDER_HOSTS`/`FOREIGN_FUNDER_NAMES`/`FOREIGN_FUNDER_SPONSORS` registries + `detectForeignOpportunity` (the ccTLD superset; identity patterns are funder NAMES, never topics — bare "energy saving grants" needs its UK qualifier). Eligibility: `INSTITUTIONAL_PASS_THROUGH_PATTERNS` (CDBG/ESG/EDA-planning/Voc-Rehab FORMULA — "State VR SERVICES" deliberately does NOT match, individuals use those/WIOA allotments/AARP Community Challenge → REJECT for individual roots), `GOVERNMENT_AGENCY_PROGRAM_PATTERNS` (feral swine, AIS interjurisdictional → REJECT when the type resolves non-agency), profession both-sides gate wired into `makeDecision` via `professionEligibility` (+`dance`/`farming` defs, `occupation` section consulted). Adapters stop minting "U.S. Federal Agency" (NULL sponsor rides no-sponsor handling). Text hygiene `utils/htmlTextHygiene.js` decodes entities at the ingest choke point + read paths. **Boot net** `enforceNonGrantNoticeMatches()` (totality 52 → 53; `ENFORCE_NON_GRANT_NOTICE_SCOPE=0` count-only; SQL LIKE superset before LIMIT; purges MATCH rows only — catalog never deleted; a merely-past deadline never purges, it re-buckets at presentation) + `enforceForeignJurisdictionMatches` extended with the funder host/name predicates. MISSING = NEUTRAL: a signal-less row ROUTES to Directories (never deleted), and a stored engine ACCEPT is exempt from no-signal routing (silence is not a denial); positive junk evidence overrides ACCEPT. Item scanner UI: reset preserves the selected profile (`src/pages/itemFundingState.js` — "Try broader words" used to drop it to "All profiles" → every follow-up returned 0 "Needs a profile") and the input clears on submit (the "wheelchair raused pickup truckmp" mid-cursor concatenation). Verifiability: re-runnable per-profile diff report `backend/scripts/qa-junk-filter-report.mjs` (read-only) | `backend/tests/fundingResultFilters.test.js` (61 tests on the owner's verbatim junk records) + `backend/tests/nonGrantNoticeSweep.test.js` (7) + `backend/tests/itemNeedSearch.test.js` ("the item scanner runs the SAME junk/country chain") + `src/pages/ItemFunding.helpers.test.js` + `backend/tests/enforceInvariants.test.js` (totality 53). **Mutation-verified via `backend/scripts/qa-junk-filter-mutations.mjs` — 9 mutations, each printing applied=true killed=true** (partition routing off ×3, dedup off ×1, engine foreign gate blind ×4, profession gate ×1, sweep no-op ×4, name-predicate dropped ×2, item-lane chain removed ×1, frontend reset reverted ×1, regex reverted ×10; the harness itself hardened after a live "0 reddened everywhere" runner failure — a missing vitest summary reports HARNESS failure, never 0) |

### "Recommended" ≠ "strong match" (the locator rule)

A run's recommendation list and the ACCEPT decision are DIFFERENT facts, and
re-coupling them re-creates a two-sided defect. A DIRECTORY locator is a
pointer: it promises a place to look, never an award, and carries no per-award
amount by design. It is admitted to the recommendation list at **REVIEW** by
`isRecommendable()` (`backend/crawler-os/matchEngine.js`) — the single admission
rule both `pipeline.js` and `webLane.js` must consult — while
`computeMatchDecision` never lets it claim ACCEPT.

> Do NOT "fix" a locator visibility problem by letting locators reach ACCEPT,
> and do NOT "fix" a false-positive problem by dropping locators from the list.
> Those are the two ends of one defect: recommendations used to require ACCEPT,
> so #886 made locators ACCEPT to keep the county/211/state-portal/disease
> fleet reachable (hyperlocal_recall_miss ×50/day), which manufactured Amy's
> false_positive ×56. Change the admission rule, not the honesty of the score.

Guard tests: `backend/crawler-os/tests/countyCityAndWishlistLanes.test.mjs`
(a locator stays recommendable AND never claims ACCEPT; a PROGRAM at REVIEW is
NOT recommendable). Amy's `false_positive` detector excludes declared DIRECTORY
locators — they are generically titled by design, so counting them measured the
naming convention, not the matcher; a generic-titled NON-locator clearing
ACCEPT is still a real false positive.

**Never weaken these guardrails:** NULL match_score is not junk; protected
(user-progressed) statuses are never auto-purged; `link_unverified` ≠ dead; all
comparisons are profile-scoped. See the "INVARIANTS" section of
`docs/canonical_rules.md` for the full rationale and the list of invariants that
are documented-but-not-yet-auto-enforced (source denylist, zero-result-but-no-junk,
agent observability).
