/**
 * Application Constants
 * Centralized timeout values and magic numbers for maintainability.
 */

// Timing constants (milliseconds)
export const ANALYSIS_DELAY_MS = 2000;
export const PROFILE_REFETCH_INTERVAL_MS = 30000;
export const RLS_PROPAGATION_DELAY_MS = 300;
export const MONITORING_INTERVAL_MS = 300000; // 5 minutes
export const BATCH_DELAY_MS = 10000;

// Pagination defaults
export const DEFAULT_PAGE_SIZE = 50;
export const MAX_LOG_ENTRIES = 100;